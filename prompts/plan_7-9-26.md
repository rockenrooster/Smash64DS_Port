/task Renderer fidelity fast-iteration session: land the two planner-confirmed
fixes (per-tile cache key wiring, near-plane clipping), then instrument the
white classes. FAST LOOP POLICY (Tyler): per-change gate = check-gbi-decode-
fixtures + canonical realtime verifier + capture/assert ONLY; one commit per
landed fix; dev-fast + boundary + RegressionCore ONCE at session end; NO full
Regression sweep until rendering is right (debt acknowledged and accepted).

Acceptance rule for every fix (non-negotiable): pixels must move. Do not land
"same screenshot, better counters" changes. Each fix's capture goes to
artifacts/visibility/2026-07-09_* with a one-line note of what changed.

CONFIRMED FIX A - per-tile state never reached its consumers (this is why last
night's per-tile work changed nothing on screen): nds_renderer.c:3183-3184
still populates the texture cache key from the SHARED clobbered fields
(stats->texture_tile_size_uls/ult), and the bind path still reads shared
dimension/origin/wrap fields. Wire BindTexture dimensions/origin/wrap/palette
AND the cache key (tile_uls/ult/lrs/lrt + wrap/mask/shift) directly from
stats->texture_tiles[active render tile]. Keep shared fields for monitoring
only. Add a cache-alias counter proving two source rects of one TMEM image no
longer collide. Expected pixels: fence alignment changes; some rainbow texel
reduction. If the fence moves at all, the wiring is live - iterate there.

CONFIRMED FIX B - no near-plane clipping in the projected-submit path (prime
suspect for fighters "broken into pieces"): nds_renderer.c:3463-3465 divides
by w with only a w==0 guard, then ndsRendererHardwareClampS64ToV16 (:1643)
saturates to +/-32767 - vertices at/behind the near plane fling to screen
extremes and stretch triangles across the frame. Steps:
  1. Surface the EXISTING gNdsRendererProfileHWVertexSaturateCount (:1680,
     :1696) in a RENDER_CLIP marker; run once. High count during battle =
     confirmed; near-zero = skip to the fallback below.
  2. Implement pre-divide near-plane triangle clipping (Sutherland-Hodgman
     against w = small positive epsilon, interpolate position/UV/color, emit
     0/1/2 triangles). Keep the w==0 guard as a backstop. The saturate clamp
     stays only as a never-hit safety; its counter should drop toward zero.
  3. Expected pixels: Mario/Fox resolve into coherent bodies. If saturation
     was near-zero instead, the fallback hypothesis is per-part DObj matrix
     assembly (push/pop pairing, MUL-vs-LOAD, billboard flags) - verify with
     the part-cluster check: projected part positions must cluster within
     ~+/-64 px of the fighter root's projected position.

WHITE CLASSES (tree, floor patches, stage bottom, bushes/flowers) - the
texture-only probe already proved these are NOT lighting washout. Instrument
before fixing: ndsRendererHardwareUseTexture (~:1893-1919) has FIVE untracked
FALSE-return paths - reject counters are blind to them. Add one counter per
path + a one-shot snapshot of the offending combine words/state flags. Run,
then fix per evidence, smallest first:
  - "never raised TEXTURE_STATE_ON": honor SETTILE|SETTIMG|LOADTILE in
    texture_mask as implicit texture-on when a render tile is armed (cite the
    decomp draw path using tile 0 unconditionally).
  - "combine clobbered": stop unconditionally overwriting the recorded
    combine (~:1014-1015) after texturing is armed; snapshot combine state
    per-triangle rather than last-writer-wins.
  - "2-cycle/TEXEL1": extend the TEXEL0 test to 2-cycle COMBINED chains,
    mirroring the existing OutputUsesColor cycle handling.
Escalation probes only if path counters are inconclusive (reference file
prompts/chatGPT55pro_7-9-26.md has ready sketches, apply by hand): exact-
sampled-texel diagnostic with cache disabled; tile mask-period (1<<mask wrap
vs rectangle); tile-6 secondary-load isolation for fighter FRAC/SPLIT
materials. All probes compile-time, default OFF, never shipped, reverted.

INPUT (bounded to 45 min; the hop is already localized by live evidence):
  Tyler tested the inputhud ROM: the raw-keys HUD line responds to held
  buttons, but the sy (game controller struct) line stays zero. Planner
  verified the osContGetReadData live shim (controller_backend.c:210-246)
  correctly reads ndsPlatformHeldKeys and maps pad0 - so the prime suspect
  is that NOTHING in the canonical mode-163 realtime loop ever calls it:
  the loop polls ndsPlatformReadInput() and discards the result, and the
  original per-frame controller read chain (osContStartReadData/
  osContGetReadData -> syControllerUpdateGlobalData -> gSYControllerDevices)
  may simply not be running in this slice.
  a. Confirm with one trace: who calls osContGetReadData in mode 163
     realtime? (gNdsControllerLiveReadCount staying 0 across the battle
     window proves no-caller.) If no caller, run the ORIGINAL controller
     update in the realtime loop - cite the decomp site where the game
     polls controllers per frame (scheduler/gametick controller process)
     and route it through the existing shim. Do not hand-write a bypass
     that pokes FTStruct directly.
  b. If there IS a caller and pad0 maps nonzero, the break is
     syControllerUpdateGlobalData -> gSYControllerDevices[0] - add one
     marker at that hop and fix only it.
  c. Playback hypothesis is CLOSED unless evidence reopens it: defaults
     off (controller_backend.c:95-118), verifier asserts it stays off,
     and live keys visibly reach the HUD in the shipped config.
  d. Rebuild BOTH ROMs (demo HUD=0, inputhud HUD=1) at session end -
     success = Tyler holds RIGHT and the sy line + mario px both move.

Gate ratchet (cheap, do once): add a detail-pixel fraction assertion to the
canonical gate - pixels that are non-clear AND non-white AND non-dominant-
green - baselined against today's capture, one-way upward. This is the metric
the white-class fixes must move.

Out of scope: wallpaper/SObj background (markers exist, composition deferred);
cache/perf and the raw DS matrix path repair (next slice after fidelity);
any new harness mode; any full TU import.

Session end: verify-dev-fast -Build, verify-boundary, RegressionCore prebuild
+ -NoBuild ONCE; check-docs; check-harness-registry; PORTING entry with per-
fix capture paths; commit per fix (WIP branch codex/wip-renderer-fidelity-
2026-07-09 for anything partial); snapshot -Mode Lean last. NO full sweep.

Constraints: decomp/ read-only; pixel gates one-way ratchets; renderer
behavior derives from game-submitted data with GBI/decomp citations; debug
probes compile-time default-off and never shipped; per-hypothesis timebox
~10 emulator runs / ~1h then checkpoint; one writer for the tree.
