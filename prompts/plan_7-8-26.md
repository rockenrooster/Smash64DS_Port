/task Complete the canonical HW battle scene: lit-shade/material precedence,
palettized texture binding, then the missing draw classes (fighters, platforms,
background). Two external reference patches are attached (from a ChatGPT
analysis of the 20260708_191229 snapshot) - treat them as validated hypotheses
plus sketches, apply surviving ideas BY HAND, never patch -p1. The probe patch
is diagnostic-only and must not land.

Current evidence: Dream Land silhouette + grass green render correctly; almost
all other surfaces are flat white with no texel detail; fighters, platforms,
and background are entirely absent; exactly one on-screen element samples real
texels (artifacts/visibility/shipped-hwtri-hudoff-verified.png).

Unifying mechanism to validate (planner-verified in code): at
src/nds/nds_renderer.c:1853 UseVertexColor yields to UseMaterialColor whenever
the combiner references PRIM/ENV - lit SHADE is discarded; and in the submit
path (~:3540) a FAILED BindTexture silently falls back to NoTexture + material
color. Bind-fail + white PRIM/ENV material = flat white untextured polys.

1. Lit-SHADE/material precedence (reference patch A): first log the distinct
   combine-mode words the canonical scene actually uses (one run, marker or
   log), then fix precedence from that data: combiners that reference SHADE
   under G_LIGHTING must get the lit shade; material color remains the path for
   combiners that do not request SHADE. Also fix the LIGHT_1/LIGHT_2 handling:
   stop brightness-sorting; assign ambient vs diffuse from the decoded
   G_MOVEMEM/G_MW_LIGHTCOL slot offsets (GBI: diffuse lights first, ambient
   last) - derive from the decoded stream, cite the GBI semantics.
2. Texture bind failures per format: add markers for textures converted /
   bound / palette-bound / bind-FAILED (with failure reason) per format
   (RGBA16, CI4, CI8, IA4/8/16, I4/8). Prime suspect: CI4/CI8 palette upload/
   binding (VRAM bank E slots, per-texture palette base, pal4/pal16/pal256).
   Fix so every format class used by the scene has nonzero successful binds and
   zero unexplained failures. Capture: dirt block region stops being white-
   dominant; grass shows texel detail instead of flat green.
3. Missing draw classes - diagnose before fixing:
   a. Report gNdsRendererProfileHardwareOverLimit + per-frame submitted
      poly/vertex counts in the markers: DS hardware silently drops geometry
      past 2048 polys/6144 verts, and fighters likely submit after the stage.
      Rule truncation in or out first - it is the cheapest explanation.
   b. On a SCRATCH branch, apply the projected-submit probe (reference patch
      B): force all triangles through the CPU-projected screen-vertex path.
      If fighters/platforms/background appear, the root is the raw DS
      matrix/z-buffered path; if still missing, their display lists never
      reach the HW submitter (then add per-class submission counters split by
      GObj kind and trace where each class diverges - preview-only routing or
      fast-logic fencing is the likely shape). Record findings, REVERT the
      probe, land only the real fix on master.
4. Fighter pixel gate: camera and spawns are deterministic - compute expected
   on-screen regions for Mario and Fox from the oracle transforms and assert
   those regions contain pixels differing from the stage behind them. Counters
   alone have passed while fighters were invisible; pixels are the gate.
5. Evolve the screenshot gate (one-way ratchet): from "green exists" to green
   floor fraction PLUS a non-white/non-green stage/fighter pixel fraction PLUS
   the fighter-region assertions, so the gate fails while platforms/fighters/
   background are missing or surfaces are blown-out white.
6. Done = capture showing textured Dream Land with both fighters visible;
   rebuild shipped ROM (canonical config, NDS_DEBUG_HUD=0), save captures under
   artifacts/visibility/, STOP for Tyler's playtest. Cache/perf (3.9fps -> 60)
   stays the NEXT slice - do not start it here.
7. Verification: canonical realtime verifier + verify-dev-fast + verify-boundary
   + RegressionCore; check-gbi-decode-fixtures; check-docs;
   check-harness-registry; PORTING entry; commit; snapshot -Mode Lean. No full
   sweep in-session - Tyler launches scripts/start-overnight-regression.ps1 at
   end of day.

Constraints: decomp/ read-only; pixel/oracle/green/delta gates are one-way
ratchets; renderer behavior derives from game-submitted data with citations,
not invented constants; per-hypothesis timebox ~10 emulator runs / ~1h then
checkpoint; one writer for the tree.
