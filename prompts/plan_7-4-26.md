/task Adopt the runtime-first slice policy, clear the regression debt, then land the
battle_playable subsystem slice: camera, battle flow, KO/respawn, HUD, and a
continuous unbounded battle gate.

0. Gate debt first: 78ffa1af (+~1,100 lines shared fighter runtime) and the hwtri
   cutover series shipped without a full sharded Regression. Run
   New-MelonDSRunnerSlots.ps1 -Count 4; build-verify-profile.ps1 -Profile Regression
   -Force as its own command; then all four verify-all.ps1 -Profile Regression
   -ShardCount 4 -ShardIndex N -RunnerSlot N -NoBuild runs. Fix or cite any fallout
   ([source-corrected]/[coverage-reduced]) and commit as a checkpoint before new work.

1. Policy amendment (AGENTS.md, mirrored in STATUS/HANDOFF process sections):
   runtime-first subsystem slices —
   - minimum slice is a subsystem group aimed at a scene-level capability;
   - legacy bounded modes are migrate-or-delete: when a slice obsoletes an old
     marker stack, delete the mode + verifier with a one-line KNOWN_ISSUES ledger
     entry (still tagged [coverage-reduced]) instead of reproducing old markers;
   - new harness modes only at scene-level capabilities (battle_playable is one);
   - the primary gameplay gate is the continuous natural-runtime verifier plus
     captures, not per-branch markers.
   Unchanged: decomp/ read-only, citations rule, Boundary pair, sharded Regression
   on the pruned mode set, snapshot policy, no hand-authored gameplay.

2. battle_playable subsystem slice (whole TUs, fence-then-graduate like ftmanager):
   a. gm/gmcamera.c whole — battle camera follows both fighters; delete the DS
      camera seam feeding the renderer once green;
   b. finish gm/gmcommon.c graduation — audit which entry points still route to
      port seams and route/delete them exactly like the ftCommon* seam routing
      (watch for the same strong-seam-shadowing-weak-alias pattern);
   c. KO/respawn: import the TUs behind the currently-stubbed Dead/Rebirth status
      callbacks plus the gmcommon stock/respawn rules; blast-zone KO must come from
      source MP/gmcollision paths;
   d. if/ifcommon.c HUD (percent/stock) — IFCommonPlayer* payloads are already in
      the manifest; draw through the existing DL path;
   e. new scene-level mode battle_playable: continuous unbounded VSBattle Mario vs
      Fox on Pupupu — no bounded update cap, no state restore, no GObj pausing;
      scripted input runs the existing natural combat chain then keeps fighting
      (repeat approach+attack) until a natural KO -> respawn -> Wait completes.
      Verifier asserts: thousands of frames without crash/assert, statuses always
      valid, positions finite, at least one KO->respawn cycle, HUD percent matching
      fp->percent_damage after hits, plus a -HardwareTriangles capture.

3. Deletions along the way, each its own commit: cliffmotion preserve/restore hook
   in battleship_ftmain.c once the cliff family runs naturally in this scene; the
   legacy dash-run attack-word decoder and modes 159/160, 57/58 plus other
   superseded synthetic families under the migrate-or-delete rule.

4. Rails: BattleShip file:line citation for every expectation change; checkpoint
   commit per green stage; fresh Regression prebuild + 4 sharded runs again at the
   end (the mode set may shrink — that is expected); docs current-truth;
   New-Smash64DSSnapshot.ps1 -Mode Lean last (on tool timeout, check
   Desktop\Snapshots for the zip before rerunning).

Constraints: decomp/ read-only; stubs only for genuinely unimported dependencies
(items, audio), documented; time-box per-issue debugging (~10 emulator runs or ~1h)
then checkpoint to WIP and report. If KO/respawn drags in too large a dependency web
for one run, land camera + gmcommon + HUD + the continuous-battle mode first, leave
the KO assertion fenced with a KNOWN_ISSUES entry, and report the blocker precisely.