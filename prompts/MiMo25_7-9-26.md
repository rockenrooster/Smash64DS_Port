The project has made significant progress:

Runtime slices 1-2 landed: ftmain.c, gmcollision.c, ftmanager.c, status descriptors, ftanim.c/ftkey.c all imported
Mode 163 (battle_playable): Proves stock/KO, natural combat, specials, HUD, audio, HW stage/fighter
Hardware renderer: Working but fidelity issues remain - "white/misplaced Dream Land surfaces and broken fighter assembly"
Latest commits: Canonical HW tile state and input bridge (last 3 commits)
Why Nothing Visual Changed
The visual output is stuck because:

Raw DS matrix/depth still needs repair - canonical HW uses CPU-oracle projected-submit fallback
Fighter assembly is broken - hwftr=2/582 shows only 2 of 582 fighter pixels are correct
Dream Land surfaces are white/misplaced - per-tile/TMEM sampling not complete
Texture cache not at 60fps - still below target framerate
Next Task for Codex
/task Fix renderer fidelity: fighter assembly, per-tile/TMEM sampling, and raw DS matrix/depth.

1. Diagnose the fighter assembly failure: inspect why hwftr=2/582 (only 2 fighter-region
   pixels correct out of 582 expected). Check nds_renderer.c fighter-part MObj attachment,
   DObj parent chain traversal, and the segment 0xE material state preservation. Compare
   against decomp/sm64-nds fighter rendering approach.

2. Fix Dream Land white surfaces: investigate per-tile state/input work mentioned in STATUS.md.
   The tile state tracking is recording conv0x100/bind0x100/pal0x100 but surfaces render white.
   Check TMEM→DS texture format conversion, palette application, and combine state.

3. Repair raw DS matrix/depth: replace the CPU-oracle projected-submit fallback with
   source-correct raw matrix submission. Inspect the N64 packed-matrix → DS 20.12 unpack
   pipeline for correctness issues. The projection/modelview compose should match the
   software preview path exactly.

4. Scope to Pupupu battle scene (mode 163). Compare melonDS screenshots against
   software-preview references. Fix one issue at a time: fighter assembly first,
   then tile sampling, then matrix/depth.

5. Gates: verify-dev-fast + verify-boundary green; melonDS screenshots showing
   improved fighter rendering and correct Dream Land surfaces; STATUS.md/HANDOFF.md
   Latest Proof update; snapshot last.