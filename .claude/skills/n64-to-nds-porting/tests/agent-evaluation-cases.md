# Manual agent cases — not executed tests

| Case | Required result |
|---|---|
| CI4 transparent entry 15, opaque index 0 | Effective TLUT/alpha; remap only when the equation uses the mask; preserve opaque black. |
| Same image, alpha-using and alpha-ignoring materials | Different semantic cache identities in runtime and offline corpus; both render correctly. |
| Palette load lives above the extracted material branch | Preserve inherited root state; independent draw-state fixture catches its loss. |
| Generator/checker agree on the wrong weapon root | Trace source constructor and consumer; replace the shared assumption, not merely its pin. |
| Compact source arrays omit a trailing zero/adjacent handle | Preserve source consumer extent and termination; test traversal independently. |
| Fixed owner slot exceeds a sparse profile's enum count | Separate semantic domain from enabled count; test highest/sparse configurations. |
| Mixed-matrix triangle or overwritten matrix scratch | Preserve immutable load histories/values, not the current matrix/pointer. |
| Zero rejects; source expects no aerial weapon | Match the constructor/state; do not manufacture a weapon to satisfy a false positive-control assumption. |
| Native-only project requests diagnostic fallback | Host oracle only; no source interpreter in any target ROM. |

Grade implemented behavior and evidence, not checklist vocabulary. These prompts are not automated game, model or renderer tests.
