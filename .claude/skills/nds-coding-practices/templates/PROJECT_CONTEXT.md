# Optional backend scratchpad

Fill only missing technical facts inside an existing task/note.

- Installed SDK/runtime, target flags and hardware mode:
- Owner/API, input units, bounds and error result:
- Buffer/address space, cache/DMA visibility, lifetime/release:
- Native graphics format, palette, material and layer state:

Budgets, build commands, scheduling and acceptance stay in the documents indexed by `docs/README.md`.
