Goal: Add multiplayer to Smash64DS (multi card only)

VS Mode will have a host option (VS Mode > VS Start > option for hosting). Default off
VS Mode will have a "Join Battle" option (VS Mode > VS Join > Searching for host... > join lobby)

Host console sets all match settings

It should allow for a "1v1" player vs player or any combination of players vs CPUs up to 4 total in team battle or free-for-all, just like the original game but across DS consoles since a DS only has "1 controller" input.
Minimum 2 human players, otherwise the default non-host path should be used since CPUs don't need physical controller inputs.