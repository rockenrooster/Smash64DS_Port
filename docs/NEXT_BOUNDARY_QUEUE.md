# Legacy Boundary Queue (Historical Redirect)

This file is no longer a queue and must not receive new work items. Its former
mode-161/162 proof-mask sequence is preserved in append-only `PORTING.md` and
the detailed marker contracts remain in `DIAGNOSTIC_REFERENCE.md`.

Canonical `battle_playable_realtime`, mode `163`, is the active natural-runtime
Boundary. Modes `161/162` are diagnostic-only legacy anchors.

Use `P1_EXECUTION_BOARD.md` as the only dynamic P1 queue and
`scripts/lib/harness-registry.ps1` as the authority for profile membership.
