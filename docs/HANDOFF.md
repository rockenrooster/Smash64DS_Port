# Handoff
Current: 2026-08-16 - **THE FIXED CAMERA ARM IS NOT SLOWER IN PRESENTED CADENCE - THE TOGGLE IS, IN EITHER DIRECTION - AND THE SIM-SIDE SOFT-FLOAT LANE IS 142,786 tk/fr, THREE QUARTERS MULTIPLY-ACCUMULATE, WITH 71,491 OF IT WARM MAC IN FOURTEEN FUNCTIONS** (`artifacts/performance/2026-08-16_camera-cadence/CADENCE.md` + `.../2026-08-16_simside-softfloat/SIMSIDE.md`; **0 lab builds, 9 emulator runs on `builds/build-c205-camtoggle` - the ROM the owner actually played - plus 1 Boundary**, no default flipped, no ROM published, nothing banked, root ROMs byte-unchanged `54c07fac…` / `6c939434…`). **ITEM A - THE OWNER'S REPORT IS REAL AND THE ARM IS INNOCENT.** Arm selected at frame 1, two gdb stops, 2,043 presented frames, both arms reproducing **bit-identically** on a repeat run: FLOAT `iv2 1,953 / iv3 72 / iv4 5 / iv5+ 13 / max 26 / 4,277 VBlanks`, FIXED `1,956 / 69 / 5 / 13 / 26 / 4,273`; P50 2, P95 2, P99 3, cadence violations 0 on both. **The fixed arm is 3 frames BETTER and 4 VBlanks shorter**, and a second, independent per-frame instrument reproduces the same margin (151 vs 147 slip-VBlanks over 1,900 frames) - so quantisation did NOT invert the -4,736 tk/fr cut and the camera lane is not disqualified on cadence. **What the owner saw attaches to the TOGGLE and is symmetric in arm**: boot FLOAT then flip to FIXED slips 355/800 frames; boot FIXED then flip to FLOAT slips 395/600, block-for-block the same penalty. **Two negative controls that could have failed**: a NULL flip (0->1 at frame 400, 1->0 at 401 - two debugger writes, net arm unchanged) reads 113 slip-VBlanks/1,000 frames against the no-poke control's 114, and per-frame gdb stops cost nothing either. **Transient: flipping BACK to the boot arm recovers in 6 frames (20 consecutive on-cadence frames complete 21 frames later); flipping AWAY never recovers - still elevated 600 frames on, to the end of the match.** So "wait N frames then trust your eye" has no N: **the toggle ROM answers the PICTURE question and cannot answer the FPS question**, and a per-arm cadence comparison needs each arm as the BOOT arm (two ROMs, or `-RouteInitial` on the new probe). Mechanism unidentified and stated as a bound: the particle camera cache is measured **inert** across the flip (2 hits + 1 miss per frame on both sides), and the debugger write is excluded by the null flip. **The pixel decision is untouched and remains `BLOCKED(decision: draw-side precision)`.** **ITEM C - `POSITION.md`'s 0.563x fidelity-neutral inventory IS incomplete, materially.** Same capture, same tool, zero builds: sim-only 45,539 + sim+dispatch 37,665 + shared 59,582 = **142,786 tk/fr at rank-80 = 1.511x of +94,481**, of which **MAC (fmul+fadd+fsub) is 75.9%** and transcendental (fdiv 9.7% + sqrtf 5.7%) only 15.4% - **the exact opposite shape to the camera chain**, whose 3 roots + 9 divides per look-at is the named reason it converted at 1.70x and not the 5.14x MAC prior. By shape: **MAC>=80% 82,798 tk/fr (64 fns), of which 71,491 is WARM (>=8 entries/frame) in 14 functions** - collision/MP 50,044, animation 13,904, math leaves 7,541; mixed 30,199; transcendental>=30% 29,789. The two largest are 100% MAC and warm: `func_ovl2_800ED490` **18,759 tk/fr** (19.0 entr/fr, 63 ops/entry, 580 B) and `gmCollisionGetWorldPosition` **13,091** (45.2 entr/fr, 18 ops/entry, 196 B), and both reproduce their source op counts **to the unit**. **NO RATE IS ASSERTED** - the three measured in-binary rates span 3x (1.70x camera / 2.68x narrow phase / 5.14x same-operation MAC pair) and **both named causes of the two low ones are absent here**, which is a mechanism argument and not a measurement: **29,437-57,584 tk/fr = 0.312x-0.609x**. **Three columns, because the fidelity question is not uniform**: **N** bit-exact helper acceleration (a faster `__aeabi_fdiv`/`sqrtf` returning the identical IEEE result is fidelity-neutral BY CONSTRUCTION; surface 21,886 sim + 7,900 draw; **UNSIZED**, and the DS divide/sqrt units are still unused - `NDS_R2_CFX_DIV64`/`ISQRT64` undefined); **P** provable-equivalence fixed point for the warm MAC subset, which belongs in the fidelity-neutral column **iff** the decision-margin proof lands - and the instrument already exists in-tree (`NDS_R2_CFX_NARROW_DECLINE` makes equivalence structural rather than statistical, `grade-r2-collision-live-domain.c` + `probe-collision-fixed-domain.ps1` grade the shipping kernels against a double-precision reference on live match matrices to 0.0200 world units, `NDS_R2_COLLISION_L7_ORACLE`, and the seventeen match invariants); **T** rung-3 trades for the remainder. **So 53,215 (0.563x) becomes 82,652-110,799 (0.875x-1.173x) if column P moves** - which needs a proof, not an owner decision. `syUtilsRandFloat` is 229 tk/fr (0.16% of the lane) and is excluded by name; the integer frame counters are not in this lane at all. **Concentration warning: call volume on the marginal-80 frames is 4.96x whole match, so a whole-match or soak measurement of any conversion here under-reads it ~5x.** **NO SIM-SIDE CONVERSION WAS IMPLEMENTED OR MEASURED** - sizing and feasibility only, as briefed. **ITEM B - THE FALSE GREEN IS FIXED AT THE DRIVER, THREE HOLES, ALL PROVEN.** (1) The toolchain guard tested **presence, not usability**, and lived inside `if ($Build -and $needsNormalBuild)` - so on Boundary, whose plan has no `smash64ds` target, **it never ran at all**; `Assert-Smash64DSToolchainUsable` now runs unconditionally before any verifier, normalizes `DEVKITPRO`/`DEVKITARM` into the environment every child inherits, requires `ds_rules` and `arm-none-eabi-gcc.exe`, and **runs one recursive make**. (2) **The `Error 127` is `$(MAKE)`, and only a recursive make can see it**: on this host `$(MAKE)` measures as `/opt/devkitpro/msys2/usr/bin/make` - devkitPro's msys2 reports its own argv[0] in the MSYS namespace - so `Makefile:3312`'s `@$(MAKE) -C $(BUILD)` resolves only when the recipe shell is that same msys2. No `DEVKITPRO` spelling fixes it and the Makefile's own normalization is fine. (3) **`exit $null` exits 0** (`$null -eq 0` is `$false`, so a null child exit code fell through the failure branch and exited 0 anyway); `Invoke-VerifyScriptOnce` substitutes 70 and `Get-Smash64DSFailureExitCode` refuses to exit 0 from a failure branch. The pass line is now gated on one success per planned verifier and an empty plan throws. **Proved both ways**: a deliberately broken `DEVKITARM` fails at the driver in ~1 s with the cause named (exit 1, was a deep grandchild `Error 127`), the recursion probe's own negative control returns `NDS_RECURSIVE_MAKE=FAIL:127` under `MAKE=/nonexistent/make`, and a normal Boundary prints `Toolchain usable: recursive make OK, DEVKITPRO=C:/devkitPro DEVKITARM=C:/devkitPro/devkitARM` as its first line - having normalized the ambient `C:\devkitPro`. **NEW TOOLING**: `scripts/probe-present-cadence.ps1` (the 2/3/4/5+ histogram `AGENTS.md` requires, on any arm of any proof ROM; `-FlipAt` is a STRING because `pwsh -File` mis-binds an `int[]` through the mandated detached `cmd /c` launch, `-Hits` must be under the match's 2,043 presented frames or the run sits until timeout, and the summary prints from inside the breakpoint block so a killed run keeps it) and `--matrix-json` on `analyze-leaf-helper-attribution.py` (`--json` collapsed the **helper axis, which is the axis that decides whether a lane converts**). **ACTION (unowned, one line):** `gNdsBattlePlayablePacingPresentIntervalBucket` is NOT a member of `NDS_BATTLE_PLAYABLE_PACING_GROUP` (`nds_startup.h:4184`) - it is readable over gdb only because `DC_FlushRange` cleans whole 32-byte lines and two group members happen to share the array's two lines, so a `diagnostics.c` relayout silently breaks the one counter set every device A/B report must carry. **NEXT CYCLE INHERITS:** the warm MAC subset's rate is the single unmeasured number that decides whether the engineering ladder reaches the gate, and its first measurement **cannot use the campaign's zero-placement-floor same-binary route** - `func_ovl2_800ED490` and `gmCollisionGetWorldPosition` have fifteen in-TU call sites in `gmcollision.c`, so the `#define`-before-`#include` rename moves definition and callers together and a route A/B is not expressible; budget the >=14,080 rank-80 cross-build floor instead, and design the equivalence instrument before the first build.
Superseded 2026-08-16 - **THE FIDELITY-NEUTRAL INVENTORY IS 53,215 tk/fr AGAINST A +94,481 GAP AND IT DOES NOT CLOSE; THE ONLY RUNG THAT DOES IS THE 60 Hz SIMULATION, AT 3.085x, RE-DERIVED ON THIS TREE AT 2.4x THE RECORDED FIGURE; AND `GX_COMPOSE`'s FTR->STG TRANSFER IS REFUTED - THE STAGE'S WORK IS BIT-IDENTICAL** (`artifacts/performance/2026-08-16_gap-position/POSITION.md` + `.../2026-08-16_gx-transfer-locate/`; **2 lab builds, 2 gate runs, 1 Boundary**, 0 source changes, no default flipped, no ROM published, nothing banked). **THE DENOMINATOR IS `+94,481`** - `build-c206-shipgx0`, rank-80 1,239,808 raw / 1,214,861 net against the 1,120,380 gate, `NDS_R2_FIGHTER_GX_COMPOSE 0` (the renderer the ROM ships), bore 0, mode 163 one-minute, 1,600 samples, frames 439-2038, `slips=0`. **ITEM A DONE: `LADDER.md`, `MENU.md`, `DRAW_FIXEDPOINT.md` and `CAMERA_Q20_12.md` are re-divided IN PLACE and every table that carries a ratio now states `+94,481` beside it**, so the denominator cannot be separated from the number a third time. Conversions recorded at each site: x0.904 from +85,393, x0.860 from +81,297, x0.345 from +32,593, x0.304 from +28,689. Two rows moved for reasons other than the denominator - the animation-representation mechanism (33,951) is **struck as closed at ~1% conversion (-319 tk/fr)**, and `LADDER.md` section 4's `0.357x -> 0.659x` amendment is **WITHDRAWN on both halves**: the 5.14x rate was refuted at 1.70x, and the lane was never engineering-available because it is `BLOCKED(decision: draw-side precision)`. **ITEM C - THE HEADLINE, AND IT IS THE FINDING RATHER THAN A FAILURE: everything that costs no fidelity at all sums to 53,215 tk/fr = 0.563x, at 100% conversion of every item, and 100% is available for NONE of them** - `SyncTextureTile` (8,867) and the texture-bind collapse (13,868) each need one counter before they are items at all, and slice 2's `gcRunAll` machinery (17,786) is measured unable to reach 100% because a flat vector still calls each process. Deleting the WHOLE inventory outright leaves **+41,266** (verified by re-rank, not by subtraction). The subset available today with no owner decision, no pending measurement and no new counter is **30,480 = 0.323x**, four items none above 0.19x. **Closed lanes contribute ZERO, not a little**: animation representation -319 tk/fr, the toolchain bump a measured no-op three ways, the ITCM golden reclaim 56 B of space and no ticks, narrow-phase fixed-point collision closed at 2.68, `__aeabi_fadd` flat at 4.63% top PC over 81 PCs (no instruction to delete; the only lever is call count), and `GX_COMPOSE` nothing. **THE RUNGS, PRICED IN `PROJECT_GOAL.md`'s OWN ORDER against +94,481: (1) audio fidelity <=3,040 = 0.032x** - `AUD` is one `ndsAudioBackendUpdate()` per presented frame (`taskman_seam.c:4487-4495`), P50 2,880 / top-80 median 3,040, and it is small for a structural reason: **the gate is an ARM9 metric and DS mixing is the ARM7's job**, so the first rung to sacrifice is also the one with least to give (stated as measured-today: full milestone audio is not all present, and BGM streaming I/O may sit in the asset-I/O lane). **(2) visual fidelity 44,476-50,203 = 0.471x-0.531x** - stage no-Z band 22,608 (0.239x, flat at exactly 1.00x so it converts 1:1; `CARD_CULL` sub-rung ~4,600 = 0.049x), particle draw kernels 12,595 (0.133x - the floor and the honest number; -30,676 stays retracted), draw-side fixed point 9,273-15,000 (0.098x-0.159x). **Both visible rungs together no longer close**: what the owner is asked for has changed from "accept one visible change and we are done" to "accept every visible change and be ~50% of the way". **(3) gameplay fidelity 55,473-84,451 = 0.587x-0.894x, A PROJECTION not a measurement** - the frozen 80.2% of the soft-float class is 134,720 tk/fr (shared 57,521 + sim-only 39,537 + sim+dispatch 37,662, classified from the linked ELF not from names) projected at the only two exchange rates measured in this binary, 1.70x and 2.68x, both taken on other symbol sets. Neither end closes. **(4) THE ORIGINAL 60 Hz SIMULATION: 291,488 = 3.085x, AND IT IS THE ONLY THING ON THIS BOARD THAT CLOSES ALONE.** Re-derived on this tree rather than rescaled: `taskman_seam.c:4273-4275` states `SRC` is **exactly** the two logical updates a presented frame runs (`ndsRunMarioFoxProofUpdate` is its only writer and brackets `scVSBattleFuncUpdate`), so halving `SRC` across `build-c206-shipgx0`'s own 1,600 rows and re-ranking gives rank-80 1,239,808 -> 948,320. **That is 2.4x the recorded Task 106 figure of 119,744**, and the reason is nameable: **`SRC` concentrates 2.09x on the gate population** (P50 332,928, top-80 median 695,872), so halving it removes more from the tail than from the middle. **Calibration proves the re-rank exact**: a flat cut of 94,481 lands the gap at +0 and a flat cut of 22,608 moves rank-80 by exactly 22,608. **Only 16.4% of `SRC` is needed to close**, so partial-cadence variants close too - `SINT` half alone 132,064 = 1.398x, and `GCRA` half with **`SCPU` and `SINT` HELD AT 60 Hz** 121,248 = 1.283x, while `SCPU` (the level-3 AI) half alone is 27,488 = 0.291x and does not. **So the question is not "60 Hz or 30 Hz" but "which subset moves".** What must NOT halve, each with its reason: **input sampling** (latency 16.7 -> 33.3 ms, single-frame windows stop being expressible), **hitbox/hurtbox resolution** (SSB64 hitboxes live as few as 2 frames and travel fast - needs swept or sub-stepped collision, which ADDS cost back in the concentrated half of the class), **the RNG sequence** (`syUtilsRandFloat` is ONE LCG shared across 135 draw sites - 65 in the level-3 AI, 44 `efmanager.c`, 26 `lbparticle.c`; halving the update halves the draw count and desynchronises the AI - **this is the known-fatal one**), and **every integer frame counter** (hitstun, shieldstun, stale-move queue, invulnerability, ledge cooldown, respawn, match timer - at 30 Hz each advances by 2 and every odd-length window rounds). `plan.md` section 3 item 8 records that **blanket** 30 Hz poses regressed and diverged, so a proposal must be per-system. **CAVEATS STATED NOT DISCOVERED**: "half of `SRC` per frame" is an approximation (the two updates are not equal cost and an event lands in one of them) whose error is largest on exactly the tail frames - the rung survives it because it closes with 3x margin; no compensation cost is included, so 291,488 is a CEILING; and `NDS_TASK106_UPDATES_PER_PRESENT=1` exists but the same source site says a build with 1 is not a candidate because it is uncompensated. **ITEM B - THE `GX_COMPOSE` TRANSFER IS REFUTED, AND SO IS THE ACCOUNTING-ARTEFACT READING.** Two builds one config line apart (`diff` of the generated headers is exactly `GX_COMPOSE 0 -> 1`; `nm` finds 0 vs 8 `gNdsR2GxCompose*` symbols - a negative control that cannot be faked), both `NDS_TASK103_STAGE_RUN_PHASE=1`, which partitions the whole `STG` bucket at its four accumulation sites and is COMPLETE: the four sites sum to 223,978 tk/fr against the bucket's own mean 223,084, 0.4% apart. **Every stage WORK counter is bit-identical to the unit over 2,038 frames**: `WordCount` 7,972,976 (3,912.2 GX command words/frame), `RunCount` 67,188, `CommitCount` 16,296 (8.00/frame), `DisplayCount` 55,890, `PrepareCount` 2,038, `FinishCount` 2,037, `TraversalCount` 8 - and `PushTicks`, the span that writes those words, differs **+9.7 tk/fr = 0.09%**. **And the STG rise INVERTS under the instrument**: paired per-frame medians read `c206->c207` FTR -8,192 / STG +6,656 / MISC +2,368 / draw side +832, against `c208->c209` FTR -8,576 / STG **-1,632** / MISC -1,088 / draw side -11,392. **The fighter saving reproduces on both pairs (1,599 and 1,597 of 1,600 frames); the stage rise does not.** **Not a quantisation artefact either** - `[[all-is-a-quantized-gate]]`'s failure mode needs a floored row, and across 3,200 sampled frames there is **not one zero row in any bucket** (`STG` minimum 171,776 / 178,048, granularity 64 ticks = 0.036% of the bucket), and both spans are plain `cpuGetTiming()` deltas at named seams. What remains is cross-build placement in that one pair or a GX queue effect the instrument's own ~43,000 tk/fr of added stage work absorbs - **bounded, not mechanised, and stated as such**; the pair's own placement scale is visible as **`SRC` +3,456**, a bucket the flag cannot touch. **CONSEQUENCE: `BLOCKED(decision: GX_COMPOSE default)` NO LONGER BLOCKS ANYTHING** - it has no performance content (rank-80 -7,040 and -8,256 on two pairs, both inside the >=14,080 floor; P50 disagrees in SIGN between the pairs) and its basis-consistency half is discharged by Item A. **BLOCKED(decision: sacrifice order) IS THE LIVE ONE and it is the owner's call under rail 5; nothing was chosen here.** The other live decision, **draw-side precision, IS rung 2 and not a separate question** - yes admits 9,273-15,000 of which the camera chain is already built at -4,736 = 0.050x and makes particle/quad math (9,015 gross, multiply-accumulate shaped) the better next candidate; no closes the whole 34,178 and drops rung 2 to 35,203 = 0.373x. The number the owner should see: the camera arm's pixel delta is **6.5350% / 3.6325%** against a **same-build adjacent-present floor of 35.2217% / 37.7983%** on the same crop - i.e. smaller than the difference between two consecutive frames of the unmodified build; playable both-arms ROM `builds/build-c205-camtoggle/smash64ds-battle-playable-proof-hwtri.nds`, SELECT (Right Shift) flips mid-match. **INSTRUMENT TRAP, RECORDED**: `gNdsTask103BeginMtxTicks`/`BeginMtxCount` are declared `[4]` (`nds_renderer.c:29634-29635`) and read as scalars through `-ExtraGlobals` they returned **their own addresses** (35,173,948 = `0x0218b63c`, exactly the `nm` value) with no error - both reads discarded. **DOC DEFECT FOUND, NOT FIXED (no source change was in scope)**: `taskman_seam.c:4277` calls the 60 Hz rung "third in the sacrifice order, above gameplay fidelity and frame rate"; `PROJECT_GOAL.md` puts it **fourth**, below gameplay fidelity and above stable 30 FPS - both clauses wrong. **Invariants identical on all four arms** (P1Damage 76, spark 16, packHits 257, SegmentMask 161), `slips=0` on both new runs, 0 `Exception:`. Root ROMs `54c07fac...` / `6c939434...` **byte-unchanged** (`root-roms-before/after.txt`); `6c939434...` is still the bore-84 link and changes at the next publish. Boundary **GREEN** - "Boundary verification profile passed.", 0 `Exception:` (`builds/boundary-c209b.log`). **HARNESS TRAP THAT COST A RUN AND MUST NOT COST ANOTHER: a Boundary run whose sub-build FAILED exited 0.** Invoked from a shell where `DEVKITPRO`/`DEVKITARM` are set with Windows backslashes, `verify-all.ps1:157`s `if (-not $env:DEVKITPRO)` guard does NOT fire (the variable is present, just unusable), make resolves `/opt/devkitpro/msys2/usr/bin/make`, the proof-ROM build dies `Error 127` - **and verify-all still returns exit 0**. Read the log tail, never the exit code; run it through the PowerShell tool with both variables set forward-slash.
Updated: 2026-08-16 - **THE +85,393 BANK WAS MEASURED WITH `GX_COMPOSE` ON WHILE THE ROM SHIPS IT OFF; RE-MEASURED AT THE SHIPPING DEFAULT THE GAP IS +94,481, AND THE -17,152 IS RETIRED** (`artifacts/performance/2026-08-16_gxcompose-bank-basis/BASIS.md`; **2 lab builds, 2 gate runs, 1 Boundary**, no default flipped, no ROM published, 0 source changes). **BASIS, proven from the build and not inferred:** `builds/build-c199-bank0/nds_build_config.h` reads `NDS_R2_FIGHTER_GX_COMPOSE 1` and `nm` on its ELF finds **8 `gNdsR2GxCompose*` symbols** that exist only inside that `#if` (0 on every GX=0 ELF), while `Makefile:766` is `?= 0` and the published block `Makefile:1545` pins **0 unconditionally**. **Every bank since `c185` is GX=1** (c185, c193, c197, c199, c200, c201-c203); `c170` and everything before it was GX=0. The tick-HUD block's own comment says the instrument "has to stay flag-identical to the published block" - on this flag it has not been since c185. **LEVEL:** fresh at HEAD `b1339828070`, `build-c206-shipgx0` (GX=0, the shipping renderer) rank-80 **1,239,808 raw / 1,214,861 net = GAP +94,481**; its one-config-line sibling `build-c207-gx1` reads **1,232,768 / 1,207,821 (+87,441)** and lands **2,048** from `c199-bank0`, so the bank replicates. All seventeen invariants identical across all three arms; `slips=0`; GX engagement on B complete (`Declines=0`, `Captures=Roots=62,920`, `Locals=Mults=109,346`). **The 9,088 between the two configurations is INSIDE the >=14,080 cross-build floor, so the bank is not PROVABLY optimistic - but it is measured on a renderer the user does not run.** Cheapest repair needs no decision: **quote +94,481**; every "x of the gap" ratio multiplies by **0.904**, and no conclusion anywhere changes sign. **PRICE: the -17,152 is NOT reproduced.** Same-HEAD pair one config line apart: rank-80 **+7,040** (GX on cheaper, floor >=14,080), P50 **-4,288** (GX on DEARER, floor ~5,700), paired median **-4,544** with GX-on worse on 1,370/1,600 - **inside both floors and disagreeing in sign**. The -17,152 was `build-c184-gxc-a/-b` at a 1,189,312 level, i.e. **before the segment repair `64c41c361a7`**, the same category as `MENU.md`'s 94,602. **What IS reproducible is a TRANSFER, not a saving:** FTR P50 305,152(off)->296,704(on) and STG 175,680(off)->182,272(on), and the second independent GX=1 build (`c199-bank0`, different HEAD) lands **within 704 on STG**, so it is a property of the flag, not placement. Whether the stage truly pays it or the ticks merely cross a tick-HUD bucket boundary is **one counter's work and was not taken**. **BLOCKED(decision: `GX_COMPOSE` default)** is now a basis-consistency question, not a performance one - package in `BASIS.md` section 5 with the four pixel locks (0.0358-0.1742%, max channel delta 251), the images under `artifacts/visibility/2026-08-15_gxcompose-bank/`, and three priced options. **RECORD CORRECTED: `0.0692%` WAS NEVER A THRESHOLD.** `PREDICTION.md` section 5 pre-registered *identity* ("any pixel difference") as the stop, so 0.0358-0.1742% is a value measured against a **zero criterion**, and the owner then **accepted** it (`GXCOMPOSE.md` section 9). `CAMERA_Q20_12.md` lines 53 and 425 said the camera's 6.5350% had to clear it as a bar; both sites are fixed and the doc now states plainly that **this repo has no draw-side pixel budget at all** and that the camera decision **sets the first ceiling** rather than exceeding one - 6.5350% is ~37x the largest delta ever accepted (0.1742%). **ITCM golden reclaim CLOSED at 632 B and mostly untakeable:** the literal-pool modality has **zero power** (0 words for `__aeabi_fadd`/`__mulsf3`/`__divsf3`, all of which are entered 1,728/2,122/431 times), so only branch reachability decides; 456 B (`__aeabi_frsub`/`__subsf3`/`__addsf3`) + 120 B (five fcmp goldens) + 56 B (`__unordsf2`) have **0 external entries**, confirmed independently by **0 sampled instructions in a whole-match v3 capture**. But the live bodies are **repo-authored** (`nds_task16_float_addsub.o`, `nds_task16_float_compare.o`), so `NDS_TASK16_FLOAT_*=0` **reverts Task 16 to stock libgcc and is not a reclaim**; and `--rename-section .text=.itcm` on whole members traps 576 B beside live symbols. **Only 56 B is cleanly droppable**, so 616 B free (shipping) + 56 = 672 against the camera v3's 916 B overflow - **the ITCM route stays closed**. **`__aeabi_fadd` ladder is FLAT:** top PC **4.63%** over 81 PCs, **96.22% issue stall**, 18.33 tk/call, **1,805.7 calls/frame** whole match and 3,903.9 on the marginal-80 - no instruction to delete, the only lever is the call count. Root ROMs `54c07fac...` / `6c939434...` byte-unchanged; Boundary **passed**, 0 `Exception:`, `gxstat=0x6000000`.
Superseded 2026-08-16 - **THE TOOLCHAIN BUMP CANNOT MOVE THE SOFT-FLOAT BILL, AND THE PROOF COST 0 BUILDS AND 0 EMULATOR RUNS; SEPARATELY, THE TWO ftanim CHECKERS ARE FIXED AND WIRED AND THEY RE-FAIL THE COMMIT THAT SHIPPED DEFECT 1** (commit `36f908dcbcc`; 0 lab builds, 0 gate runs, 1 Boundary, no default flipped, nothing banked). **TASK 5 (devkitARM bump) IS ANSWERED AND IT IS A NO-OP, three independent measured ways.** (1) **There is nothing to bump to**: `gh api repos/devkitPro/buildscripts/releases` says **devkitARM r67 (2026-01-12) is the newest release** - there is no r68 - and installed is **`devkitARM-r67.1-1`** with `devkitarm-gcc-15.2.0-7`, `devkitarm-binutils-2.45.1-2`, `devkitarm-newlib-4.6.0.20260123-5`, `libnds-2.0.2-1`, `calico-1.2.0-1`, matching r67's own notes (binutils 2.45.1, gcc 15.2.0). **A web-search snippet claiming "r68, 2026-06-10" is NOT corroborated by the API and was not believed.** (2) **The helpers are not compiled code**: all six objects the Makefile extracts (`Makefile:2508`) carry `754-sf.S` in `.debug_str` - they are **hand-written assembly from `libgcc/config/arm/ieee754-sf.S`** - so a newer GCC does not recompile them into anything faster. (3) **What is left from libgcc is ARCHITECTURE-INVARIANT, and what is not was already taken by this repo.** Comparing the linked `thumb` (**`Tag_CPU_arch: v4T`**) multilib against `armv6k/fpu` in the SAME install: only `_arm_addsubsf3.o` (684->592 B, **gains 2 `clz`**) and `_arm_cmpsf2.o` (276->252 B) differ; **`_arm_muldivsf3.o` (fmul+fdiv, 760 B), `_arm_unordsf2.o`, `_arm_fixsfsi.o` and `_arm_fixunssfsi.o` are BYTE-IDENTICAL across architecture levels**. And in the linked ELF (`build-c182-cfxnarrow-a-d0`) **`__aeabi_fadd`/`fsub`/`i2f` and all six `__aeabi_fcmp*` are ALREADY repo-authored** (Task 9 / Task 16), libgcc's originals renamed `__nds_task*_libgcc_*_golden`, and **the repo's `__aeabi_fadd` already contains a `clz`** while libgcc's `__addsf3` contains none. **So the CLZ opportunity in libgcc soft float is already banked, and `fmul`/`fdiv` - which are NOT already replaced - cannot benefit from any bump or retarget because their bytes do not vary with `-march` at all.** **devkitARM ships NO armv5te multilib** (`-print-multi-lib`: `.`, `thumb`, `be`, `armv6k/fpu`, `thumb/be`, `thumb/v6-m/nofp`) and configures `--with-march=armv4t`, so `-march=armv5te -mtune=arm946e-s` selects `thumb`, i.e. the v4T build; `__ARM_FEATURE_CLZ` is defined at armv5te and undefined at armv4t (`-dM -E`). **NO TOOLCHAIN WAS STAGED, INSTALLED OR MODIFIED** and no `pacman -Sy` was run - `pkg.devkitpro.org` is Cloudflare-gated to pacman, so **live pkgrels are UNKNOWN and that is the one unresolved half**; syncing modifies the live toolchain, which is an owner call. **RISK, STATED FOR WHEN THE OWNER DOES ABSORB ONE**: a codegen change re-bases every CROSS-BUILD number in the campaign (the >=14,080 P95 / ~5,700 P50 floors are calibrated on this compiler); **same-binary `.data`-route deltas survive, cross-build deltas do not.** `Makefile:2506/2527/2529` pin `libgcc.a`/`libc.a`/`libm.a` by SHA256, so a bump **fails the build loudly** at `sha256sum -c -` rather than drifting - the pin is live and correct (`c755adc3...` verified). **ITEM A - BOTH CHECKERS WERE RED SINCE THEIR OWN CREATION COMMIT `514fad238da` AND NEITHER WAS EVER WIRED**, and the cause is one commit for both: **`69ce92e279f` (Requirement 4)**. **MEASURED, NOT ARGUED**: the ORIGINAL `check_ftanim_transcribe.py` run against the port body at `514fad238da` and at `69ce92e279f^` (with the contemporaneous PATCHED decomp, recovered from the retired `scripts/decomp-patches/` patch) is **GREEN**; at `69ce92e279f` it is **RED**. **`69ce92e279f` is the commit that shipped the segment-phase defect**, which stood until `64c41c361a7` on 2026-08-15 - **nothing ran them, so nothing caught it.** **The token-equality proof is NOT recoverable, and pretending otherwise is why the file stayed red**: Requirement 4 restructured the port (lazy `NDS_R2_FTANIM_TRACKS()`, a second fixed-point representation behind `q`, `ndsR2AnimAdvanceTail`, hoisted `len_new`, the runaway guard), so no inverse-substitution table can recover the decomp text. It now proves the two invariants that SURVIVE the restructure, per opcode arm (both switches still carry the **same 16 labels in the same order**): **(1) ADVANCE SEQUENCE** - each arm's ordered `AObjAnimAdvance` reads, and the field each takes, must equal the decomp arm's (**12 arms, 25 advances**); **(2) HOIST LIVENESS** - any local the switch assigns at arm level must be assigned before every read in every arm that reads it. **Proof 2 re-fails the historical defect naming its three arms EXACTLY** (`SetVal{,Block}`, `SetValRate{,Block}`, `SetValAfter{,Block}`) and is green at `69ce92e279f^` and at the repair - a proven positive AND negative control. `check_ftanim_target_exact.py` was red because the extracted tail of `ndsR2AnimTargetValue` has carried `q != 0u` branches since the same commit and stopped compiling; it now passes **`q = 0`, the shipping route** (`NDS_R2_CUBIC_FIXED ?= 0`, `Makefile:455`), and links the Q helpers as **POISONED stubs** so reaching one aborts instead of quietly comparing something else. **524,288 cases, 0 mismatches**, and mutating one `sNdsR2AnimFracShift` entry turns it red on 65,535. **The Q route is NOT duplicated here** - `check_r2_cubic_error_bound.py` already proves it exhaustively over the same domain and is already wired. Both new checkers now run inside `check-gbi-decode-fixtures.ps1`, i.e. on **every** `verify-all.ps1` profile. **ITEM C - THE `GX_COMPOSE` RECORD IS SETTLED FROM THE ARTIFACTS AND 0.0692% IS THE CORRECT NUMBER.** "Pixel-identical" is the **slice-43-era, pre-blink** claim, which `GXCOMPOSE.md` section 6 itself forbids inheriting ("no capture was run, so 'pixel-identical' is UNMEASURED on this tree"). The capture was then RUN - `GXCOMPOSE.md` section 8, committed, evidence under `artifacts/visibility/2026-08-15_gxcompose-bank/` - on `build-c184-cap-a`/`-cap-b` at matched simulation-clock locks: **83/120,000 = 0.0692% (lock 50), 0.0358% (48), 0.1233% (1694), 0.1742% (1692), max channel delta 251**, localized on fighters/ground. **They are not rival measurements of one thing**: one is a stale pre-repair claim, the other is the measurement. **AND 0.0692% IS NOT A THRESHOLD** - `PREDICTION.md` section 5 pre-registered **ANY pixel difference** as the stop, so 0.0692% is a measured VALUE against a zero criterion, which makes `CAMERA_Q20_12.md:53/:426`'s framing of it as a bar the camera's 6.5350% must beat a category error. **`plan.md:1149` records that the owner then inspected the diff masks and accepted that delta** - a RELAYED owner decision I did not verify against an owner turn and did NOT act on. **Nothing changed: `NDS_R2_FIGHTER_GX_COMPOSE` stays `?= 0`, no capture re-run, `plan.md` untouched.** **RETRACTED IN-CYCLE, before it reached anything**: a first crude search read "0 references to the Task 9/16 goldens", then "7", from a regex matching symbol-header lines rather than branches. Re-run **with a control that fires** (2,116 branches to `__aeabi_fmul`), the goldens take **0 branch instructions** each and the lone branch to `__addsf3` is `__aeabi_frsub`'s internal fallthrough. **That is suggestive, NOT proven** - a literal-pool or function-pointer path is unchecked - so "~600 B of ITCM is reclaimable" is a **LEAD, not a finding**, and it matters only because the tick-HUD instrument has **580 B free ITCM**. **LEAD FOR A LATER CYCLE, sized by nobody yet**: libgcc's `__addsf3` normalization ladder at `+0xd0..+0x108` is **15 predicated instructions** where a CLZ form needs ~4 - but this repo already replaced `__aeabi_fadd`, so that ladder only runs if something still reaches `__addsf3`. **Do not price it as instructions x calls** - that is the residual-over-count trap - the free falsifier is a PC histogram over `__addsf3`'s 444 B on a v3 capture already on disk. **BOUNDARY GREEN, exit 0, 0 `Exception:`** (`builds/boundary-c206.log`, 316,156 lines, "Boundary verification profile passed."). **Both root ROMs BYTE-IDENTICAL before and after**: `smash64ds.nds` `54c07fac...ac68a`, `smash64ds-battle-playable-hwtri.nds` `6c939434...67c99` - **no ROM was published, and `6c939434...` is still the bore-84 link.**
Superseded 2026-08-16 - **THE CAMERA CHAIN IS BUILT AND MEASURED, AND THE 5.14x SAME-OPERATION PRIOR IS REFUTED IN SITU AT 1.70x** (`.../2026-08-16_camera-fixedpoint/CAMERA_Q20_12.md`; **3 lab builds, 4 gate runs**, two same-binary `.data`-route A/Bs, route ships at **0**, nothing banked). Two of the three camera producers converted to Q20.12 - the game camera `gmCameraLookAtFuncMatrix` (2.000 entries/fr) and the renderer adapter's `syMatrixLookAtReflect` (2.000/fr) + `syMatrixPerspFast` (2.138/fr) - sharing one look-at and one perspective kernel; **`syMatrixF2L` is DELETED, not converted, for all 6.138 of its entries a frame**; divides and roots go to the DS hardware units and **nothing was added to `__udivmoddi4`**. **NET, same-binary route (floor zero), `WORK-H`, 1,600 frames, `slips=0`: paired median -4,736 tk/fr, mean -4,728, improved 1,439/1,600, P50 -4,160, P90 -6,016, trimmed -4,573, top-80 paired median -5,568; FTR -3,264 + STG -1,664 and no other bucket moves.** **The rank-80 point estimate of -1,408 is NOISE**: rank-by-rank the delta is -7,808/-6,016/-4,160/-3,904/-4,160/-5,248/-4,992 at ranks 40/160/320/640/800/1200/1600 (a LEVEL, as a per-frame-constant workload must be) while ranks 10 and 20 read **+30,336 / +20,224** from cartridge-read frames that do not reproduce between sessions - **quote the paired per-frame median**. **THE RATE: gross 11,657 marginal-80 / 11,504 whole match, `R = gross/(gross-net)` = 1.700x whole match, 1.915x on the gate population, against 5.14x. It is an UPPER bound** (the numerator carries only the soft-float library bill; the float form's self time, which the prior's 2,921 tk did include, drives R toward 1). **Why it does not transfer: the prior is a pure multiply-accumulate pair, while a look-at needs 3 roots + 9 divides per entry and a perspective 5 more - the lane's transcendental half converts far worse than its arithmetic half, and the camera chain is its most transcendental-heavy member.** **ITCM IS NOT AVAILABLE and the 2,976 B free is the WRONG ROM** - the tick-HUD instrument is 32,188/32,768 = **580 B free** (2,976 is Boundary's manifest for the *proof* ROM); two links overflowed `itcm` by 916 B and 1,452 B; and structurally **a `.data` route can never test ITCM residency for a replacement of an ITCM-resident library**, because both arms must be resident at once. **SURPRISE, AND IT IS THE CYCLE'S MOST USEFUL RESULT: inlining the leaves INVERTED the win.** v2 made every leaf `always_inline` (42 calls/entry -> 12) for +3,032 B and the paired median went **-4,736 -> +1,600**; across the two same-binary pairs and the measured cross-build control term (+5,440 on two builds whose route-0 path is identical) the kernel became **+11,776 tk/fr more expensive**, ~30 cycles per added line per entry - inside the measured 23-51 cycle `icache_fill` band. **On a kernel entered a handful of times a frame, inlining is a COST.** Two build traps reproduced and fixed before any number was believed: the kernels first compiled to **Thumb** (18 `bl __aeabi_lmul` in one look-at), and even in ARM state `noinline` plus mismatched `target` attributes kept 42 calls/entry because GCC will not inline across differing target attributes. **ENGAGEMENT: 8,148 fixed look-ats / 8,224 fixed perspectives against 2 and 2 float (one pre-poke frame); control 0 fixed, 4,076/4,152 float; Saturate 0, Degenerate 0, Rescale 0.** **CONTROL: all eight new or routed symbols classify `draw+dispatch` 100.0% from the linked ELF, `gGMCameraMatrix`'s four readers (literal pools) are all display/present callbacks, and ALL SEVENTEEN match invariants are bit-identical across all four runs and equal to `build-c199-bank0`'s.** **2026-08-16 FOLLOW-UP - THE OWNER'S PLAY-TEST INSTRUMENT EXISTS AND THE DECISION IS STILL THEIRS.** The owner chose "build a playable ROM first" over deciding on still frames, so `builds/build-c205-camtoggle/smash64ds-battle-playable-proof-hwtri.nds` carries BOTH arms in one binary: new lab flag `NDS_R2_CAMERA_FIXED_TOGGLE` (**`?= 0`, never set by a target block** - it adds a keypad edge test to `ndsPlatformReadInput` and a HUD row, so a build carrying it is NOT the measurement instrument) binds **SELECT** - the one DS key the battle leaves unbound - to flip `gNdsR2CameraFixedEnabled` mid-match, printing `CAM FLOAT shipping[SELECT]` / `CAM FIXED Q20.12 [SELECT]` on battle text-HUD row 3. **In the repo's melonDS config SELECT is `0x81000020` = Qt::Key_Shift | bit31 (melonDS's right-modifier flag) = RIGHT SHIFT**, confirmed by driving it. **END-TO-END PROVEN, not argued**: linked-ELF disassembly shows `keysDown` -> `lsls #29`/`bpl` (KEY_SELECT bit 2) -> load/logical-not/store to the literal `0x020fde2c` = `gNdsR2CameraFixedEnabled`, and a live run flipped the HUD FLOAT->FIXED with the match continuing normally, no corruption (`artifacts/visibility/2026-08-16_camera-toggle-playtest/`). **THE FLIP CANNOT DESYNC**: both arms recompute every value they publish each entry, so nothing carries across the flip, and the only live field both write, `gGMCameraStruct.look_at`, has exactly two referrers in-tree (those two writes) with its sole source consumers `gSPLookAtX/Y`, RSP commands this port never emits. `NDS_R2_CAMERA_FIXED` **stays `?= 0`; nothing is banked and no default moved.**
Superseded 2026-08-15 - **THE DENSE ANIMATION RUNTIME IS WIRED AND ITS ON-TARGET ORACLE READS 0 MISMATCHES OVER 12,232 DECISION POINTS - AND IT FOUND A SHIPPED ONE-FRAME PARSER REGRESSION AND A SHARED-DECODER BLIND SPOT** (`.../2026-08-15_ftanim-dense-runtime/DENSE_RUNTIME.md`; 4 lab builds, 4 gate runs, **no default flipped**, `NDS_R2_FTANIM_TRACK ?= 0`, **no re-bank**). **DEFECT 1, REPAIRED, AND IT CHANGES THE SHIPPED ROM:** `69ce92e279f` (Requirement 4) hoisted `-anim_wait - anim_speed` into a local `len_new` in all four write cases of `ndsR2FtAnimParseDObjFigatree` and added the ASSIGNMENT to only one (`SetVal0Rate{,Block}`); the other three read a function-local reset to `0.0F` every call. `git show 69ce92e279f^` has the fresh expression at all four sites (:334/:375/:415/:476) and the decomp writes it fresh in each case (`ft/ftanim.c:158/193/232/293`). **Opcodes 4+5 are 45,679 of 55,261 items-off write commands = 82.7% and are in a case that never assigned**, so every new segment started at phase 0 and `gcPlayDObjAnimJoint`'s `length += speed` put the first sample a whole frame INTO the segment. Restored in three lines. **IT MOVES THE FIGHT - spark 15->16, shield 1,352->480, AObj high-water 1,266->774, packHits 197->257 (P1Damage 76, runaway 0 unchanged) - so NO tick comparison against the c185 bank is like-for-like and none is made; `build-c193-segfix` reads rank-80 1,228,608 and THE BANK MUST BE RE-ESTABLISHED ON THE REPAIRED TREE before the next performance verdict.** **DEFECT 2, found by the oracle on its first run:** `ftanim_reloc_probe.decode_script` filled `targets` only for commands carrying per-track WORDS, so opcode 11 `AddLen` (which scans `flags` and adds the payload to `length` per selected track) looked like it touched nothing - and `ftanim_script_model.run_commands` iterates the same `targets`, so reference and candidate shared the wrong answer and layers A/B/C could not see it. Fixed at the decoder; corpus hash `cb28f9bf65c4` -> **`64b2f5a6a7e8`**, sizes unchanged; the three-layer proof re-run PASSES (4,901 scripts / 66,022 rows / 81,646 commands / 75,237 states -> **0**, falsifiers 1/1/4,272). **STAGE 3 WIRED:** bind once at `lbCommonAddFighterPartsFigatree` (keyed by figatree ENTRY index, new `ndsBattlePackAssetIdForSlotTable` gives the id from the pointer), then step typed rows - no command-word decode, no flag scan, no per-call AObj list walk, no per-call Q migration, no `ftAnimGetTargetValue`, and `ndsR2AnimRecipSlot`'s f32->Q30 becomes one `ldr` from a generated table built by a bit-for-bit Python model of `ndsR2F32ToFixed`. **ENGAGEMENT IS EXACT ON ONE ROM** (`-SetGlobals gNdsFtAnimTrackDispatch=1|0`): generic parse calls 144,383 -> 115,288 = **-29,095**, and the dense counters read **24,197 early-out + 4,898 stepped = 29,095 EXACTLY**; control reads hard zero on all five. All six invariants identical between the arms. **STAGE 4 ORACLE = 0 MISMATCHES over 12,232 decision points**, fail-closed, and **PROVEN ABLE TO FAIL** (it fired 4 and cleared the route word before defect 2 was fixed). It compares DECISION POINTS - opcode class, block bit, flag mask, payload, every per-track word, in order, with the reference cursor advanced by the parser's own Loop rule - because `func_anim` has no writer and an observed-callback oracle cannot fail. **A/B IS TICK-NEUTRAL AND NOTHING IS BANKED: rank-80 1,231,872 -> 1,232,000 = +128, P50 +576, trimmed mean +1,261, `SINT` +77 trimmed / +1,664 rank-80.** Placement floor is ZERO (one poked word). **A PARTIAL conversion cannot win by construction** - the generic parser still serves 79.8% of calls so its bytes stay hot, and the dense stepper's 3,368 B of code plus 12,244 B of sparsely-read rows are pure ADDITION to the fetch footprint. **Coverage is 8 of 137 Fox clips** because the pack is `.rodata` and the real budget is `gNdsTaskmanGeneralHeapFreeMin` **53,136 minus the mandated 32,768 reserve = 20,368 B**, not the boot ladder's 312,448; free-min ends at **40,848**. Selection is ascending asset id = ascending motion index, and it shows: 5.8% of clips take **20.4% of clip binds**. **No v3 capture, so the neutrality is NOT attributed** (icache vs off-percentile deletion) - one v3 run on this same byte-identical pair splits it with no rebuild. **NEXT: full coverage, which means the pack in the ARENA in place of `battlepack_fox.bin` (stage 2's proven -822 B drop-in), a cross-build A/B, and a fresh bank on the repaired tree.** `check_ftanim_transcribe.py`/`check_ftanim_target_exact.py` still RED and unwired - and defect 1 is exactly what a live transcribe checker would have caught the day it landed.
Superseded 2026-08-15 - **THE DRAW-ONLY SOFT-FLOAT LANE IS 34,178 tk/fr, IT IS FLAT, AND THE COLLISION LANE'S 1.00 EXCHANGE RATE IS FALSIFIED AT 5.14x - THE 16K GATE IS PASSED WITH NO BUILD SPENT; SEPARATELY, THE FOX BORE IS 0 BY THE OWNER'S OWN DECISION AND THE "FABRICATED APPROVAL" CLAIM IS ITSELF RETRACTED** (`.../2026-08-15_drawside-softfloat/DRAW_FIXEDPOINT.md` + `.../2026-08-15_ftanim-full-coverage/REBANK.md` section 2; **0 builds, 0 emulator runs for Task B**, 1 Boundary, commit `b16dc16997a`). **TASK B - DRAW-SIDE FIXED POINT, PRICED FROM A v3 ALREADY ON DISK.** Arm `build-c200-trackprof-off` (c185 config + `TASK37_PROFILE=1`, `TICK_HUD_DRAW=0`, `GX_COMPOSE=1`, `DISPATCH=0`), marginal-80 threshold **1,224,970** - **1,654 from the bank's 1,226,624, so the mask IS the gate population**. **THE STALE "31.2% x 94,602" SHORTCUT IS NOT USED ANYWHERE.** Soft float total **168,060 tk/fr** at rank-80 (cross-checked 165,187 by direct `nm` range sum, 1.7% apart; whole match 78,460, class concentration 2.11x). **PHASE SPLIT FROM THE LINKED ELF, not from names** - reverse call graph pruned to the **1,363 of 3,803** symbols that executed, roots labelled by callback ROLE before module prefix: **shared 57,521 (34.2%) - sim-only 39,537 - sim+dispatch 37,662 - DRAW+DISPATCH 17,407 - DRAW-ONLY 13,231 - unresolved 2,702**. **DRAW-SIDE = 30,638 tk/fr, +3,540 draw-side `sqrtf` = 34,178 = 0.420x of +81,297**, over 2,271.9 helper calls/fr, 872 call sites, 35 functions, 43,214 B. **IT IS FLAT: draw-side concentration 1.12x against the class's 2.11x** - the concentration lives in the collision half, so the draw side converts 1:1 against a LEVEL gate. Categories: camera+projection **13,209** / particle+quad **9,015** / fighter matrix prep 3,445 / renderer adapter 2,431 / material+colour 1,301 / HUD 1,084 / stage 153. **THE DECIDING MEASUREMENT, AND IT COST NOTHING: the same operation exists in this binary in both forms.** A float 4x4 concat `guMtxCatF` costs **2,921 tk/call** (140 B, 1.00 entry/fr, 64 fmul + 64 fadd reproduced to the unit); the port's 20.12 concat `ndsRendererMtxMul20p12` costs **568.40 tk/call** (312 B, 18.55 entries/fr). Same mask, same build, both live. **RATIO 5.14x - the collision lane's 0.987/1.00/2.68 does NOT transfer.** **AND THE MULTIPLY IS 1.00 TICK EITHER WAY**: every long multiply in the image runs 2.09 cyc; `ndsRendererMtxMul20p12`'s `smlal` and `__aeabi_fmul`'s own `umull` are both **2.00 cyc**, so all 26.5 cycles of difference are unpack/align/normalise/round. **Corollary: only 11.3% of the fixed concat's cost is its multiplies (1,187 of 10,544 tk/fr) - a design that counts SMULLs and calls the rest free will over-claim.** **WHY THE FETCH STORY DIFFERS FROM THE RING**: the float library is **ITCM-RESIDENT** (all 14 helpers at `0x01ff8000`+, `Task 9 float ITCM passed itcm=29792/32768 **free=2976**`), which is why a float call is ~0.8 fetch-tick and why the ring's `.main` replacement at 0.97 entries/fr lost issue -1,717 to icache +1,854. The draw package need not repeat it: **1,242.4 calls/fr (55% of the lane) convert to inline add/sub/cmp/shift that are SMALLER than the 4-byte `bl` they delete**, and the multiply can stay a `bl` to a ~32 B ARM leaf in ITCM's 2,976 free bytes. **CONSERVATIVE CEILING - three routes, lowest quoted: 24,677 (measured 5.14x) / 25,814 (per-op inline) / **21,364 (pessimistic ITCM-leaf)** + ~3,200 sqrt via the hardware unit = **24,564 tk/fr = 0.302x = 1.54x the 16,000 floor. THE GATE IS PASSED; THIS IS NOT A STOP.** **REFUSED BY CONSTRUCTION**: `__udivmoddi4` is **already** running 11.70x/fr at 248.65 tk/call = 2,909 tk/fr and no design adds to it; no generic 64-bit divide/sqrt loop; no large cold kernel. **THE DS HARDWARE DIVIDE AND SQRT UNITS ARE UNUSED IN THIS TREE** - a search of `src/`+`include/` finds only the two still-undefined hooks `NDS_R2_CFX_DIV64`/`ISQRT64` (`nds_r2_collision_fixed.h:205/:215`), so 73.7 fdiv/fr and 24.3 draw sqrtf/fr have an uncontended seam, owned by whoever defines it (single shared registers, no interrupt-time use). **THE THUMB TRAP IS LIVE AND MEASURED**: `__aeabi_lmul` runs 539.2 `muls`/fr at **4.49 cyc**, 2.2x a hardware `umull` - every Q multiply must be `target("arm")` or an ITCM leaf, never plain Thumb `(s64)a*b`. **NOT DONE, PLAINLY: no implementation, no build, no A/B, no pixel pair, no counters, and owner visual acceptance is not an agent's to give.** No sub-package clears 16K alone (camera+projection is the largest at 13,209; with the renderer adapter 15,640), and a partial conversion is the shape that already failed twice here. **NEXT STEP IS NAMED AND IS A FALSIFIER, NOT THE PACKAGE**: one build, one same-binary route, one gate run on the camera chain only - `syMatrixLookAtF`/`LookAtReflectF`/`PerspFastF`/`guMtxCatF` into Q20.12 so `syMatrixF2L` (1,933 tk/fr) is DELETED rather than converted; gross **15,812 tk/fr**, 2,764 B, 181 sites, 19.2 entries/fr. **The route word needs an explicit `.data` section attribute** or it lands in `.bss` and carries the ~10,000 tk/fr placement floor the last pair paid. **THREE TOOL DEFECTS FOUND**: `objdump --no-show-raw-insn` makes `analyze-leaf-helper-attribution.py` return a **3-caller** table with `__aeabi_fadd` at **86,183 cyc/call** instead of failing (new `classify-softfloat-caller-phase.py` now REFUSES under 1,000 function labels); a whole-match `--census` against `--mask marginal` read fadd at 514.4 instead of 38.0 cyc/call, **13.5x** - use `census-marginal-frame-owners.py --report --census-out`; and `--thunk fsub=fadd` matches no symbol and silently disables the fold (pass no `--thunk`; the default is correct). **INTEGRITY, CORRECTED: an earlier revision of this surface asserted that two verbatim owner verdicts had been INVENTED by the agent and deleted them (`88abf259bda`/`9b25d4e1095`). THAT ASSERTION WAS WRONG - BOTH QUOTES ARE GENUINE OWNER SPEECH, relayed accurately; the fabrication conclusion was inferred from a working tree that contradicted them (an uncommitted restore of `?= 84` of unknown provenance), NOT from the owner. Deleting a real owner verdict on tree evidence is the error being corrected. Restored verbatim: *"fox beam is perfect!"* and *"i said it was perfect, that includes the mario crouching avoiding the beam"* - and the second exists precisely BECAUSE the acceptance was challenged with v5's crouch geometry, so IT COVERS THE CROUCH CASE, not only the visual.** **THE OWNER HAS SINCE SETTLED IT DIRECTLY: *"bore should be zero, no offset, not needed anymore"*. `NDS_FOX_BLASTER_BORE_OFFSET_Y ?= 0` is the shipping default in `Makefile` and `nds_effects.h`, still build-overridable; `docs/BUGS.md`'s Fox row is CLOSED, not `BLOCKED`.** **`plan.md` IS NOT CONTAMINATED AND THE INSTRUCTION TO DELETE IT IS WITHDRAWN** - it is the orchestrator's own campaign record, untracked and owner-owned; its quotes are genuine; do not edit it. **v5 IS MEASUREMENT ON A STALE POSE AND IT DOES NOT CONTRADICT THE OWNER**: both terms are evaluated poses captured 2026-08-12 inside the defect window, and it reads ONE SPHERE FROM TWO DIFFERENT EDGES (`45.180648` off the laser bottom, `1.180648` off its top) - read consistently the bore-0 figure is **-38.819352** and the bore needed to clear that stale pose is **>=38.82, not 84**. A re-capture (`probe-fox-crouch-collision.ps1` + the hurtbox dump) refreshes both terms; it is a documentation refresh, not a gate. **THE BANK is `build-c200-bank84`** - c185 configuration on the repaired tree at bore 84 (`BOTH_CPU=1`+`BATTLEPACK=1`+`KEEP_CACHE=1`+`GX_COMPOSE_LAB=1`, generated header reads `GX_COMPOSE 1`), DLDI on, mode 163 one-minute, 1,600 samples, `-RingDump`, frames 439..2038, `slips=0`: **P50 942,656 - P90 1,123,328 - rank-80 1,226,624 raw / 1,201,677 net - top-1% 1,546,304 - max 5,090,560 - trimmed mean 963,510 - over-gate 166/1600 - VBI 2:1711 3:303 4:16 5+:8 max 19 total 2038**. **THE EXACT NET GAP TO 1,120,380 IS +81,297.** **`+28,689` IS DEAD AND SO IS EVERY LEVER PRICED AGAINST IT** - c185 measured a match the shipped segment-phase defect made CHEAPER, so the requirement did not grow because anything regressed; the `MENU.md` ladder must be re-read against +81,297. **BASIS, because two conventions are in circulation: rank-80 is recomputed from the 1,600 per-frame rows in the run own JSON** (it reproduces the `DENSE_RUNTIME.md` table exactly); the harness banner `p95` column uses a different rank convention and is NOT the banked figure. **TWO INDEPENDENT SHIPPING-BORE ARMS BRACKET THE LEVEL**: `c193-segfix` (earlier HEAD) 1,228,608 and `c200-bank84` 1,226,624 are **1,984 apart**, inside the >=14,080 cross-build P95 floor, P50 256 apart inside ~5,700. The bore-0 arm `c199-bank0` reads 1,230,720, so **the whole 84-vs-0 spread is 4,096 at rank-80, inside the same floor - the bore is not a performance question either way.** Invariants **P1Damage 76 - spark 16 - shield 480 - AObj high-water 774 - packHits 257 - runaway 0 - CaptureOutcome 2 - SegmentMask 161**, parser 144,383/108,128/36,255/70,796, arena **ChosenSize 1,548,288 AllocFail 0**, BattlePack resident 287,904 fails 0, heap free-min **53,136** against 32,768 - **IDENTICAL on all three arms**, so the 84->0 bore move changed no blaster hit outcome in this deterministic match. **WHY 84 EXISTED AND WHY IT IS GONE**: it was tuned by eye 24->36->48->72->84 on 2026-08-14, one day BEFORE `64c41c361a7` repaired the stale-pose defect, i.e. it was compensation for a gun-joint pose that was a frame stale - and the pose is repaired, so at 0 the laser sits on BattleShip's own source line. `make ... NDS_FOX_BLASTER_BORE_OFFSET_Y=<n>` still trials any value with no source edit. **NO DESYNC IS EXPRESSIBLE AT ANY BORE VALUE**: one definition (`nds_effects.h`), three consumers - beam draw `nds_renderer.c:15375/15379`, glow draw `battleship_lbparticle.c:2588/2590/2606`, weapon attack collision `battleship_fox_blaster.c:77/78` (`lr`-signed so both facings give world +Y) - a tree-wide search for `344064`/`0x42a80000`/`0xc2a80000`/`84.0F` returns NOTHING else, and the BASE is also one value, `gmCollisionGetFighterPartsWorldPosition(joint 17, {60,0,0})` handed to both the weapon and the glow. **THE OTHER THREE 2026-08-14 EYE-TUNED FIXES ARE ENUMERATED AND NONE IS SUSPECT**: Whispy is a per-frame validation fix with no constant and stage joints take `gcParseDObjAnimJoint` (AObj32) at `reloc_backend_compat_shims.c:2057` while the repaired parser is the `else` arm at `:2061`; Dream Land BG `K=9/8` is a static q16 background scale; P1 VFX symmetry is a missing MASKS/MASKT atlas submit. **Only the bore read a pose.** **TASK B - FULL COVERAGE IS REFUSED ON ARITHMETIC, BEFORE A BUILD WAS SPENT.** Stage 2 "-822 B drop-in" is a BYTE-COUNT IDENTITY, not a functional interchange: three consumers need the o2r stream `battlepack_fox.bin` carries - (1) the bind recovers the asset id from the o2r POINTER via `ndsBattlePackAssetIdForSlotTable`, (2) the generic parser is the fail-open path and **Mario is unconverted entirely** (`BindMiss` 296 of 372 clip binds even at 8 clips), (3) the oracle reference cursor IS the o2r stream (`const AObjEvent16 *ref`). So full coverage means COEXISTENCE, and coexistence needs **+287,082 B of arena**: measured `ChosenSize 1,548,288` with `AllocFail 0` against a banked grantable ceiling of **1,564,672** leaves **+16,384** - **SHORT BY 270,698 B**. `.rodata` is bounded by the same meter at 53,136 - 32,768 = **20,368**. **A sizing failure is a STOP; no arm was built, no soak and no gate run spent.** Full TWO-fighter coverage is 557,670 B against a **1,096,512 B** taskman residue = **50.9% of everything taskman has left**, and the 163,840 B raw-cache reclaim does not close it - **it needs the o2r dependency designed out, which is not a budget problem.** **THE v3 WAS TAKEN AND IT CLOSES THE ANIMATION-REPRESENTATION LANE** (`.../2026-08-15_ftanim-dispatch-attribution/RESULT.md`, prediction registered in its `PREDICTION.md` before the first build; 2 `NDS_TASK37_PROFILE=1` builds `build-c200-trackprof-on`/`-off` differing only in `NDS_R2_FTANIM_TRACK_DISPATCH`, 1,600 frames each). **THE FETCH BRANCH WAS TAKEN**: the dense side is **72.4% icache+dcache fill** (2,736 of 3,781 tk/fr whole match) and the eviction signature is present - the generic parser SURVIVING calls cost **+5.14%** more per call on the ON arm. **THE ISSUE BRANCH IS REFUTED** three placement-immune ways: the ON arm executes **664,438 FEWER instructions** (-415/fr) and its issue stalls fall 338.5 tk/fr. **RETRACTED - MY OWN "+69.4 tk per exchanged call, ~1.59x the generic path": the exchange is EXACTLY 1:1** (ON 54.01 generic + 16.36 dense = 70.37; OFF 70.37, two decimals) **and the dense call costs 208.0 tk against the generic parser 215.5 = 0.965x. The +69.4 was A RESIDUAL DIVIDED BY A COUNT** - my third instance of that trap - **and its consequence "full Fox ~ +3,090 tk/fr" is wrong IN SIGN and 9.7x in magnitude**: measured whole-match named exchange at 23.25% parse-call coverage is **-74 tk/fr**, linear to 100% **-319 tk/fr**. **AND THE LANE IS DEAD AS A GATE LEVER**: both sides of the exchange are ~3,800 tk/fr and they CANCEL, so full conversion is worth order **10^2 tk/fr** against the +81,297 gap (<0.005x; bounded under 4,700 = <0.058x even allowing full contamination by the pair measured **~10,000 tk/fr ABSOLUTE placement floor**, inside which every net in that document sits - **nothing is banked**). **33,951 was the lane SIZE; the representation converts ~1% of it**, because the parse path is fetch-bound and the replacement is another fetch-bound path of similar footprint. **CONSEQUENCE: THE FULL-COVERAGE ARENA ARM NO LONGER NEEDS BUILDING** - the 270,698 B shortfall stops mattering because the mechanism is priced without it. **TWO TRAPS RECORDED THERE**: the brief "one `.data` word apart" pair DOES NOT EXIST as specified (at `DISPATCH=0` the route word is zero-initialised so it moves to `.bss` and every later `.data` object shifts 4 B; `.text` is address-identical, 3,842 symbols, `nm` diff 0, so PC attribution and icache are sound but the data side is a cross-build pair - fix is an explicit `.data` section attribute on the route word, recorded not made); and a first reading that the dense path DELETES soft float was retracted before publication - it deletes ZERO (66,457 vs 66,446 tk/fr at 3,722.64 calls/fr on BOTH arms), the apparent deletion was a 3-of-80 frame population difference between the two arms OWN masks. **BOUNDARY GREEN at the bore-0 shipping default, 0 `Exception:`.** **Root ROMs entering this cycle: `smash64ds.nds` `54c07fac...ac68a`, `smash64ds-battle-playable-hwtri.nds` `6c939434...67c99`. THE BORE MOVE CHANGES BOTH AT THE NEXT PUBLISH** - `6c939434...` was linked at bore 84 and the constant reaches the shipped battle ROM through all three consumers, so expect a NEW `smash64ds-battle-playable-hwtri.nds` hash the first time it is rebuilt; that is intended, not drift. **The move is gameplay-inert over this deterministic match** - all eight end-of-match invariants and the four parser counters are bit-identical across the bore-84 and bore-0 arms. **HARNESS TRAPS PAID FOR TWICE**: `sample-tick-hud-buckets.ps1` needs **pwsh 7** (ternary) and dies with a parser error under Windows PowerShell 5.1; and through `cmd /c` you must use cmd own `> log 2>&1`, because the PowerShell `*>` is passed through as a POSITIONAL argument and silently binds to `-MelonDS`. Both fail instantly and both are invisible without the full log.
Superseded 2026-08-15 — **c185 GX-compose remains the bank; renderer-state micro-cuts are CLOSED below the 16K floor.** Bank unchanged: P50 **938,112**, P90 **1,088,192**, rank-80/P95 **1,174,016 raw / 1,149,069 net**, top-1% **1,520,832**, over gate **122/1600**, exact gap **+53,636 raw / +28,689 net**; DRAW=0 cadence **90.731%**. Task107 (`…/2026-08-15_renderer-state-redundancy/STATE_REDUNDANCY.md`) measured tile sync **146,221 calls / 106,500 exact repeats = 72.835%**, ceiling **6,458 tk/fr**; + proven no-Z **1,305 = 7,763 exact local**. Binds: 208,327 requests / 95,934 already-elided / 112,393 issues / 26,769 same-frame revisits; revisits require ordering, and even perfect ordering adds only **2,484** → **10,248 total** (impossible all-sync upper **12,656**). Zero overflows, invariants/GXSTAT match, Task107 symbols survive in census and are absent in shipping, default ROM pre/post SHA **6C939434…67C99 byte-identical**. **No micro-cut, no re-bank, no new cadence run. Published GX compose remains pinned OFF.** **SITR DECOMPOSITION IS DONE AND `SITR` DOES NOT CLOSE AS A BRACKET — ITS OWNER IS ANIMATION AND THE MECHANISM IS 41,376 tk/fr** (`…/2026-08-15_sitr-direct-children/SITR_DIRECT_CHILDREN.md`; 2 lab builds, 2 v3 captures, 3 reduce passes, 1 Boundary, **0 production source edited, 0 flag flipped, no re-bank**). Arm `build-c192-sitr-profile-gxc` is the c185 bank configuration to **four defines** (profiler, `TICK_HUD_DRAW=0`, two default-0 census defines, git string) with **`GX_COMPOSE=1`** via the documented `NDS_R2_FIGHTER_GX_COMPOSE_LAB=1` escape — the first arm came out at flag **0** because the tick-HUD block `override`s it, caught by reading the generated header. **DIRECT CHILDREN, marginal-80 (mask ≥1,172,523 ticks, basis cycles/160), against the measured bracket:** `ftMainPlayAnim` (ANIM) **89,099** at **1.55x** (whole 57,356) > status callbacks **62,155** at 2.47x > root body 7,986 at 1.06x > `ftMainUpdateMotionEventsAll` 2,998 > `ftMainUpdateColAnim` 1,905; **`ftComputerProcessAll` 12,435 is SUBTRACTED by construction (it IS `SCPU`)**; `ftKeyProcessKeyEvents`/`ftHammerUpdateStats` measure **0.00 calls/frame**. Named non-SCPU subtotal 164,144 = **63.6%**; the 94,052 residual is the shared leaf pool (soft float 2,819+3,086 calls/fr, `ftGetStruct` 9,806 at 264/fr, memcpy/memset) and is **never charged twice** — the static SHARED row is 508,694, 1.97x the whole bracket, which is why the split is built from **measured call rates against program-wide caller sets**, not reachability. **THE MECHANISM: the PARSE half of the animation lane — `ndsR2FtAnimParseDObjFigatree`+`BuildTrackTable`+`TargetValue`+`AObjToQConvert` — is 41,376 tk/fr at rank-80 (whole 18,564, conc 2.23x), 96.94 calls/marginal frame re-deriving AObj fields from STATIC FIGATREE ROM DATA = 1.44x the +28,689 requirement, 2.59x the 16K floor.** It is a representation change (AOT typed track rows in the Q form `ndsR2AnimValueQ` already consumes), **not** the evaluate half (`gcPlayDObjAnimJoint`+`ndsR2AnimValueQ` 53,818 at 1.40x, already fixed-point, reducible not deletable), **not** AObj layout, **not** cadence. **Hand it to the animation-representation task verbatim.** **RETRACTION: `SITR = 310,662.4 marginal-80` is one instrument frame.** Frame **756** carries a 2^22 event in `SINT` (4,163,136 over median, 0.74% off 4,194,304) inside the c185 raw top-80 and alone contributes **+50,309**; the honest bracket is **260,354** (DRAW=1, 79 frames) / **258,196** (DRAW=0, 80) — two arms 0.83% apart. **The callback verdict is untouched and strengthened** (6,373.5 / 14,395.5 are 2.5% / 5.6% of a 16%-smaller bracket); `NDS_TASK108_SITR_CALLBACK_CENSUS` stays default OFF. **SAME FIGHT, PROVEN**: dropping frame 756 lifts the two c185 arms' `SINT` correlation 0.5790 → **0.9991** with `SCPU` 0.9998 / `SPRM` 1.0000 already there; three profile arms at three HEADs correlate **r ≥ 0.982** (c192-c191 72/80 mask overlap). **INSTRUMENT TRAP, MEASURED: the profile's frame→region map is `frame − 439`, not the banner's `− 438`** — at `−438` the c185 rank-80 frames land at median profile rank **454 of 1600** and every `SITR` row reads *below* its whole-match rate; at `−439`, median rank **43**, 63 of 79 inside the profile's own top-80, and every row reproduces the primary table within 5% (PARSE half **40,003** vs 41,376). Lags −1/+2 are as bad as 0. **Cycle 109's "~96% in ftMainPlayAnim + ftComputerProcessAll" is half refuted**: right about the owner, wrong about the size — the pair is 39.3%, and the AI half is subtracted out. **Also sized for the first time on the marginal mask**: `ndsRelocAssetIDForToken` **4,228 at 6.22x** (the K0 line-7 residue, still unfixed, 0.15x the floor alone). **TOOLING**: `analyze-subtree-attribution.py` gained `--child-group` (a dispatch slot is ONE child, not 60; an inlined source child gets its name back), `--census-whole`, and `--callers`, which prints each subtree symbol's exact entry-PC call rate and program-wide caller set — that is what turned an unusable SHARED row into an attribution; `census-marginal-frame-owners.py` gained `--region-list` carrying the measured `−439` alignment in its docstring. **Both root ROMs BYTE-IDENTICAL before and after** (`54c07fac…`, `6c939434…`). **BOUNDARY GREEN at the shipping default, 0 `Exception:`.** **DECOMP SOURCE-OF-TRUTH ADOPTED (owner, 2026-08-15):** all ten `scripts/decomp-patches/` patches retired; `decomp/` is byte-for-byte upstream and provably so (`fetch-battleship-reference.ps1 -VerifyOnly` now hashes 10 pinned files **and** the whole `decomp/src` tree; `check-decomp-pristine.ps1` also fails on any `SSB64_TARGET_NDS` marker and on the mere existence of a decomp patch, and it runs inside `check-gbi-decode-fixtures.ps1` on **every** `verify-all.ps1` profile including Boundary). Eight patches now apply to an ephemeral `$(BUILD)/battleship_overlay/` copy via `generate-battleship-import-overlay.ps1`; `ftmain`/`mntitle` were lifted into `src/import/`. **Zero built bytes moved:** `build-c190-overlay-byteproof` rebuilt the c185 configuration at the same HEAD and `compare-elf-sections.py` reads **0 differing bytes** over `.itcm/.text.hot/.text.hot.draw/.main/.main.rw/.dtcm`, all section headers identical (only `.debug_*`), `.nds` both 12,541,952 — **the bank stands.** **`-freorder-blocks-and-partition` is REFUTED at the capability gate, 0 builds:** devkitARM GCC 15.2.0 prints `'-freorder-blocks-and-partition' not supported on this architecture` for `-mthumb` armv5te, `-marm` armv5te and armv7-a; no `.text.unlikely` is emitted, so `HOT_FOOTPRINT`'s 42,892 B needs manual out-of-lining, not a flag.

## R2-07 `BUGS.md` — symmetry FIXED / OWNER-CONFIRMED; three P1 atlas-coverage cells remain open

`docs/BUGS.md` carries the owner's own wording and is the queue — do not reword it. The 2026-08-12 playtest
closed old rows 2/3, fire burn and Fox gun (`artifacts/bugs/2026-08-12_r2-07-cluster/`,
`…/2026-08-12_c130-fire-gate/GATE.md`), and the "all three rows converge on ONE texture-residency capability"
framing was wrong — never reinstate it. **A magenta bar beside Fox is the BEAM ITSELF**, relocData 316's
RGBA(219,0,134), not a debug quad. Rows 1/2 DECIDED in `…_c-residue/OWNER_DECISIONS.md` §1/§2.

- **Whispy — FIXED, OWNER-CONFIRMED 2026-08-14.** Dynamic stage matrices now validate every frame; direct A/B + owner eye closed it.
- **Fox — CLOSED 2026-08-15, OWNER-DECIDED: the bore is 0.** *"bore should be zero, no offset, not needed anymore"*. The 2026-08-14 "perfect" at **84**
  was tuned against the frame-stale pose `64c41c361a7` repaired; the owner's bore-0 acceptance covers **crouch avoiding the beam**, not only the visual.
- **Dream Land BG edges — FIXED, OWNER-CONFIRMED 2026-08-14.** DS-only centered **K=9/8** covers far-floor overscan;
  affine q16 scale metadata stays bit-exact. `artifacts/verification/2026-08-14_dreamland-bgstretch/`.
- **P1 VFX symmetry — FIXED, OWNER-CONFIRMED 2026-08-14.** MASKS/MASKT was missing from the common-atlas submit; **19**
  masked scripts reconstruct 2/4 pieces over the same rect; ledge FlashMiddle is script5/texture2/**MASKST**. ROM **8C7507F0…**; `…/2026-08-14_p1-vfx-texture-audit/`.
- **P1 coverage 4/11/14 — IMPLEMENTED 2026-08-14, NEEDS THE OWNER'S EYE ON A SHIELD BREAK AND A SIDE KO.** Source **32x32**, cell cap **64**, frame cap **1**, **same four 8 KiB sheets**. It was **packer waste, not VRAM** — 27,520 of 32,768 held, and the shelf packer's 5,248 free texels were 16-tall tails no 32x32 cell can use, so the **64 -> 32 cell-cap shortcut was never necessary**.
  Palettes went **one per sheet** (zero VRAM; `glColorTableEXT` already ran per sheet), which stops the extra cells pulling the shared k-means: **24 of 31 decode BETTER, 2 worse by <=0.002, none dropped**. 28/31/35/36 stay out **by name** (`QUAD_P1_DEFERRED`) — admitting 31+35 cost texture 33 **+39%**. ROM **2015FBD1…**; `…/2026-08-14_p1-texture-4-11-14/EVIDENCE.md`.
- **THE TREE IS SETTLED AND THE TWO REDS ARE ATTRIBUTED (2026-08-14).** `HEAD DID NOT COMPILE`: `33d7cc5d3b7` shipped `nds_renderer.c` with `mirror_mask` in `ndsRendererSubmitParticleQuad`'s definition while its declaration stayed in the worktree. All four owner-confirmed 08-14 fixes are now committed (`9b6c9e72a25`).
  **`check-gbi-decode-fixtures.ps1` IS GREEN.** Two separate faults, one hidden behind the other: the M3 consumed-field certificate still classified `workspace.slice44_validate_cursor` under `…PrepareNativeStageMatrices` after the Whispy fix removed that read (narrowed in the generator + manifest, the field still lives under `…PrepareNativeStageOwner` and the rigid guard); and `33d7cc5d3b7` respelled `ndsRendererScanList` to `NDS_R2_CENSUS_EVICTED_CODE`, so the five-hot-path count read four (`813207773c1` teaches the count the alias **and pins the alias to `NDS_RENDERER_HOT_CODE` at census 0**).
- **BOUNDARY IS GREEN 2026-08-14 — THE RED WAS A CORRUPT DLDI SD IMAGE, NOT A COMMIT, AND THE FIVE-COMMIT BISECT WINDOW IS REFUTED** (`artifacts/verification/2026-08-14_boundary-red/BOUNDARY_RED.md`; log `boundary-after-dldi-reset.log`, 0 `Exception:`, marker capture **27.8 s of the 120 s ceiling**). **No source byte changed.** `emulators/melonds/dldi.bin` (gitignored, 536,870,912 B) made the ROM load NOTHING — `sNdsRelocLoadedFileCount 0`, `gNdsRelocAssetPayloadReadCount 0` — so `gMPCollisionGroundData->wallpaper` stayed the raw token **`0x3eb`** and `lbCommonMakeSObjForGObj`'s `sprite->bmsiz` (`[0x41c]`) took a data abort. Calico's `__excpt_entry` then **disables the PU** and `blx`es junk slot `0x205`, so the ARM9 slides through zeroed RAM in ABORT mode (`cpsr=0x400000b7`, `r12=0x205`, `lr_usr=lbCommonMakeSpriteGObj+26`) and every capture times out. Three arms: 512 MB image = ABORT · DLDI **off** = SYSTEM (diagnosis only, DLDI-on is retail parity and ~29,696 P95) · **fresh 16,957,440 B image = SYSTEM and green**. Old image kept at `dldi.bin.broken-2026-08-14`. **The window is refuted by measurement, not argued**: `build-c-collfixed` (08-13 19:25) and `build-c156-vfxsymmetry` (08-14 10:44) — both BEFORE the window — abort identically. Eight of the eleven commits ship no byte anyway (`54d7d7862e4` is comment-only; `813207773c1` is a checker the Makefile never invokes). **`gdb-markers.ps1` now classifies a timeout**: gdb's attach line reading `in ?? ()` means the guest had already crashed, so do not raise the ceiling. **A second attach is impossible — melonDS's stub refuses every reconnection**; sample the PC with a FIRST attach instead. A probe that may be killed still needs `set logging enabled on`.
- **R2-08 IS ONE MAKEFILE LINE, STAGED NOT LANDED — §6 items 1 and 4 RE-MEASURED ON THIS TREE 2026-08-13**
  (`…/2026-08-13_c-r2path-recheck/SWITCH_READY.md`): Boundary green through `NDS_R2_PATH=1`; soak `NO-FREEZE`, 2 START rematches;
  gate arm 929,344/**1,204,352** vs control 924,864/1,210,944 — inside the floors, opposite in sign, invariants identical,
  **+80 B text**. Owner-only: `:391` retail test and `:385` the eye — take the eye AFTER the flip, Boundary shoots `PATH 0`.

## The two arms — same 60 s match; ONLY `BOTH_CPU 1` is R2-07's gate

| arm | role | `WORK-H` P50 | P95 |
|---|---|---:|---:|
| `battle_playable_realtime` (`BOTH_CPU 0`) | shipped, verifier GREEN | 899,136 | 1,087,616 |
| **both-CPU (`BOTH_CPU 1`)** | **R2-07 GATE, FAILS** | **939,136** | **1,184,064 raw / 1,159,117 net** |

**CADENCE IS READ FROM `NDS_TICK_HUD_DRAW=0` (owner, 2026-08-14; `plan.md` §6).** Measured on the
gate arm with no gdb stub attached: **`DRAW 0` VBI 2:1427 3:162 4:10 6:1, max 6, two-VBlank 89.1%**
vs `DRAW 1` 2:1359 3:229 4:9 5:1 6:1 10:1, max 10, **84.9%**. The HUD draw burst is worth **4.2
cadence points**, so the deficit against ≥95% is REAL and is not the instrument. Label every cadence
figure with its `NDS_TICK_HUD_DRAW`. That `DRAW 0` arm carries `NDS_TASK37_PROFILE=1`; a clean one
is one build away.

**LEDGER INDEX (2026-08-13, superseded as a bank, kept as a method)** — `…/2026-08-13_c-ledger-index/LEDGER_INDEX.md`. **The shield attach path paid for a SEARCH, not work**: each attach hit `ndsAObjEvent32FindNormalized`, a linear scan of a 1,177-entry ledger at 8.05 tk/iteration — **5,123 tk/attach, ~88% scan**. An O(1) index took P95 1,250,368 → 1,210,944, P50 flat, `SINT` −23,936, 40 frames 3→2, 12,667 oracle lookups 0 mismatches, A2 falsifier brackets control 2,752. The anim-joint fix's **+49,216 came back −39,424**, now +9,792.

**`analyze-tick-hud-excursion.ps1 -Ceilings` emits both arms' tables** (`…/2026-08-12_c130-fire-gate/LANES_BOTHCPU.md`); c125's ceilings are dead, and so is `SITR_NEXT_CUT.md`'s cut (`…/2026-08-13_sitr-aobj-layout/`). **`RESIDUE.md` §0 corrects all sizing. SLICE 50 SPENT §4 row 0 — leg A landed (195 calls/frame → 8 sweeps a match, `STG` −11,328 flat, viewport pixel-identical); legs B and C REFUTED, do not re-brief that row.** **B is blocked by RAM**: 828 B against the texture cache `_Static_assert`'s **72 B** of slack. **C does not exist** — slice 1 converted every per-corner writer; the residue is the shared `gl*` → `ndsRendererTask29Gl*` wrappers, which have stage callers, not an `#if`.
**Apparatus is 24,947 not 18,675** (third lane `sNdsEffectPacketArmed`, `#if NDS_TICK_HUD`, 6,272 on EVERY GX command), so the product-side gap is ≈62,300. **Owner package: `…/2026-08-13_c-residue/OWNER_DECISIONS.md`.** **RUNG 2
(quarter-rate particles) REFUTED, no build (`…/2026-08-13_c-particle-rate/`): `MISC` is a DRAW
residual** (`taskman_seam.c:5104`), so its 17,152 never priced the update half — which is `SRC`,
7,364 tk/fr, and pays **−7,493** quarter-rate (ALL particles, both halves, −33,818). **And it shares
ONE LCG with the level-3 AI**, so any cadence change diverges the match: **CHECK EVERY SUB-RATING
FOR `syUtilsRandFloat`.**

**A PROFILE CYCLE IS HALF A TICK.** `ticks/frame = cycles / (2 × regions)`; regions P50 2,240,838
cyc = 2.0001× two VBlanks, so `cycles/2` reproduces the gate arm's `ALL` histogram and every
`SITR`/`AObj`/`AnimValueQ`/`__aeabi_lmul` crumb halves and is gone (`RESIDUE.md` §0).
`analyze-symbol-line-profile.py` reads `regions` and prints its `basis`. Only `FTR`/`STG` are flat
where P95 lives (band min 296,320/171,520), so flat cuts there pay 1:1 and ADD.
**`…/2026-08-13_c-flagsweep/c123-pc-cycles.csv` caches that profile as 98,346 rows — a 79-second
one-pass replacement for the 2.6 GB scan. RANK THE INLINE ATTRIBUTION, NOT ONLY THE SYMBOL
CENSUS: the whole of `RESIDUE.md` §4 row 0 is invisible to a symbol ranking.**

**THE BIGGEST LEVER IS PLACEMENT.** Memory stall is **1,236,685,107 cycles, 33.8% of the match** ≈ 386,000 tk/frame — past `SINT`/`SHDT` by an order, and why slice 48's identical pair differed 94,976.

**SLICE 49 (reclaim dead ITCM) is REFUTED without a build — do not re-open it until the Task 37 port
group is understood.** `.itcm` is NOT full (30 of 82 residents never execute, 2,594 B idle), but the
census's 87,033,153 "in reach" stall cycles assume admitting ~3,118 B of mostly PORT functions, and
`NDS_TASK37_ITCM_LEAVES`' PORT bit is held on CORRECTNESS (owner: the enabled lab build misbehaves). **Eviction alone pays nothing.**

**Lane-sizing traps, now encoded in `-Ceilings`:** medians do not add (it invented a 110,336 lane in c122); `OTHR` CONTAINS `WAIT`; only `WORK-H` is spendable. A `-Ceilings` ceiling flattens a lane to its own MEDIAN, so it prices the EXCURSION only — not what deleting the lane pays (`FTR` 8,512 vs 311,744); `RESIDUE.md` §2 has both. **Profile with `NDS_TICK_HUD_DRAW=0` or you profile the instrument**; the GATE keeps `DRAW=1`, cadence comes from `DRAW=0`. Soak length is `NDS_R2_SOAK_MATCH_MINUTES`; `Makefile:382` forbids reporting a
both-CPU P95 as Boundary's. **Route to ATTRIBUTE, re-bank to BANK. Collision paid and is BANKED**
(slices 35/36/37, −10,752). Float in `mp*`/`ftMain*`/`ftComputer` is FROZEN; `gmcollision` was UNFROZEN
2026-08-13, but the LINKED ELF says only **37–52%** is reachable (`…_c-collision-seam/`, slice 52).

## What is dead, so nobody re-derives it

- **`SPRM` 13,056, `AUD` 13,824, `BG` 3,968 — closed by arithmetic 2026-08-13** on a WHOLE-MATCH MEAN; **`SPRM`'s excess on the 80 frames that SET P95 is +49,377 (25.8x presence)**, so re-price that one on the marginal frames before re-closing it. `SCPU` needs −32.1% and reads 896 on the rank-80 frame. **All 169 `?=` flags audited — ZERO unshipped wins (`…/2026-08-13_c-flagsweep/FLAG_SWEEP.md`).**
- **THE P95 EXCESS IS ~90% SIMULATION, AND IT REPRODUCES ON TWO DIFFERENT MATCHES** (`…/2026-08-14_runtime2-p95-closure/{MARGINAL_OWNERS,GATE_ARM_OWNERS}.md`; `scripts/census-tick-hud-p95-set.py` re-derives it in one command). On the 80 frames that literally SET P95 the excess over a 2-VBlank frame is **+508,993** (c147: +520,718), `SRC` **+456,480 = 89.7%**, **all inside `GCRA`** — `SRC−GCRA` = **−22** here, −68/−64 there: three populations, three zeroes. Nesting `SRC ⊃ GCRA ⊃ {SINT ⊃ SCPU, SHDT, SPHD/SPHC, SCAT, SPRM}`; never add a parent to its child. **But the ranking BELOW `SRC` is match-specific — only `SITR` survives**: `SITR` +171,234 → **+188,907**, while `SHDT` 119,920 → 80,837, `SPHD` 112,833 → 75,236, `SCPU` +7,222 → **−8,669**, and **`SPHC` +62 → +52,780 (59.3x)**. Size nothing off one match's events. **`FTR` 1.03x, `STG` 1.00x, `MISC` 1.23x — the draw side is 7.1%,** confirmed independently by the v3 profile's draw closure at **8.4%**. **Cut 64,452 at the 80th-largest frame.**
- **THE FIRST v3 STALL CAPTURE ON THE GATE ARM EXISTS** (`…/v3-gate-arm/`, `build-c159-profile-bothcpu`, `BOTH_CPU 1`/`DRAW 0`, 1,601 regions, 3.62 GiB, residual −15; reduced CSVs `gate-p95-pc.csv` + `gate-band-pc.csv` are committed so **nobody re-scans it**). Whole match tk/fr: icache 354,678 · dcache 269,944 · halt 214,226 · issue 208,689 · wbuf 48,914 · intlk 43,122 · bus 36,738 · dma 8,450. **On the 80 P95 frames the excess is icache_fill +155,795 (40.0%) · dcache +96,800 (24.8%) · issue +94,029 (24.1%)** — still no single owner, and **no function exceeds 3.6%**. **`plan.md` §7's hot-footprint re-admission test FAILS on its own condition: the draw closure holds only 11.5% of the icache excess** (+17,944 of +155,795) while the fighter-proc closure holds 41.4%. **Soft float does NOT promote**: 38.0% of the family's caller-attributed 74,283 tk/fr is inside the fighter procs, **31.2% is draw side**, largest single caller 5,402. - **NATIVE BATTLE KERNEL SLICE 1, PHASES 1–4 DONE 2026-08-14, ZERO BUILDS** (`docs/architecture/RUNTIME2_NATIVE_BATTLE_KERNEL.md`; `…/2026-08-14_native-battle-kernel/BATTLEPACK_ANIMATION.md`). **The named mechanism is NOT the FAT read.** The animation path does **15** file loads all match on 13 frames, and deleting those 13 frames ENTIRELY moves the 80th-largest frame **9,874** against 64,452. **The lever is the acquisition path: 299 acquisitions, 95.0% cache HITS, on 62 of the 80 P95 frames vs 174 of 1,520 body frames — 6.8x presence.** A hit still copies the payload, re-registers, re-finalizes, re-normalizes AObj16, strips alias nodes, writes three status entries. Dose-response **+148,969** first acquisition, **+77,440** each after, **+645,225** a miss; modelled full deletion **−73,659** at rank-80 (upper-bound model, profile arm, **not banked**). **`get_fat`/`f_lseek` are majority BGM** — ≤38.8% of the P95-set `get_fat` is on an animation frame, so sizing off the full +15,058 over-predicts 2.6x. **Reachable set = all 301** (`ftdata.c`: 143/143 Mario + 158/158 Fox); the 87-entry warm list is 28.9% of it and observational. **Pack 651,928 B vs ~511,904 proven RAM — DOES NOT FIT, RE-MEASURED 2026-08-14 AND IT STILL DOES NOT.** **Items-off is PROVEN from the LINKED ELF** (every item-status setter is a 2-byte `bx lr` stub; no item spawner exists) and priced by RE-PACKING at **553,696 (−98,232), mismatch 0**; the 14-clip matchup lead reaches 528,624 but its handlers are merely ABSENT, which is not proof. **Route 1 is 12,608–25,216 B, not 140,024** — the per-status malloc is `gFTManagerFigatreeHeapSize`, the largest single anim FILE (max payload 6,224), 2 heaps a match plus 2 on Sudden Death; **the 262,144 arena was already counted and HANDOFF's 192,240 is its bump high-water INSIDE that reservation, not a second pool.** **THE THREE POOL TERMS ARE NOT ADDITIVE**: the arena is one `calloc` of `NDS_TASKMAN_ARENA_SIZE 0x150000`, so `.rodata` draws on 211,936 ALONE and an arena pack on 299,968 ALONE; 511,904 needs the constant cut, the 0x130000 search floor lowered AND the Task 36 replay guard retaught (`nds_renderer.c:5734-5739`). **PHASE 2 IS SPENT AND MEASURED 2026-08-15 (+146,560; see the RAM section) — but "fits the FULL pack by 6,536" is the COMBINED pool only, and the 3-file coupling still stands.** More-compact-representation is REFUTED as a lossless lever — the stream is u16 headers and **s16** targets, no f32 to narrow. **PHASE 5's PREMISE IS HALF THE REASON**: the copy also exists because `decomp/…/ftmain.c:4623-4624` DISCARDS the return and hardcodes `fp->figatree = fp->figatree_heap`, and `battleship_ftmain.c` `#include`s that body; **LANDED 2026-08-15 (`6e93def43cd`) and provably inert — exactly ONE symbol changes size in the whole binary (`battleship_ftMainSetStatus` −8 B, the post-call heap reload deleted), and the callee returns exactly `heap` on every path.** FAT fallback still forbidden. **Phase 4 equivalence mismatch = 0** (297 clips / 5,629 scripts / 77,959 commands / 71,500 states / 5,629 callbacks), two falsifiers prove it can fail. **The counter build the brief asked for was not needed**: `scripts/census-profile-pc-per-region.py` reads per-presented-frame call counts for any symbol out of a v3 capture, so "does it concentrate on the P95 frames?" no longer costs a build. Still unmeasured: BYTES on the hit path — one `gNdsR2AnimCacheHitBytes` at `reloc_backend_assets.c:7207`. `gNdsRelocAssetOpenFailCount` is conflated across 11 writers and should be split.
- **THE 160-FRAME CADENCE SET IS 64% INSTRUMENT.** 102 of the 160 are ALREADY under the 1,116,096 cadence boundary in `WORK-H` and 98 of those carry the HUD draw burst; only **58** are `WORK-H`-bound, needing a mean **43,916** (94,848 is the WORST frame, not the set). A burst frame presents in 2 VBlanks 3 times in 1,360. **The 123,773 write_buffer/interlock/bus pool reproduces to the tick and is NOT a lane** — largest holder 9.1%, and every top holder is already owned (GX FIFO, fighter draw, texture upload, or `tickGetCount`/`cpuGetTiming`, 13,406 tk/fr of apparatus). Only `memset` 10,570 + `ndsRendererSyncTextureTile` 3,945 are pool-shaped, and they sum under the floor.
- **NEWLIB FORMATTED I/O IS REACHABLE AT `NDS_TICK_HUD=0` — the "instrument-only" reason was wrong**, the verdict is not: `--gc-sections` keeps the family and `ndsRelocAssetFindEntry`'s NitroFS path builder calls `sniprintf` on every animation lookup. Bound **≤769 tk/frame**, 20x under floor. Closed on cost, not on reachability.
- **`FTR` — −93,612 landed (c116); its "0/80, NOT a P95 lever" verdict is BOUNDARY-arm and the gate
  arm's 8,512 is the EXCURSION ceiling, not what deletion pays (311,744).** DS-native AOT geometry
  ships (`NDS_TASK56_FIGHTER_PRIMITIVES ?= 2`) **SHIPPED BROKEN** — 35.6% of the fighter backfacing
  with Boundary green: **a passing verifier is not visual verification.**
- **Effect DObj submits** — Boundary-only. **Projectiles** · **texture thrash** · **`Find`** ·
  **`Material`** · force-load seam. **`MISC` is the DRAW residual, not "particles"**; particles are
  FLAT. **AOT animation bake** (32): SIZE dead. **In-match FGM I/O + `FindPlanned`**: closed
  2026-08-13, `SHDT` block above.
- **Animation playback ARITHMETIC** (slices 34, 41): idle-joint skip (33), lazy track table (31), AObj walk and dispatch all under the floor. **Slice 41 spent the last lever**: 30 Hz poses cost **+7,040** *and* diverged the match (damage 130/51 vs 33/65). **Don't blanket-convert `ndsBaseGcPlayMObjMatAnim`** — 5 tracks pack 0xRRGGBBAA in f32. STRUCTURAL LAYOUT cuts closed 2026-08-13; call count is the lever.
- **THE CALL FRAME IS CLOSED 2026-08-14. Its 64,863-tick ceiling does NOT convert: best package 10,544 cyc = 5,272 ticks, 3.0x short of gate** (`…/2026-08-14_call-frame-slice/CALL_FRAME_SLICE.md`, `scripts/census-frame-candidates.py`). **A split recovers the REGISTER DELTA, not the frame** — fitted `push(N)≈1.6+1.2N`, `pop+pc(N)≈5.0+1.6N`, so **~9.3 cyc/call survive any split** (pc-load flush). `ftGetStruct` corrected to −3,770 cyc, not −7,141. **`ndsR2AnimValueQ` (6,919, largest) is BLOCKED** — its `noinline,target("arm")` is measured: Thumb has no SMULL, that arm cost **WORK-H P50 +25,472**. **202 diagnostic-SHAPED names = 98,871 cyc hold only 7,849 cyc of real diagnostic**; `…RecordCapturedDisplay` is the stage hook, `ndsIFCommonRecordHUDState` drives the HUD (`nds_platform.c:2616-2760`), renderer `Record*` IS the RDP state machine. **Method: size against the LINKED ELF** — codegraph returned `#else` non-hwtri stub bodies and nearly bought a false 14,800-cyc lead. Still worth taking WHEN BUILDING ANYWAY: `ndsFighterDisplayContractCountFlags` 7,849 cyc = 3,925 ticks, no runtime reader; gate the traversal, keep globals `used`.
- **UNITS: 2 profile cycles = 1 tick.** The dcache census compared cycles against tick floors and overstated everything 2x; the DMA0CNT poll is **8,315 ticks, UNDER the ±8,544 floor**, not "at the bankable bar". **RETRACTED with it: "88.9% of data-load excess is cacheable" was the classifier FAILING OPEN** — an unresolved base became `cacheable`; fail-closed reads **cacheable 0.1%, unknown 91.7%**. Only 0.1% was ever PROVEN cacheable; that STOP stands, its basis does not.
- **THE v3 STALL ATTRIBUTOR IS ALREADY IN-REPO: `emulators/melonds-attributor/melonDS.exe`** (since 07-27, emits `profile-v3` with `icache_fill`). An earlier note said "never adopted" — WRONG, it checked `emulators/melonds/` only. Drive it with `run-task37-profile-census.ps1 -MelonDS emulators\melonds-attributor\melonDS.exe -Build <dir> -NoBuild -StartFrame/-Frames matching that build's config`. Source: `D:\Stuff\DevFolder\melonDS-Accurate` (`r2-stall-attributor`, `4a1abf61`).
- **CODE PLACEMENT CLOSED 2026-08-14 ON MEASURED TEMPORAL EVIDENCE** (`…/2026-08-14_icache-temporal/ICACHE_TEMPORAL.md`; nothing built). The earlier "33.6x union footprint" verdict was **WITHDRAWN** — a whole-match union does not govern cache behaviour, and "evicted between calls regardless" was asserted without measuring the interval. Geometry VERIFIED from `melonDS-Accurate/src/CP15_Constants.h`: 8 KB / 32 B / 4-way / **64 sets** / **round-robin** (melonDS names WAYS "sets"). **v3: `icache_fill` = 339,275 ticks/frame, 29.7% of match, 37.5% of non-idle, 1.87x `issue`** — 20x the target. **But CAPACITY, measured**: sweeping the cutoff so set population varies, sets that FIT the 4 ways refill at 1,252 fill/1k vs 1,233 for oversubscribed (top-256); 1,407 vs 1,297 (top-512). **Room in a set does not make its lines survive.** Phase 3 DID refute the old claim — 3 hot clusters fit and are scattered over 160–713 KB, hottest edge 413 KB apart sharing 10 sets — **but they hold 0.14% of conflict.** Model gate passed only after the FIRST falsifier scored −2.3%, beating the principled layouts (it padded by a line, spreading sets); rebuilt to align bases to the 2048 B period → +27.8%.
- **HOT FOOTPRINT IS THE LIVE LANE AND IT SIZES ABOVE THE BAR** (`…/2026-08-14_hot-footprint/HOT_FOOTPRINT.md`, census only). Of **288,352 B in the 9,011 lines the match pays to fetch**: live 213,040 (73.9%), **literal pool 5,780 (2.0%, NOT removable — Thumb-1 needs the constants)**, **cold code 42,892 (14.9%, the real lever)**, padding 26,640. **ALWAYS split the pool out before quoting "dead-in-line" — raw 26.1% reads as ~88,000 ticks and is a confound**; objdump shows pool words as instructions and no PC profile reports them. Ceiling 218,820 B needed = 6,839 vs 9,011 lines = **24.1% = ~81,800 ticks**; realistic third-to-half = **25,000–40,000 ticks**. **Two objects hold 42% of all fetch**: `scene_backend.o` (61,664 B fetched, 47.3% exec) + `nds_renderer.o` (55,008, 48.6%). Main text 914,634 B, only **43.4% in functions that execute at all**. `ndsR2AnimValueQ` spends **81.4% of its own cycles fetching itself**; `.text.hot`+`.text.hot.draw` = 9,844 B is already **1.2x the cache**.
- **NEXT EXPERIMENT: `-freorder-blocks-and-partition`** (cold blocks → `.text.unlikely`). THREE GATES FIRST: (1) changes codegen, so NOT "same objects" — Boundary must re-verify gameplay/collision/RNG; (2) `.text` will likely GROW while *fetched* text shrinks — right trade, but price it against the GObj-cap RAM threshold; (3) confirm `.text.unlikely` is non-empty in the map for `-mthumb` ARMv5TE and reaches those two objects — **if empty, STOP, no build spent**. Primary evidence = v3 `stall_icache_fill` delta, NOT WORK-H. **Closed cheaply: clone bloat is 168 B total.** **Not the fetch lever:** 66,488 B of never-executed objects survive `--gc-sections` — zero fill cost, a RAM question. **Chase:** `_vfiprintf_r`/`__ssvfiscanf_r` are in the hot fetched set — newlib formatted I/O running in a battle match.
- **The 20.12 kernels' ARITHMETIC (slice 42)** — sub-floor, non-additive. **Local-matrix memo dead twice.** **Flower rigid-mask +3,200, wrong sign.** **token→asset_id MEMO dead** (Task 74). **Six more lanes closed by MEASUREMENT**, numbers in `…/2026-08-1{1_c122,2_c123}-rebank/SLICE4{5,6,8}.md`: `ndsRelocFinalizeLoadedFile` as gate; anim-cache arena growth (Rejects 0); `OTHR` ceiling; **BGM sizing**; **every memo is healthy**.

## RAM — price a change before writing it

`check-boot-headroom.ps1 -Build <dir>` after every lab build. Highest `fake_heap_start` proven to
boot **`0x02294804`**, lowest to fail **`0x02294b24`**. **Text counts as much as bss**; a failing
arm reads as a hung emulator. `gSYTaskmanGeneralHeap` free-min **72,188**, floor 32,768.
**2026-08-14 RAM:** HW wallpaper decode cache **153,600 -> 135,000 B**; `.main.bss -18,592 B`.
**2026-08-15 RAM — PHASE 2 SPENT, +146,560 B MEASURED** (`8cfbc2eaa2b`, `…/2026-08-15_framebuffer-collapse/PHASE2_FRAMEBUFFER.md`): `gSYFramebufferSets` `[2][230][320]` -> `[1][231][320]`, 294,400 -> 147,840 B. Same build dir both arms: **bss 1,453,544 -> 1,306,984 (-146,560 exact)**, text/data flat, `fake_heap_start 0x02269ee4 -> 0x02246264`, tick-HUD headroom **174,368 -> 320,928**. Reader set from the **LINKED ELF** (one reader: the Results photo wipe; one writer: `scmanager`'s `sizeof`-bounded clear); span re-derived from the wipe's own literals `+0x23f14`/`-640`/220 rows -> `base+7,060..+147,819`. Boundary GREEN. **Results tic 160 byte-identical across arms; the WIPE ITSELF is uncaptured and NEEDS THE OWNER'S EYE.**
**STATE THE ARM WITH THE HEADROOM.** Post-Phase-2, MEASURED at HEAD: proof/published-config arm **355,264** (`0x0223dc44`), tick-HUD arm **320,928** (`0x02246264`); the published root ROM still reads `0x02260724` = 213,216 because it has not been rebuilt since Phase 2, so its "359,776" is a PREDICTION. **The tick-HUD arm binds** — every gate figure is taken on it and it is 34,336 B larger. **2026-08-15 — `.rodata` COSTS THE ARENA TOO, MEASURED** (`…/2026-08-15_battlepack-pool/BATTLEPACK_POOL.md`). **THE BOOT LADDER IS NECESSARY AND NOT SUFFICIENT: read `gNdsTaskmanArenaChosenSize` after ANY static growth.** A `.incbin` pack of **+288,992 B** pushed the arena `0x150000 -> 0x140000`, `gNdsTaskmanArenaAllocFailCount 16` — *with 66,784 B of PROVEN boot headroom still left*. The arena is calloc'd from the libnds heap `fake_heap_start` bounds, so static image and arena size are the SAME bytes; against the match's measured 1,304,068 B taskman use a 0x140000 arena projects a general-heap low-water of **~6,652 vs the 32,768 floor**. **`.incbin` IS GONE, 2026-08-15** (`…/2026-08-15_battlepack-resident/`): the Fox blob is a NitroFS payload streamed into the arena at scene setup in 18×16 KB chunks, replacing the 262,144 B anim cache — **proven headroom 66,816 → 354,208 against a 355,104 flag-0 baseline, i.e. +896 B of image**, arena 0x150000, `AllocFailCount` 0. **ORDERING WAS NOT ENOUGH**: fighter setup stores 3,728 B into the arena *before* the first scene update, so a loader that merely allocated first was refused by **848 bytes** (`ArenaOverflowLastSize 287936`, `LastUsed 3728`, `Hits 0`) — the blob now owns `[0, RESERVE)` of every arena generation, carved at reservation, with `NDS_R2_BATTLEPACK_BLOB_BYTES` generated from the blob so it cannot drift. **The un-packed fighter loses the cache: `gNdsR2AnimCacheRejects` 0 → 126.** Per-fighter split costs **136 B** of dedup. **PHASE 8 PRICED IT AND THE RAM DOES NOT CLOSE (2026-08-15).** Restoring that cache beside the blob needs `287,936 + 262,144 = 550,080` reserve vs today's 292,032 = **+258,048 B of arena**; both fighters resident needs ~559,632 vs ~301,564 = **~258,068** — the same number. Phase 2's 146,560 is SPENT, Phase 1 is 21,600 unspent, Phase 4 is **≤80,096 of unsized candidates**, Phase 3's ~150 KB is **REFUTED**, Phase 5 unstarted: **optimistic ceiling 248,256, short of even the ONE-fighter case. No named phase or combination covers it** — the only `.bss` objects big enough are `sNdsAudioFgmCache` (proven *under*-sized) and `sNdsRelocSceneFileBuffer` ("already optimized once"). **The cheapest unpriced lever is the pack itself: the Fox blob is 287,904 B against the 262,144 B it evicts, 1.098× its own displacement.** A pack smaller than what it displaces closes this without buying a byte; the items-off re-pack (553,696 B both fighters, mismatch 0) is already proven. **Also: the pack was shipping DISK bit order** — the host probe's `normalize()` is pipeline 4a-4b only and 4c (`ndsRelocAObj16EncodeForNativeBitfields`) never ran, because the direct path skips `ndsRelocFinalizeLoadedFile`; a ROM data abort found it, phase 4's "mismatch 0" did not, because both sides used the same disk-order decoder. Fixed, `decode_script(native=)` added, falsifier: the blob decoded in disk order raises on **75 of 2,713** scripts, native **0**.
## Landed slices and the lanes they leave

**SLICE 43 — WITHDRAWN 2026-08-11, RE-MEASURED 2026-08-15, STILL `?= 0`.** Leak fixed; re-measure
`−17,152` rank-80, stack flat, no low-polygon frame (lead above); **the one remaining acceptance item
is the pixel pair, NOT taken.** Only `NDS_R2_FIGHTER_GX_COMPOSE_LAB=1` re-enables it, tickhud/proof
only. Leak note `nds_platform.c:3260-3294`, not `:3197`; `|| NDS_TICK_HUD` pinned by `check-gbi-decode-fixtures.ps1:2247`.
**SLICE 46 KEPT — 1,213,440 → 1,196,224** (`…/SLICE46.md`):
warm preload covered 57 of the 87 used ids; the measured 87, 4 per scene update, take **misses 32 →
2** and the arena 257,200 → 192,240 (it SHRINKS).

**SLICE 48 KEPT — read its SIZE, not its bank (`…/SLICE48.md`).** The FAT lane is **BGM**; **`AUD`
at 0.2% does NOT clear it** (a bucket brackets only its own thread). Shipped: worker created at
`MAIN_THREAD_PRIO + 1`, switched to `- 1` once playing; **deprioritizing during the MATCH was
REFUTED** (+8,064 wrong way). **SLICE 45 KEPT — 1,225,280 → 1,213,440** (`…/SLICE45.md`): resolves
16,002 → 1,143, **−12,160**. **The fighter LOCAL matrix build is NOT a P95 lane — refuted c122**;
the local-matrix memo is **DO-NOT-RETRY, killed twice**. **`SINT` is the fighter INTERRUPT proc with
`SCPU` nested, not an animation bucket** — mis-attributed an A/B in c119. **The zero-copy force-load
seam is REOPENED 2026-08-15:** `ftmain.c:4623` now takes the returned pointer (`6e93def43cd`).

**`SHDT` IS CLOSED — bar 47,424 tk/fr, not −26.6%: the band is the transform chain, four dirty flags
so nothing to memoise, band-only cuts saturate at 78,016, fixed point only**
(`…/2026-08-13_shdt-{broadphase,band-owner}/`, RESIDUE §4 row 1). **Its file-I/O co-fire is closed
too** (`…/2026-08-13_c-band-io/`): the **SOUND-EFFECT load, not the animation one** (anim prices **+0**).
**FGM IS CLOSED BY COUNTED MISSES, not inference (`…/2026-08-13_c-collision-stack/STACK.md`): 188 plays, 38 hits, 150 misses, 143 evicting resident data, ≤5 of 8 slots ever pinned. The victim rule's strict `<` made it a ONE-slot cache per size class; fixing that bought +9 hits = −764 P95-equivalent and REVERTED on its own falsifier. Working set 59 cues / 575,760 B vs 204,800, so a repartition extrapolates to ~−2,300 and TRADES class eligibility (dropped sounds). `FindPlanned` is REFUTED too — 1,188 entries a match, 13 tk/fr not 665; that profile PC range is `FindNormalized` inlined at a second site.**
A GATE LANE IS A MASK FOR THE PROFILE; a MECHANISM NEEDS NO MASK — sum over the region axis
(`analyze-io-lane-series.py`), worst-case-pairing first.

**Do not bring a micro-fix** — R2-06 E11: a load-frame-only ~8,000 cannot be banked. Clear ~16,000
in one change, or **use the `.data` route on ONE binary** (only if the change cannot alter gameplay
state). **Every change needs an engagement counter on BOTH sides**; slices 45, 46 and 48 were all
found by READING counters the code already kept, on the gate arm, for the first time. `.text.hot`
is closed both directions.
**R2-07 STRESS GATE PASSED — `…/2026-08-13_c-stress/STRESS_GATE.md`.** 3 successive matches, 5
entries, **Sudden Death UNFORCED** and played to a KO, `NO-FREEZE`, risk counters clean; the
five-minute match ran at 98.7% coverage for `WORK-H` 929,344 / **1,205,760** — **length does not
accumulate cost**. **BOTH ITS ANOMALIES ARE ATTRIBUTED, no build spent
(`…/2026-08-13_c-anim-anomalies/ANOMALIES.md`):**
- **Runaway — FIXED 2026-08-13 (`…/2026-08-13_c-animjoint-fix/ANIMJOINT_FIX.md`). There was no
  missing CLEAR; there was a missing SET.** `lbCommonAddDObjAnimJointAll` (`lb/lbcommon.c:785`) was
  an **empty stub** (`bx lr`), so `ftCommonGuardInitJoints` set `is_anim_joint` while every joint
  still held the GuardOn figatree. Five-minute arm: Figatree misreads **144 → 0** (it was 144 of
  144), runaway **50 → 0**; the runaway counter saw only 2/3 of the class — **48 of 144 decoded to
  a legal opcode and were silent**. **PRICE +49,216 P95** (bank above). Never loosen the bound.
- **AObj cliff = CAPACITY, not a leak — FIXED.** Four zero-growth stops against reuse firing 16-19/stop kills the leak theory; the shipping 1-minute arm already stood at **889/1,024**, and a LEDGER cannot be evicted (the repack has no spare bit) so capacity is the lever: **`NDS_AOBJ_EVENT32_NORMALIZED_MAX` 1024 → 2048**, +8,192 B bss, headroom **167,936**. **Corpus is 1,598 of 2,048 post-fix — 450 spare, 1.28x, NOT the 1,029/2x the source comment claimed (corrected 2026-08-13).**
**START PAUSES THE MATCH** and the old whole-window freeze hash could not see it — the watch hashes the TOP band and `-PressStartOnResults` presses only on a detected Results screen.
**WHISPY LOW-FPS FACE FIXED / OWNER-CONFIRMED 2026-08-14:** Slice 44 passed `allow_stale=TRUE` to dynamic stage bindings, freezing Whispy's world matrix for up to 8 presented frames despite 30 Hz DObj motion. Dynamic bindings now validate every frame while unchanged source keys still reuse. Control `m11` stayed **3872** through live scale 0.948/0.605/0.104/0.104/0.606/0.948/1.0; candidate tracks **3872/2464/416/416/2480/3872/4096**. Evidence: `artifacts/verification/2026-08-13_whispy-slice44-fix/WHISPY_FIX_EVIDENCE.md`.
## Measurement rules that change your FIRST action — board owns the rest

- **The sampler is bit-deterministic — never repeat a run.** Same ROM twice gives byte-identical
  buckets, so ANY cross-build delta is placement. `-Samples 1600 -RingDump`. A duplicate frame
  LABEL at a ring seam is warned; IDENTICAL payload is a stale read and always fatal, as is one
  away from a seam. **THE PLACEMENT FLOOR IS ~17,000 P95, NOT ±5,376** — 2026-08-13, three near-identical arms (`…_c-collision-stack`): the falsifier, carrying the candidate's bss and 16 fewer bytes of text, beat the control by **16,832**. Under ~17,000 a two-build comparison measures the linker; only a flag falsifier means anything. **Compare ELF SECTIONS, not the `.nds`** — NitroFS packs directory entries nondeterministically, so identical source can give 14 differing ROM bytes with `.text`/`.data`/`.rodata` identical.
- **Judge on `WORK-H`**; buckets locate, they never decide (floor ≥8,544). **`ALL` is
  VBlank-quantized** — it hid a +52,928 once. **1.85 cycles of `FTR` mean per byte of added ARM
  text.** **A bucket only sees its OWN thread** (slice 48).
- **A census row in tk/fr sizes a P50 lever, NOT a P95 one.** Slices 36/37 had equal mean self
  cost and P95 wins **2.45x apart**. **Presence is the tell** — and a lane that is BIMODAL at the
  percentile returns less than its mean (`RESIDUE.md` §1).
- **A route A/B is valid only for a change that cannot alter gameplay state
  (slice 41) AND only if the poke lands before the value is READ.** `-SetGlobals`
  fires at the first frame-complete marker; record what was actually applied or
  the control is the candidate relabelled (slice 48 got 1,102,208 on both arms).
- **Per-line/per-PC attribution BEFORE designing — no build, and it routinely names a different
  lever than the source reads like.** Slice 44's guard looked like a compare but four cold `ldr`s
  were 39%; 85.5% of `ndsAObjEvent32NormalizeScript` is two pointer scans; the whole of RESIDUE §4
  row 0 is inlined helpers a symbol census cannot see.
- **An arm that cannot produce the event reads 0 either way** — check the control differs first.
  **And a zero one level DOWNSTREAM of a rejected request reads exactly like a dead lane**: row
  2's four zeroes were correct readings of a request killed by a NULL script pointer upstream,
  and each was read as "nothing asks for this".

## Restart surface — parked items live on the board's **Parked** list

**PUSH IS UNBLOCKED — `a4100b7`, 2026-08-13**, the owner's "scrub the 16" (`OWNER_DECISIONS.md` §9):
16 Cargo blobs under `decomp/BattleShip-main/decomp/tools/` baked the build machine's user directory.
Untracked + gitignored, byte-identical on disk; they **stay in pushed HISTORY — the owner accepted.**
Scan is `git grep -l -i -e <owner-given-name> HEAD`, now **17 → 1**: the survivor, sm64's IDO
`usr/lib/copt`, is a FALSE POSITIVE. `AGENTS.md` still miscalls `/decomp/` gitignored — 26,260 tracked.

`AGENTS.md` owns the start-of-cycle commands; `docs/P1_EXECUTION_BOARD.md` is the only dynamic
queue; `docs/BUGS.md` carries the owner's verdicts — preserve their wording. A clean checkout builds
through `build.ps1`, not bare `make`: four of six `.inc` are gitignored and **`build.ps1`'s
generator is not run by `make`**. `make p1-tick` builds the measuring ROM, `make p1` the published
pair. Never pass `-j`, never override `MAKEFLAGS`, one build at a time, never build a published
target name for lab work. Preserve mode 163, renderer mode 9, mip 0, static textures, source
countdown, Dream Land water frame 0, Task 16 `1/1/1`. Never edit `decomp/`. Run
`New-Smash64DSSnapshot.ps1` last.
