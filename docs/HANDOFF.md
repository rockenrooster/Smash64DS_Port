# Handoff
Current: 2026-08-16 - **`SITR` IS A CALL-COUNT EVENT, NOT A COST: A STATUS TRANSITION RE-ATTACHES 22 ANIMATION JOINTS AND THE PER-JOINT PIPELINE RUNS AGAIN IN THE SAME FRAME - 288 FRAMES, 72,768 AT RANK-80; AND THE `frsub` EVICTION DOES NOT EXIST** (`artifacts/performance/2026-08-16_sitr-excursion/SITR_EXCURSION.md` + `.../2026-08-16_itcm-frsub/ITCM_FRSUB.md`; **1 lab build `build-c221-sitrprof`, 1 v3 capture, 2 counter runs on the existing basis ROM, 1 Boundary**; **0 production source edits, 0 defaults flipped, nothing published, nothing re-banked, both root ROMs SHA-256 IDENTICAL `54C07FAC…` / `6C939434…` and not rebuilt - mtimes 2026-08-09 and 2026-08-15**). **THE LEVEL IS UNMOVED AT +65,297** on `build-c220-camship` (rank-80 1,210,624 raw / 1,185,677 net, band 41-120 1,218,356), and `overgate.py` **re-derives it from the basis rows with the leaf closure exact on all 1,600 rows** - 0 violations, 0 negative leaves. **ITEM B (`SITR`) - THE MECHANISM IS NAMED AND THE LEVER IS SIZED.** The inherited cluster figures restate on the current basis: `IO_AUDIT.md` §5's 27 frames / 231,264 / -51,200 were `c219`; on `c220` it is **25 frames, median own excess 227,968, median `SITR` 332,288 against a run median of 104,320 = 3.19x, exact re-rank 45,056 -> level +20,241**. **Quote 3.19x for `SITR`, not 57x** - 57.69x is `SHDT`'s, whose run median is 4,608. **The per-PC census is the FIRST ever taken in the SHIPPING configuration** (`build-c221-sitrprof`: its `nds_build_config.h` differs from the basis in **four lines**, all of them the profiler or the git string - `GX_COMPOSE 0`, `FTANIM_TRACK 0`, `CAMERA_FIXED 1`; every prior census, c200/c191/c192, was `GX_COMPOSE=1` and/or `FTANIM_TRACK=1`). **Entry-PC call rates, per frame, on the 288 attach/force-load frames against the other 1,313: the bracket's own root `ftMainProcUpdateInterrupt` is 4.00 vs 3.87 = 1.03x**, so nothing here is "the frame ran more simulation" - but `battleship_ftMainSetStatus` **1.22 vs 0.00**, `lbCommonAddFighterPartsFigatree` **1.22 vs 0.00**, `ndsRelocAssetIDForToken` **2.61 vs 0.00**, `gcAddDObjAnimJoint` **22.06 vs 0.35 = 62.4x**, and the attach then re-runs the whole per-joint pipeline inside the same presented frame: parse **100.83 vs 63.69 = 1.58x**, `ndsR2AnimValueQ` **386.15 vs 242.05 = 1.60x**, `ndsR2AnimBuildTrackTable` **2.83x**, `ndsR2AnimTargetValue` **6.36x**, `ndsR2AnimAObjToQConvert` **12.68x**. `ndsRelocAssetIDForToken` is a ~110-branch linear `if`-chain plus two pointer scans over 143+158 animation ids at **1,578 ticks per call**, and its own source header already says *"remove this work … or move it off the gameplay frame entirely"*. **THE LEVER: 288 of 1,600 frames (18%) carry an attach or a force-load, hold 81.2% of the run's whole `SITR` excess, and on them ONLY `SITR` moves - 1.77x against `SCPU` 0.87x, `FTR` 1.00x, `STG` 1.00x, `SHDT` 1.01x, `GCRARES` 1.03x, `SPHD` 1.08x. Exact re-rank of clearing it: 1,210,624 -> 1,137,856 = 72,768 moved, level +65,297 -> -7,471, i.e. 1.11x the entire remaining requirement.** Shape per event frame: **ANIM parse +28,094, ANIM evaluate +26,813, ATTACH chain +23,801**, card I/O +15,576, memory movers +14,319, soft float +6,028. Three candidate routes, none built: the token resolver (**+4,118 tk/fr, pure lookup, cannot change behaviour** - smallest and most self-contained), the per-joint quantisation (+6,246), and the re-attach itself (~half of +54,907, **a gameplay question BattleShip source must answer first - NOT proposed**). **REFUTED: "the force-load is the owner"** - the attach (r=+0.623) and the *stepped* parse (r=+0.650) outrank the force-load (r=+0.487) and the card read (r=+0.549), **10 of the 25 cluster frames carry no force-load**, and the implied per-load cost still falls with count (+122,931/+96,777/+68,794), reproducing `IO_AUDIT.md` §4 on a new basis. **RULED OUT, each measured**: the 2^22 artifact (2 corrected samples, frames 1464/1849, neither a cluster frame), the HUD refresh (r=+0.058; the 114 HUD frames read `SITR` median 104,384 against a run median of 104,320), the draw side (`FTR` 1.00x), and extra logic ticks (root entry 1.03x). **Three DEAD counters, named so nobody spends a run on them**: `gNdsFTComputerStatusChangeCount` and `gNdsR2FtAnimRecipMisses` read 0 for the whole run, and `gNdsFighterStructStatusSetCount` is **never incremented anywhere in the tree**. **The alignment was re-measured, not inherited**: `region = frame - 439`, with the 7 card-read frames at median profile rank **12** of 1,601 there and 315-699 at every other offset in 434..444, `r`=+0.694 vs +0.342 at 438; those 7 frames are **456, 830, 1015, 1186, 1625, 1655, 1886**, the identical list `IO_AUDIT.md` measured on `c219`, so the load events belong to the match and not the binary. **ITEM A - THE 456-BYTE `frsub` EVICTION IS NOT AVAILABLE, AND THE UNREACHABILITY PROOF IS NOT WHAT FAILED.** `NDS_TASK9_FLOAT_MAIN_MEMBERS` operates on `_arm_addsubsf3.o`, which is **ONE 0x2ac = 684-byte `.itcm` input section**: 456 B dead (0 executing PCs) followed by **228 B LIVE - 42 executing PCs, 2,396.6 instr/frame whole match, 3,544.7 tk/fr on the marginal-80, from zero-wait ITCM today**. A linker cannot split an input section, so **684 bytes move or none do**; `__aeabi_ui2f` has 99 call sites in 21 functions and the shipping-config census reads `__aeabi_l2f` **3,016,910 cycles** against `__aeabi_frsub` 0. **NOT TAKEN, and it should not be by this mechanism.** **ITEM B (bind placement) NOT BUILT**: ranking every `.main` symbol of 16-800 B by the metric `ANIM_ITCM.md` §6 itself used puts `ndsRendererHardwareBindTextureName` (14.19 tk/fr per byte) **SEVENTH**, behind `__syscall_getreent` 39.20, `DynamicArrayGet` 39.06, **`ftGetStruct` 32.59**, `ndsStageCollisionLoopGeometryReady` 27.62, `__aeabi_lmul` 26.27, `get_fat.isra.0` 24.33 and `ndsR2AnimBuildTrackTable` 22.52 - and `v3-c221`'s own census §D agrees from the campaign's own tooling. **THE FREE-SPACE ROUTE THAT IS ACTUALLY AVAILABLE, with its premise now CLOSED in the shipping configuration**: `ndsRendererNativeEmitDenseRawRun` 256 + `ndsRendererNativeApplyStateSpan` 192 + the two production raw-run emitters 240 = **688 B executing ZERO instructions**, plus `ndsFTParamsInvalidateFighterParts` 54; one-line `NDS_TASK82_ITCM_CODE` removal each, none pinned by `check-renderer-itcm-placement.ps1`; instrument free **220 -> 908 B**, proof ROM **2,572 -> 3,260**. **NOT TAKEN** - it needs its own build and Boundary. The census's own *"+1,858 B recoverable by eviction"* **over-counts by 456**, because it is symbol-level and includes the frsub blob. **BY-PRODUCT: the `GX_COMPOSE=0` per-PC census the board asked for now exists** (`v3-c221`, 1,600 frames), which is what a precise re-derivation of the stale draw-side soft-float split needs; not re-derived here. **OPEN**: the 4 cluster frames with no counter movement (530, 989, 991, 1302) are diagnosed only to "the simulation, not the draw" - their `SCPU` reads 2.25x, unexplained. **NEXT CYCLE INHERITS**: the attach-driven re-parse at 72,768 with three sized routes and the token resolver as the safe first cut; the 688 B ITCM route with its premise closed; `ftGetStruct` (92 B, 264 calls/marginal frame) as the top ITCM admission, needing a dispatcher-based route because it has hundreds of call sites; and `linker/nds_hot_text.ld:180-200`'s Task 94 prohibition on `gcPlayDObjAnimJoint`, whose evidence is a **128-frame cross-build** pair from before both the whole-match instrument and the same-binary route - recorded as re-testable by route only, **not re-opened**.**
Superseded 2026-08-16 - **THE OWNER ACCEPTED THE DRAW-SIDE PRECISION ARM: THE CAMERA SHIPS IN Q20.12, THE LAST TWO LEADING POLLS CAME OUT WITH IT, AND ITCM IS 44.3% COLD** (`artifacts/performance/2026-08-16_camera-ship/CAMERA_SHIP.md`, `artifacts/performance/2026-08-16_itcm-census/ITCM_CENSUS.md`; **1 lab build, 4 whole-match gate runs, Boundary PASSED with 0 `Exception:`**, no ROM published, both root ROMs SHA-256 IDENTICAL before and after `54C07FAC...` / `6C939434...`). **THE DECISION IS THE OWNER'S AND IS QUOTED VERBATIM:** *"I think camera fixed point is ok"*, after playing `build-c205-camtoggle` and saying of the picture *"otherwise it looks fine"*. `NDS_R2_CAMERA_FIXED ?= 0 -> 1`, still build-overridable. That closes `CAMERA_Q20_12.md` section 8's `BLOCKED(decision: draw-side precision)` and sets this project's FIRST draw-side pixel ceiling: **6.5350%** of the top screen at a simulation-locked tic against a same-build adjacent-present floor of **35.2217%**. **PRICE -6,336 tk/fr PAIRED MEDIAN, SAME BINARY, ZERO PLACEMENT FLOOR** (`build-c220-camship`, one poked `.data` word, `romSha256 5AE3F716...` on both arms): whole-run paired median **-6,336** (93.3% of frames win), marginal-80 **-7,264** (69/80), rank-80 **1,219,520 -> 1,210,624 = -8,896**, band 41-120 **-5,425**, P50 **-6,624**; complement exact to one quantum, `WORK-H -6,336 / WAIT +6,272 / ALL +0`; attribution `FTR -4,416` (99.4% of frames) + `STG -1,856` = -6,272, with `SRC`/`SINT`/`GCRA` FLAT at the median. **NEW BASIS `build-c220-camship`: rank-80 1,210,624 raw / 1,185,677 net, band 41-120 1,218,356. REQUIREMENT +64,977 -> +65,297.** That +320 is PLACEMENT, NOT A REGRESSION: this binary's OWN float arm reads **+74,193** on the same window, i.e. **+9,216 against `c219`'s float arm for identical float behaviour** - the >=14,080 cross-build floor re-measured on this pair. **THE ROUTE IS THE PRICE; THIS IS THE LEVEL.** **ALL SIXTEEN LEADING POLLS ARE NOW GONE FROM THE BINARY.** Isolated on a second same-binary route on the previous basis (flip alone **-3,776** paired median / -3,072 rank-80): difference of differences **-2,560** against **2,721 PREDICTED BEFORE THE RUN** from `HWROUTE.md` section 2's per-call figures (43.5 tk a divide, 23.0 a root) and this cycle's own engagement counts (56.2 divides + 12.0 roots a frame). Right sign, 94% of size, but it carries a residual cross-build term inside the ~5,440 paired-median floor - **CORROBORATION, NOT A PRICE**; what is banked is the -6,336 for the pair. **ENGAGEMENT, SHIPPED ARM: 8,152 fixed look-ats, 8,228 fixed perspectives, FLOAT CALLS ZERO** (a build default has no pre-poke frame); `Saturate`/`Degenerate`/`Rescale` **0 on all four arms**; route 0 is a live negative control that restores the float chain. `.main` **-32 B**, `.itcm` unchanged. **THE 2^22 FILTER RAN LIVE FOR THE FIRST TIME, ON ALL FOUR RUNS (5/2/2/1 corrected), AND IT IS LOAD-BEARING:** the artifact is DETERMINISTIC WITHIN AN ARM and lands on DIFFERENT FRAMES BETWEEN ARMS, so an uncorrected pair would have carried three spurious 4,194,304-tick rows on one side and none on the other. A fresh run of the previous build reproduced `ANIM_ITCM.md`'s `ship-rows.csv` **BYTE FOR BYTE ON ALL 1,600 FRAMES** once corrected offline - so the instrument is exactly deterministic, `-SetGlobals` costs nothing, and the offline correction is the sampler's. **`CAMERA_Q20_12.md` section 9.4's "a default flip must measure the Task 9 hash first" IS RETIRED:** `Makefile:1683` already settled that the hash asserts BIT-EXACTNESS while this change is authorized NON-bit-exact, so it can only ever report "differs"; it is `?= 0` and no verifier references it. **THE DRAW-SIDE SOFT-FLOAT SPLIT RE-BASES - DO NOT QUOTE THE OLD ONE:** shared 57,521 / sim-only 39,537 / sim+dispatch 37,662 / **draw 34,178** is stale on the DRAW ROW ONLY (the camera is `draw+dispatch` 100.0%); the chain's **11,504 tk/fr whole-match gross** is no longer called, leaving ~**22,521**. A precise re-derivation needs a NEW per-PC census - the existing one is `build-c200-trackprof-off` at `GX_COMPOSE=1`. **ITCM: EVERY RESIDENT CENSUSED, 0 BUILDS AND 0 EMULATOR RUNS.** 32,180 walked bytes, **14,242 (44.3%) never execute** in the 1,600-frame window - 2,448 B in 33 wholly-dead blocks (reproducing last cycle's 2,454 to within 6 B) and **11,010 B COLD INSIDE 52 LIVE BLOCKS**, 4.5x the whole-symbol pool. **`ndsRendererScanList`, 23.8% of the region and never censused before: 7,728 B resident, 46.0% executed, 4,112 B COLD**; four more renderer functions hold another 5,372, **9,484 B in five functions**, NONE of it reclaimable by evicting a symbol because all five are hot - only a source split reaches it. **`__aeabi_frsub`'s 456 B blob is UNREACHABLE BY CONSTRUCTION, not merely silent**: no branch into it from outside the blob, **no word in ANY allocatable PROGBITS section holds any of its three addresses**, zero executed instructions. **NOT TAKEN** - it buys zero ticks and needs its own build, Boundary and checker declaration. **THE FIXED-POINT PLACEMENT OBJECTION IS REAL BUT MIS-LOCATED, AND ON THE ANCHOR ROW IT RUNS TOWARD FIXED POINT:** from the linked ELF, `guMtxCatF` is at `0x020356e8` (**.main**) and `ndsRendererMtxMul20p12` at `0x01ff9dac` (**ITCM**) - the 5.14x prior's FIXED arm is the ITCM-resident one, so **5.14x OVERSTATES** what a fixed rewrite converts at. **EVERY OTHER PAIR HAS BOTH BODIES IN `.main`** (camera, sim leaf, collision); what is at `0x01ff...` is the soft-float LIBRARY LEAVES a float body calls, not the float body. The real asymmetry is one level down and is **already measured with a sign, twice, both against fixed** (`CAMERA_Q20_12.md` section 6: +3,032 B of inlined leaves turned -4,736 into +1,600; the collision ring's `K-ICACHE` null). **NO CLOSED LANE REOPENS.** **BUDGET: 220 B free on the INSTRUMENT, 2,572 B on the PROOF ROM; 676 / 3,028 after the frsub eviction.** A particle/quad fixed kernel needs 1,100-1,500 B, so **the warm re-test DOES NOT FIT on the instrument** - the only target that produces a tick number. **STRUCTURAL: a placement candidate can fit the ROM that SHIPS and still be unpriceable because it does not fit the ROM that MEASURES.** `ANIM_ITCM.md` section 6's Item B (268 B) sits exactly in that gap and FITS after the eviction. **HARNESS TRAP FIXED IN `docs/VERIFYING.md`:** `verify-all.ps1:167` writes each child checker's stdout with `[Console]::Out.Write`, straight to the console handle, so `*>&1 | Tee-Object` captures a THREE-LINE log and loses every per-checker counter. Use `cmd /c "pwsh -File scriptserify-all.ps1 -Profile Boundary > log 2>&1"`.
Superseded 2026-08-16 - **`SHDT` IS CLOSED: HIT DETECTION AT LITERALLY ZERO COST CLEARS +64,977 BY ONLY 2,543, AND THE 2^22 SAMPLER FILTER IS BANKED** (`artifacts/performance/2026-08-16_shdt-mechanism/SHDT_MECHANISM.md`; **0 lab builds, 0 emulator runs of my own, 1 harness change, 1 Boundary PASSED with 0 `Exception:` over an 18.9 MB log**, no default flipped, no ROM published, both root ROMs SHA-256 IDENTICAL before and after the Boundary rebuild `54C07FAC...` / `6C939434...`). **DO NOT BRIEF `SHDT` AS AN 88%-OF-THE-GAP LEVER AGAIN.** Exact re-rank of `build-c219-animitcm-ship`'s own 1,600 2^22-corrected rows (rank-80 1,210,304 raw / 1,185,357 net, band 41-120 mean 1,217,946, REQUIREMENT +64,977): delete `func_ovl2_800ED490` everywhere **-6,704** -> +58,273; the whole collision chain's excursion **-23,674** -> +41,303; the whole chain everywhere **-44,088** -> +20,889; `SHDT` down to its own P50 on every frame **-63,040** -> **+1,937**; **the WHOLE `SHDT` bucket deleted on every frame -67,520 -> -2,543**. **The lane at ZERO clears the requirement by 3.8% of it, and nothing short of zero clears it at all.** **MECHANISM, MEASURED NOT READ:** 57 of 1,600 frames run >=1 (attack collision x hurtbox) test, work premium **+268,255 tk/fr**; the victim's **~14** hurtbox joints get a world matrix, a 3x3 cofactor inverse and axis scales built ONCE EACH, then one rectangle test per pair. **THE COST IS PER FRAME, NOT PER PAIR** - natural experiment on `build-c191-sitr-profile-c185` (`COLLISION_FIXED 0`, `GX_COMPOSE 0`, `BOTH_CPU 1`): 44-pair frames run **exactly 2.00x** the `TestRectangle` of 22-pair frames while `SetInvertMatrix` 0.98x, `EDBA4` 0.98x, `ED490` 0.97x, `__aeabi_fadd` 1.04x, `__aeabi_fmul` 0.98x and `sqrtf` 0.98x stay FLAT and the frame gets **10,004 ticks CHEAPER**; OLS over the 57 gives **-440 tk/pair at R2=0.003** and the implied per-pair figure collapses 327,966 -> 5,229 across the count buckets (63x). That closes three routes at once: **the source's latch is ALREADY OPTIMAL** (nothing to memoise), the pair test is nearly free, and a per-hurtbox broad phase would remove the flat half (a second, independent refutation of slice 47). **OWNER:** the fighter collision transform chain, **+102,988 tk/fr = 38.4%** of the engaged premium (self 35,277 + leaf 67,712, `--attribute-leaves` over 8,746 `bl` sites). **THE ATTRIBUTION IS EXACT, CHECKED FOUR WAYS AGAINST THE SOURCE** - leaf calls per invocation land on `gmcollision.c`'s own operation counts at **+0.0%** every time: `func_ovl2_800ED490` **63.01** vs 36 `fmul` + 27 `fadd` = 63; `gmCollisionSetInvertMatrix` **61.02** vs 61; `gmCollisionGetWorldPosition` **18.00** vs 9 mul + 9 add = 18; `func_ovl2_800EDE5C` **18.01** vs 3 x (3 `fmul` + 2 `fadd` + 1 `sqrtf`) = 18 - and the last two divide by the JOINT count 13.96, not by their own call count, which re-proves the latch a second way. Soft float costs **32.7 ARM9 cycles per helper call**, 4,877 extra calls per engaged frame. **93.5% is genuine marginal work; the cache-displacement reading is 4.5%** (classified by exact call ratio, cycles summed afterwards so no symbol picks its class). Mask controls: random-88 negative **-19,189 with NO collision symbol**; the profile's own cost-ranked mask overlaps at **37/88** (chance 4.8) and reproduces the asset-load ranking instead. **THE ONE SUB-LANE WHOSE REFUTATION DOES NOT TRANSFER, AND IT IS STILL DECLINED:** `EXCHANGE.md`'s 2.68 exchange rate was driven by `__udivmoddi4` at 4.0 sixty-four-bit divides per entry, and **an affine multiply has no divide** - but it changes the joint matrices that decide hits (gameplay fidelity, rung 3), it is worth **6,704 = 0.103x** of the requirement even free, and `HWROUTE.md` S7 prices a byte in that chain at 3.61 tk/fr (the collision ring's 3,228 B cost 11,650 tk/fr of fetch alone = **11.8x** `ED490`'s entire 987 tk/fr whole-match cost). **Named, sized, NOT recommended - no owner decision asked for.** **BANKED - THE 2^22 FILTER:** `sample-tick-hud-buckets.ps1` now detects `ALL >= (1<<22)` and subtracts it from `ALL` and from every bucket at or above it, with a new TRAILING **`WRAPFIX`** CSV column (last, so no existing column moves; every consumer in `scripts/` reads by name, checked) and a warning naming each frame against the run's corrected `ALL` median. Verified on the basis's own five known-bad rows: all five reproduce `IO_AUDIT.md` S2 **EXACTLY**, frame 1357 corrects **6** buckets = `SCPU`+`SINT`+`GCRA`+`SRC`+`WORK`+`ALL` (the parent chain S2 Proof 3 names, and the bucket COUNT was not used to build the filter), 0 rows remain over threshold, and the largest clean `ALL` is 3,358,080 = a **25% margin** under it. **Trigger still unproven; magnitude, site and arithmetic are.** **RETRACTED, FOUR:** the board's "`SHDT` -57,152" deletes EVERY leaf's excess on those 33 frames - `SHDT` alone is **-50,752**; "mean 14,544 / P50 4,608 = 56x concentration" is **3.2x** (56.4 is cluster excess / run P50, a different quantity); my own subtraction of `armWaitForIrq` from a premium that already excluded it (caught in-cycle, before it reached a conclusion); and my own cache-displacement hypothesis (measured at 4.5%). **NEXT:** the **`SITR` cluster** (27 frames, -51,200) is now the largest un-diagnosed item on the board; the force-load per-PC account was NOT reached and cycle 106's 25.1% remains the only instruction-level view of it.
Superseded 2026-08-16 - **YES, THE CARD IS READ DURING THE MATCH - SEVEN TIMES, AND ALL SEVEN LAND IN THE TWELVE MOST EXPENSIVE FRAMES; THE INSTRUMENT ALSO INFLATES FIVE FRAMES BY EXACTLY 2^22** (`artifacts/performance/2026-08-16_match-io-audit/IO_AUDIT.md`; **0 builds, 0 source changes, 3 whole-match runs on the existing `build-c219-animitcm-ship` ROM, 1 Boundary PASSED with 0 `Exception:`**, no default flipped, no ROM published, root ROMs byte-unchanged `af968925...` / `d5f37f22...`). **ITEM A - THE CARD IS READ 7 TIMES INSIDE FRAMES 439-2038**: `gNdsRelocAssetPayloadReadCount` **101 -> 108**, `HeaderReadCount` 1,593 -> 1,600, `ShortReadCount` **0**, each read one animation-cache MISS (`Misses`/`Fills` 3 -> 10). **Cycle 105's mechanism is CLOSED, not improved** - arena `Overflows 0 / Rejects 0`, `UsedBytes` 415,984 of 451,776 reserved, `WarmFailed 0`, against that cycle's `142/142` at a full arena. Of the **349** in-match animation acquisitions **215 (61.6%) never reach the load path** (battle pack), 127 hit the RAM cache, **7 read the card**, on frames **456, 830, 1015, 1186, 1625, 1655, 1886** = ranks **4, 1, 7, 10, 12, 8, 9** of 1,600. Priced against a MATCHED control (a frame that force-loads and does no I/O): **+593,728 median / +536,988 mean**, 490,476 of it in `SITR`; **worth 12,736 at rank-80** by exact re-rank. `POSITION.md`'s "seven load frames | 9,863" is the same lane, correctly counted. **ITEM A2 - `CAMERA_Q20_12.md` SS3.2 IS WRONG AS STATED and is corrected in place**: the instrument is BIT-DETERMINISTIC (three separate emulator sessions on one ROM produced BYTE-IDENTICAL 1,600-row CSVs), and ranks 10/20 are NOT THE SAME FRAME in the two arms (rank-10 = control frame 1655 vs candidate frame 1975; the top-20 sets overlap 19/20 but only **9/20** share a rank), so +30,336/+20,224 are rank-permutation artifacts of a rank-by-rank estimator on a permuting tail. The banked **-4,736 is untouched**. Still unexplained and NOT this cycle's: the load-frame population moves **+-100,000-150,000** between two SAME-BINARY arms (830 -124,160, 1500 -142,016, 747 -103,360, 1975 +152,448, 1447 +129,280) against a whole-run paired median of -4,768. **FOUND IN PASSING, AND IT MOVES THE GATE: `cpuGetTiming()` REPORTS A SPAN EXACTLY 2^22 = 4,194,304 TICKS TOO LARGE, IN 8 OF THE LAST 13 WHOLE-MATCH RUNS.** Proven three ways - subtracting 2^22 lands `ALL` within **-192/+256/+192/+128** of the run's own median on four of five frames; the inflated `ALL` is **9.505 VBlanks** where every clean `ALL` is a whole number; and it lands in whichever span was open (`FTR`, `SCPU`, `SPHD`) with nothing else on the frame moving. Mechanism from the LINKED IMAGE: `cpuGetTiming` is `((u32)(tickGetCount()-start)) << 6` (`0x020be710`, which is also where the 64-tick granularity comes from) and `tickGetCount` (`0x020bfc2c`) credits **one** pending 16-bit timer overflow via `((timer ^ 0x8000) >> 15) & (IF >> 5)`; when that heuristic fails on a span's START read the span is one overflow long = 2^16 x 64 = **2^22**. **THE REQUIREMENT IS +64,977, NOT +71,569** - corrected, `build-c219-animitcm-ship` reads rank-80 **1,210,304 raw / 1,185,357 net**, band 41-120 = 1,323,072 / 1,300,480 / 1,266,240 / 1,225,152 / **1,210,304** / 1,188,480 / 1,166,144 / 1,154,944 / 1,140,608. Per-run correction **-1,536 ... -6,592** and it is ASYMMETRIC (it can only inflate), so it adds build-dependent noise on top of the >=14,080 placement floor. **NOTHING WAS REBANKED** - both numbers stand until the sampler filters it; **the filter is one condition, `ALL >= (1<<22)`, complete by construction because `ALL` contains every span, and it is NOT BUILT.** **`-PerFrameGlobals` EMITS A TORN BUCKET ROW**: `ALL` from iteration f, every other bucket from f+1, so **1,526 of 1,600 rows violate `ALL == WORK + WAIT`**, an identity the ring path satisfies on all 1,600; its counters are sound at ring offset **+1** (association +212,620 there vs -6,751 at 0 and -882 at -1). Cycle 105's spike probe used this path - a second reason not to quote its per-frame excess figures as prices. **ITEM B - THE FORCE-LOAD PATH IS LIVE AND IS THE LARGEST IDENTIFIED ITEM, BUT "~228,600 PER CACHE HIT" IS REFUTED AS A PRICE**: 134 force-loads on 116 frames, `WORK-H` mean lift **+193,677 / +179,545 / +274,542** for 1/2/3 loads on the frame = implied per-load **193,677 -> 89,773 -> 91,514**, which does not scale. The count-linear part is **`SITR`** (+87,805 / +193,086 / +205,572, i.e. **~68,500-96,500 per load**), so **cycle 105's `SINT` attribution is VINDICATED**; `SPHD`/`SHDT`/`SPRM` also jump on single-load frames and none of them scale, which is the state change rather than the load. Exact re-rank: deleting the premium on the 109 non-I/O force-load frames is **52,736 = 81% of +64,977**. **ITEM C - THE OVER-GATE SET IS FIVE POPULATIONS, NOT ONE**, and two independent methods agree (dominant-excess-owner labelling; Ward on excess-share vectors, best k=5, silhouette 0.458), with leaf closure asserted EXACT (`leafsum == WORK-H` on all 1,600 rows, no clamping): **`SHDT` 33** (median own excess 259,776, delete = **-57,152**), **`SITR` 27** (231,264, **-51,200**), `SPHD` 8 (215,136, -14,464), `SPRM` 7 (298,496, -12,736), `MISC` 5 (215,680, -6,016). They are DISJOINT - `SHDT` frames carry 14,112 of `SITR` excess (median 102,944) and `SITR` frames carry 2,176 of `SHDT` excess (median 4,608) - and 94.8% median of an over-gate frame's excess is inside `SRC`. **`SHDT` (live hitbox hit detection) HAS A RUN P50 OF 4,608 AND A MEAN OF 14,544 - A 56x CONCENTRATION - SO EVERY LANE SIZING DONE FROM A MEAN OR A MEDIAN HAS READ IT AS NOISE, YET IT OWNS 41% OF THE OVER-GATE FRAMES AND 88% OF THE REMAINING GAP.** Force-loads are enriched 6.0x over their 7.2% base rate in the over-gate set but ONLY in the `SITR` and `SPRM` clusters (23 of 34 frames); the largest cluster, `SHDT`, is NOT a force-load population - **two different levers, neither subsuming the other.** **NOT DONE**: no fix built, no counter added, no sampler filter, no default flipped, nothing rebanked, no per-PC profile of the force-load path (cycle 106's 25.1% is still the only instruction-level account), `build-c205-camtoggle` not rebuilt. **HARNESS NOTE, COST 20 MINUTES**: `verify-all.ps1`'s pre-flight throws *"Recursive make is unusable ... `NDS_RECURSIVE_MAKE=FAIL:127`"* when launched from a Git-Bash-spawned shell and passes under PowerShell - the message blames the toolchain and the toolchain is fine. Run the harnesses from PowerShell.
Superseded 2026-08-16 - **THE CAMPAIGN'S LARGEST FIDELITY-NEUTRAL ITEM WAS A PLACEMENT QUESTION, ITCM HELD 2,454 DEAD BYTES, AND `ndsR2AnimValueQ` NOW LIVES IN IT** (`artifacts/performance/2026-08-16_anim-itcm/ANIM_ITCM.md`; **2 lab builds `build-c218-animitcm` + `build-c219-animitcm-ship`, 3 whole-match runs, 1 Boundary PASSED**, 0 gameplay values changed, no fidelity default flipped, no ROM published, root ROMs byte-unchanged `54c07fac...` / `6c939434...`). **ITCM IS NOT FULL - IT WAS 2,454 BYTES DEAD, AND FINDING THAT COST NO BUILD**: the per-PC census that priced the kernel also prices every ITCM resident, and 33 non-overlapping blocks execute ZERO instructions across the whole 1,600-frame window (`itcm-census.txt`, `nm` joined to `c200-off-pc.csv`, both already on disk). **736 B taken**: `_arm_cmpsf2.o` + `_arm_unordsf2.o` (332 B - the port defines `__aeabi_fcmpeq/lt/le/ge/gt` itself and `Makefile:3845` renames libgcc's copies to `*_golden`, so those bodies have NO CALLER BY CONSTRUCTION, which the census confirms from the other side) and `ndsRendererHardwareGetLightShadeLut` (404 B - the miss-path LUT *builder*; `...FindLightShadeLut` is the lookup and stays). The other 1,718 B (exception vectors, cache maintenance, thread paths, the two raw-run emitters) were LEFT: gate-window silence is not proof a kernel path is unreachable. **READ A SYMBOL TABLE AS NON-OVERLAPPING BLOCKS**: `nm` reports `__aeabi_frsub` 456 B, `__subsf3` 448 and `__addsf3` 444 as three overlapping aliases of ONE blob, and summing them gave 33,486 B of ITCM inside a 32,736 B region - a total that cannot exist. **PREMISE CORRECTED: `ndsR2AnimValueQ` IS NOT "FETCHED WHOLE ON EVERY CALL BY CONSTRUCTION"** - its own per-PC census says **162 of 257 instruction slots and 23 of 33 cache lines** execute, and **320 B are never fetched at all** (it is a three-arm switch; the Cubic arm runs on 192.6 of 370.6 entries = 52.0%). The 21.13 tk/fr per byte PRICE stands; the mechanism attached to it did not. That is why the whole 1,028 B moved rather than a hand-split hot half - **the cold 320 B are already free of fills, so splitting them buys ITCM space and no fetch**. **A PLACEMENT CHANGE CAN HAVE A SAME-BINARY ROUTE**: placement is a property of a symbol's address so it cannot be toggled on one copy, but it CAN be toggled BETWEEN TWO copies of one body at two addresses - `ndsR2AnimValueQItcm` in `.itcm`, `ndsR2AnimValueQMain` in `.main`, both emitted from one `always_inline` impl, `gNdsR2AnimItcmRoute` (`020ea228 D` = `.data` NOT `.bss`) choosing the `bl`. **Disassembled before measuring: 257 instruction words each, ZERO DIFFERING WORDS.** **ENGAGEMENT EQUALITY, AND IT COULD HAVE FAILED: `gNdsR2CubicEvals = 290,076` on BOTH arms.** **RESULT (1,600 samples, frames 439-2038, `slips=0`, zero repeat floor): paired median -3,840 (85.8% of frames improve), marginal-80 median -6,432 (73 of 80), rank-80 -14,208, ranks 41-120 band -15,572, P50 -3,552**; complement `WORK-H -3,840 / WAIT +3,840 / ALL +0` to the tick; buckets land where the mechanism says (`SRC -5,760`, `GCRA -5,728`, `SINT -4,832`, `FTR -128`, `STG -64`). **Every one of 14 sampled ranks improves, rank 1 through rank 1200** - unlike last cycle, rank-80 is NOT a local kink. The +/-4,197,000 frames (766/1608/990 improve, 1529/1902 regress) are cartridge frames landing one presented frame apart between arms and are why the marginal-80 MEAN reads -172,098 against a median of -6,432; **quote the median**. **BANK -3,840**, the most conservative of five readings of ONE zero-noise instrument; the percentile readings are 2.2-2.4x it because the saving CLUSTERS on expensive frames (a heavy frame plays more Q AObj nodes, so it takes more of the 370.6 entries). **NEW BASIS `build-c219-animitcm-ship` rank-80 1,216,896 raw / 1,191,949 net = LEVEL +71,569**; its cross-build delta is -1,856 with the band moving the OTHER way (+7,074) and P50 -1,248, all inside their floors - **not a price**. **THE >=14,080 FLOOR WAS RE-MEASURED IN SITU THIS CYCLE**: the route build's arm 0 (kernel in `.main`, i.e. the pre-move behaviour) reads rank-80 1,231,616 against the c217 basis's 1,218,752 = **+12,864 for changing nothing about the kernel's placement**. The two builds agree where they should: route arm 1 1,217,408 vs ship 1,216,896 = **512 apart**. **SHIPPED KERNEL vs BASIS KERNEL: 253 of 257 words byte-identical**; slots 169-170 are the Step arm's `(inv <= len)` emitted commuted (`cmp ip,lr`+`ldrle` vs `cmp lr,ip`+`ldrge` - the same predicate for every signed input including equality) and slots 252-253 are literal-pool addresses that moved with `.bss`. **Behaviour identical; the encoding is NOT bit-identical and this is not claimed.** **SECOND INDEPENDENT CONTROL - Boundary's proof ROM reads `binds=54 vtx=2484 tri=828` and `ftrTri=132712/p067840/p164872/own424` IDENTICAL to `boundary-c206`/`-c209b`/`-c217`, for `ticks` 294,363,520 -> 294,353,408 = -10,112 = -47.7 tk/fr.** **TWO TRAPS, BOTH NOW STRUCTURAL.** (1) **`linker/nds_hot_text.ld:113` matches `*.itcm.*` BY FILENAME**, so dropping `--rename-section` freed NOTHING and the first link overflowed `itcm` by exactly the 332 B that were supposed to have moved; the member must also leave the pattern, hence `<stem>.mainram.o` and the new `NDS_TASK9_FLOAT_MAIN_MEMBERS`. (2) **`check-task9-float-itcm.ps1` asserted a PLACEMENT POLICY where it meant to assert the Phase-2 RENAME** and went red; five assertions carried a hardcoded `.itcm` and three private-copy paths named `<stem>.itcm.o`. It now reads each member's placement from **the build's own emitted object name** (an artifact independent of the ELF under test) and maps input `.text` to the linked output section `.main`. **NOT loosened - proven three ways**: new layout passes, OLD layout still passes, and c219-ELF-vs-c217-build-dir THROWS. Its byte-for-byte equivalence proof reports the SAME member hashes on both layouts (`_arm_cmpsf2.o=276/2B656E12...`), which is what makes the eviction placement-only. **Its new fail-closed guard then caught a hazard the old one could not**: a build directory that previously held `<stem>.itcm.o` KEEPS it when the member moves, so the two look like one member claiming two placements - the Makefile recipe now removes BOTH spellings for every member. **BYTES: `.itcm` +292 (32,224 -> 32,516 of the linker's 32,736 B region), `.main` -296.** ITCM headroom is **per target - always say which**: tick-HUD instrument **512 -> 220 free**, proof ROM **2,572 free**. (`check-task9-float-itcm.ps1` prints `free=` against the gross 32,768, so its line reads 252/2,604 for the same builds; use the region figure for a placement decision.) **ITEM B - THE BIND PLACEMENT IS STILL UNBUILT AND NOW BLOCKED TWICE, BOTH QUANTIFIED**: it ranks SECOND on the metric asked for (`ndsRendererHardwareBindTextureName` 14.19 tk/fr per byte vs the kernel's 21.13), it **does not fit** (268 B wanted against 220 B free), and at a 3,802 tk/fr port-reachable ceiling it is **INSIDE the >=14,080 cross-build floor** - so it needs its own SAME-BINARY ROUTE, not just its own build; it is `static` in `nds_renderer.c` so the two-copy pattern applies directly, and the next 240 B of ITCM come from the two raw-run emitters (dead at the shipped strip route 2). **PREMISE FLAGGED, NOT RE-MEASURED**: the 21,719 and the 3,802 both come from `build-c200-trackprof-off`, which differs from shipping in `NDS_R2_FIGHTER_GX_COMPOSE` (1 vs 0) and `NDS_R2_FTANIM_TRACK`/`_DISPATCH` - they are SIZES, not predictions, and this cycle measured the shipped configuration directly instead of inheriting them. The measurement recovers 18% (paired median) to 65% (rank-80) of the 21,719 and the gap is deliberately not explained. **Boundary GREEN**, "Boundary verification profile passed.", **0 `Exception:`**. **NOT DONE**: Item B not built, the remaining 1,718 cold ITCM bytes not taken, the kernel's 320 never-fetched bytes not split out, the 21,719 not re-measured in the shipped configuration, no draw reordering, no fidelity decision taken or asked for, `build-c205-camtoggle` not rebuilt, no pixel capture beyond Boundary's own.
Superseded 2026-08-16 - **BOTH COUNTER-GATED ITEMS ALREADY HAD THEIR COUNTER AND IT HAD ALREADY BEEN READ; ONE IS REAL AND SHIPPED, THE OTHER IS REFUTED AND RE-ATTRIBUTED; AND C2's BYTE LEDGER IS WRITTEN AND SAYS NO BY 3x** (`artifacts/performance/2026-08-16_tilesync-memo/TILESYNC.md`; **2 lab builds `build-c216-tilesync` + `build-c217-tilesync-ship`, 3 whole-match runs, 1 Boundary PASSED**, 0 gameplay values changed, no fidelity default flipped, no ROM published, root ROMs byte-unchanged `54c07fac...` / `6c939434...`). **READ THE ARTIFACT DIRECTORY BEFORE WRITING AN INSTRUMENT.** `NDS_TASK107_RENDER_STATE_CENSUS` (`Makefile:250`, `nds_renderer.c:5035`) publishes `gNdsTask107SyncCalls[4]`/`SyncUnchanged[4]` and six bind counters, and it was **RUN on 2026-08-15** (`artifacts/performance/2026-08-15_renderer-state-redundancy/STATE_REDUNDANCY.md`, `build-c186-statecensus`). Three documents since then said "needs one counter first". That census then **STOPPED on a 16,000-tick package floor that `AGENTS.md` now explicitly disavows** ("keep every repeatable correctness-preserving gain"), which is how a 62%-redundant lane sat unbanked. `[[check-what-the-harness-already-prints]]`, 4th recurrence - the cheapest discriminating read this cycle was `ls artifacts/performance/`. **ITEM A1 LANDED - `ndsRendererSyncTextureTile` IS REAL WASTE AND THE MEMO SHIPS.** It is not a VRAM sync: it republishes 19 fields of `texture_tiles[active]` into flat `texture_render_tile_*` members, is ITCM-resident so pays **1 tk/fr of icache and 5,770 of write_buffer** of its 9,469 total. `texture_tiles[]` has **exactly two writers** (`RecordSetTile` :7361, `RecordSetTileSize` :7504) plus one `memcpy` in the persistent copy, so an exact memo is two compares: bump `texture_tile_write_serial` iff the write targets `texture_render_tile` (the last synced index) or `NDS_RENDERER_LOAD_TILE`, and skip when serial AND index both match. Exact at init too (memset gives 0==0 and the outputs are already 0), and `ndsFighterDLDrawResetTransientRendererStats` bzeroes only up to `offsetof(othermode_h)` so it preserves tiles, outputs and serials together. The serials are **absent from both state hashes** and **present in the persistent copy**, deliberately. **ENGAGEMENT, AND THE CONTROL COULD HAVE FAILED: `gNdsR2TileSyncSkips=89,511 / Runs=54,594`, BIT-IDENTICAL ON BOTH ROUTE ARMS** (both arms evaluate the predicate and both advance the serial, so the only difference is the store burst) = **62.12% of 144,105 calls provably redundant**, 85.3% of the memcmp oracle's 72.835%. **SAME-BINARY ROUTE** (`gNdsR2TileSyncRoute`, `nm` reads `020e72e8 D` = `.data` not `.bss`; disassembled at 0x01ff87c0 before measuring): **paired median -3,648, 87.1% of 1,600 frames improve, marginal-80 -3,456**, complement-controlled `WORK-H -3,648 / WAIT +3,648 / ALL +0` to the tick. **Its own rank-80 reads +6,912 and that is a LOCAL KINK, not a regression** - the own-rank curve is -7,296/-7,104/+7,808/+6,912/-384/-128/-576/-3,904/-4,736/-4,160 at ranks 20/40/60/80/100/120/160/240/400/800, i.e. eight of ten sampled ranks improve and two adjacent ones do not, while every one of the control's marginal 80 frames gets cheaper frame-paired. Two +/-134,000 cartridge frames make the marginal-80 MEAN (-580) disagree with its MEDIAN (-3,456); quote the median. **SECOND, INDEPENDENT CONTROL - Boundary's own proof ROM carries the memo (`nm`: `ndsRendererSyncTextureTile` 0xf8, no route symbols) and its 212-frame realtime smoke reads `binds=54 vtx=2484 tri=828` and `ftrTri=132712/p067840/p164872/own424` IDENTICAL to `boundary-c206` and `-c209b`, for `ticks` 294,482,496 -> 294,363,520 = -118,976 = -561 tk/fr.** Same geometry, same binds, same triangles, measurably less time. **NEW BASIS `build-c217-tilesync-ship` rank-80 1,218,752 raw / 1,193,805 net = LEVEL +73,425**, cross-build -8,640 rank-80 / -6,656 band / -9,920 P50 / -9,216 paired at 93.2% win. **BANK ONLY -3,648**: -8,640 is inside the >=14,080 cross-build floor and this pair's own placement term is visible as `SRC +3,136` and `MISC +1,216`, buckets the memo cannot touch, while `build-c216`'s memo-OFF arm already sat 11,328 below the c215 basis. **+73,425 IS A LEVEL, NOT A BANK.** **ITEM A2 - THE TEXTURE-BIND COLLAPSE IS REFUTED AS AN ELISION ITEM AND RE-ATTRIBUTED AS A PLACEMENT ONE.** The 2026-08-15 census measured **ZERO** exact locally-redundant bind issues; 95,934 of 208,327 requests (46.05%) are already elided by the current-name check; the 26,769 revisits have a DIFFERENT bound name so deleting one in place changes the texture the following geometry samples - that is draw REORDERING, ceiling **2,484**. What this cycle adds: **45% of the 13,868 is `icache_fill` on 360 BYTES** - `ndsRendererHardwareBindTextureName` 268 B/104.6 ent-fr/3,802 tk-fr and `glBindTexture` 92 B/56.8/1,952, i.e. **14.19 and 21.22 tk/fr per byte**. **Ceiling 5,754, of which 3,802 is port-reachable** (the first is `static` in `nds_renderer.c` and takes `NDS_TASK82_ITCM_CODE` the way `NDS_R2_DELTA_PATH_CODE` members do; `glBindTexture` is libnds and needs the `--rename-section` route `BASIS.md` §6 found traps bytes). **UNBUILT, ONE BUILD.** **`POSITION.md` CORRECTED IN PLACE: 53,215 -> 42,366; 10,849 tk/fr of the fidelity-neutral inventory WAS NEVER THERE.** **ITEM B - C2 IS CLOSED, NO, BY 3x, AND IT COST ZERO BUILDS.** (1) **Most of C2 is already converted**: `Makefile:1680` is `override NDS_R2_CUBIC_FIXED := 1` and `NDS_R2_ANIM_CUT_ROUTE ?= 0` folds `NDS_R2_ANIM_CUT_ON(bit)` to constant 1, so `ndsR2FtAnimParseDObjFigatree` is the **Q parser in every shipped ROM** with its float arms dead-coded, its evaluator is `ndsR2AnimValueQ`, and `ndsR2AQStore` is a `__builtin_memcpy` bit-pun so the representation costs ZERO storage. The 20,357 "prize" is what SURVIVES that conversion - the f32 fields the decomp ABI fixes (`anim_wait`, `anim_speed`, `anim_frame`, `aobj->length`, the `Vec3f` joint outputs), i.e. **the representation boundary**, which `EXCHANGE_LEAF.md` already measured at R 0.83x-1.00x. `gmCollisionTransformMatrixAll` is exact about it: **21.7 ops/entry against 21 conversions = conv/op 0.97**, the same reading `gmCollisionGetWorldPosition` gave at 18/18. Moving the ABI instead touches **1,403 exact `->anim_wait`/`->anim_speed`/`->anim_frame` accesses in 117 files** and `Makefile:2302` makes every one a caller rewrite. (2) **`HWROUTE.md` §7's 3.61 tk/fr per byte is an AVERAGE (16,891/4,680) over four caller bodies and understates an ADDED HOT KERNEL by 4.6x-5.9x.** Measured on the same census: **`ndsR2AnimValueQ` 1,028 B at 370.6 entries/frame pays 21,719 tk/fr of `icache_fill` = 21.13 tk/fr per byte**; `lbCommonSin` 56 B = 18.66; `lbCommonCos` 64 B = 16.74; against `ndsR2FtAnimParseDObjFigatree` 3,016 B = 4.04. **The split is mechanism, not scatter: a large caller fetches only its EXECUTED path per entry, a kernel is fetched WHOLE on every call by construction.** So **the whole 20,357 prize is spent by 963 added bytes and half by 482** (was 5,640/2,820); the ring's 3,228 B and the camera's 3,032 B are each **2.5x-3.4x the WHOLE budget**. **THE ATTRIBUTION HAS A CONTROL THAT COULD HAVE FAILED**: `__aeabi_fadd` reads 3,903.9 entries/frame here against `BASIS.md` §7's independently published 3,903.9, and 96.2% issue share against its 96.22% - different tool, three significant figures. (The census `issue` column is a residual and goes NEGATIVE on three rows; only `total_cycles` and `icache_fill` are quoted from those.) **NEXT CYCLE INHERITS, UNOWNED AND UNPRICED BY ANY DOCUMENT: `ndsR2AnimValueQ` pays 21,719 tk/fr of PURE INSTRUCTION FETCH** on 1,028 B at 370.6 entries/frame = **0.296x of +73,425**, fidelity-neutral by construction because the arithmetic does not change. With `lbCommonSin`+`lbCommonCos` (120 B, 2,116 tk/fr of fetch) the animation chain's FETCH bill is **23,835 tk/fr against an arithmetic prize of 20,357 that does not convert.** It does not fit the instrument's 512 B of free ITCM as it stands - the open question is whether its **executed path** does, or whether 1,028 B splits so the hot half can. **BYTES: +128 B ITCM, +384 B `.main`, +96 B `.bss`.** ITCM headroom is **per target and the figures are far apart - always say which**: tick-HUD instrument 32,096 -> 32,224 of 32,736 = **640 -> 512 free**; the proof ROM Boundary grades reads **2,896 free**. **Boundary GREEN**, "Boundary verification profile passed.", **0 `Exception:`** over 316,171 lines (`builds/boundary-c217.log`, full stream in the artifact dir). **NOT DONE**: the bind ITCM placement not built, `ndsR2AnimValueQ`'s fetch not attacked, no draw reordering, C2 not built (that was the point), no fidelity decision taken or asked for, `build-c205-camtoggle` not rebuilt, no pixel capture beyond Boundary's own.
Superseded 2026-08-16 - **THE GAP IS +82,065: TWO BIT-IDENTICAL CODEGEN CHANGES SHIPPED FOR -216 BYTES, AND THIS INSTRUMENT TURNS OUT TO BE DETERMINISTIC** (`artifacts/performance/2026-08-16_hwmath-route/HWROUTE.md`; **2 lab builds `build-c214-hwroute` + `build-c215-hwmath-ship`, 7 whole-match runs, 1 Boundary PASSED**, 0 gameplay values changed, no fidelity default flipped, no ROM published, root ROMs byte-unchanged `54c07fac...` / `6c939434...`). **LANDED A**: `nds_r2_sqrtf.o` is built `-marm`, so `nds_r2_sqrtf.h:73`'s 48-bit `root*root` is one `UMULL` instead of `bl __aeabi_lmul`. **LANDED B**: the leading `DIVCNT`/`SQRTCNT` busy poll is deleted from every executing site - `sqrtf` and nine `nds_renderer.c` sites now call `ndsR2HwMathDiv64`/`ndsR2HwMathSqrt64` (`include/nds/nds_r2_hwmath_unit.h`), which are libnds's `div64`/`sqrt64` to the register minus that poll. **libnds is NOT touched** - those are `static inline` in `nds/arm9/math.h`, so the poll was compiled into our objects. Structural proof from two linked ELFs: **16 leading polls before, 2 after**, and both survivors are `battleship_gmcamera.c`'s, behind `NDS_R2_CAMERA_FIXED=0`, executing ZERO times - left alone deliberately so the owner's pending draw-side-precision arm stays priced as measured. **PRICED BEFORE A BYTE WAS WRITTEN**, from the entry PCs of a profile already on disk (`c200-off-pc.csv`, marginal-80, cycles/160): the leading poll is **2,491.3 tk/fr**, of which `sqrtf` alone is **1,845.8**; the outlined libnds `div64` executes **zero** times in this match, so nothing had to be done about libnds. **MEASURED ON A SAME-BINARY ROUTE** (`gNdsR2HwMathRoute`, `.data` not `.bss`; bit0 = ARM sqrtf body from `nds_r2_sqrtf_arm.o`, bit1 = poll skipped; **`romSha256` BEBC5801... identical on all four arms**, `readback==requested` and `stuck:true` on each, `slips=0`, level check arm0 rank-80 1,241,536 vs `build-c206-shipgx0`'s 1,239,808 = 1,728 apart): **rank-80 arm0 1,241,536 -> arm3 1,229,824 = -11,712**; ranks 41-120 band mean **-14,125**; paired median whole run **-5,184 with 79.9% of 1,581 frames improving**; split A -3,840 / B -768, additive to one quantum. **THE SHIPPING BUILD reads -12,416 cross-build on the same window** (band -12,144), better than arm 3 because it carries no route instrument, so -11,712 is a LOWER bound. **NEW BASIS: `build-c215-hwmath-ship` rank-80 1,227,392 raw / 1,202,445 net = GAP +82,065**, same target/config/window as the c206 basis it replaces. **LADDER 0.611x -> 0.695x** (12,416 banked + `POSITION.md`'s untouched 53,215 = 65,631 of +94,481), leaving **+28,850**. **BYTES ARE NEGATIVE**: `.main` 931,088 -> 930,872 = **-216 B** (`-marm` costs +24 B, the deleted polls give back more). **METHOD RESULT, REUSE IT: THIS INSTRUMENT IS DETERMINISTIC.** Repeat runs of arms 0 and 3 reproduced their 1,600-row CSVs **byte for byte** (identical sha256, VBI histogram included), so on a same-binary route with a fixed input script the same-arm repeat floor is **exactly zero** - every tick between arms is the change, and a repeat of an unchanged arm buys nothing. **READ THE CONCENTRATION, NOT THE HEADLINE**: the win is ~5,200 tk/fr at the median frame and ~13,000-14,000 on the gate population, because `sqrtf` runs **80.26 calls/frame on the marginal-80 against 45.72 whole-match (1.76x)**; rank-80 sits on a steep local slope (rank 60 -26,880, rank 80 -11,712, rank 100 -5,824), so quote the 41-120 band, and **arm 2's rank-80 of +3,072 is NOT a regression** - its paired median is -768. **ITEM C SIZED, NOT BUILT, AND THE BYTE BUDGET IS THE VERDICT** (`HWROUTE.md` s7): the four C2 members hold **4,680 B** and already spend **44.6% of their own cost on instruction fetch** (16,891 tk/fr `icache_fill` at 22-93 entries/frame), so **a byte in that chain costs 3.61 tk/fr and a 32 B cache line costs 115**. The 20,357 prize is confirmed as the soft-float gross charged to those four callers (1,446.2 helper calls/frame, disjoint from the 37,854 the bodies cost). **The whole prize is spent by +5,640 bytes, half of it by +2,820** - the ring added 3,228 B (would cost 11,650 tk/fr of fetch here) and the camera inlining 3,032 B. **So C2 converts iff the rewrite is byte-neutral or byte-negative, and no fixed-point rewrite in this tree has been; its first deliverable is a byte ledger.** Two limits it must carry: the chain's own `issue` cost is only 7,924 of 37,854 (20.9%), so the addressable ceiling is ~24,000 tk/fr = **0.29x of +82,065**; and `dcache_fill` (10,817) exceeds `issue`, so it is memory-bound on both sides and a Q26 joint matrix is the same 32 bits as the `f32` it replaces. **BLOCKED(decision: `sqrtf` IME mask)** - the ELF survey shows no interrupt-context user of either unit exists in this binary, so the mask guards a reachability that does not currently exist; priced at **698 tk/fr** for its three I/O accesses, **1,294** with their register setup. Removing a safety property is the owner's call. **NOT DONE**: C2 not built, no counter written for `SyncTextureTile` or the texture-bind collapse, `build-c205-camtoggle` not rebuilt, no pixel capture beyond Boundary's own. **CHECKER UPDATED AT ITS SEAM**: `check-gbi-decode-fixtures.ps1`'s projected-divider row now accepts either helper name - the invariant it guards (exact signed pre-clamp before the hardware divide) is unchanged, and both spellings are matched so the row cannot be silenced by reverting to a poll-carrying form. **Boundary GREEN**, "Boundary verification profile passed.", 0 `Exception:`.
Superseded 2026-08-16 - **THE DS DIVIDE AND ROOT UNITS ARE SAFELY USABLE AND 2.76x-8.31x CHEAPER THAN THE SOFTWARE FORMS, THE THREE NEVER-DEFINED HOOKS ARE DEFINED, AND COLUMN N IS 0.050x NOT 0.315x** (`artifacts/performance/2026-08-16_hwmath-units/HWMATH.md`; **4 lab builds `build-c210-hwmath`..`build-c213-hwmath4`, 5 emulator runs, 1 Boundary PASSED**, 0 gameplay values changed, no default flipped, no ROM published, root ROMs byte-unchanged `54c07fac...` / `6c939434...`). **HAZARD SURVEY FROM THE LINKED ELF, NOT GREP**: in `build-c206-shipgx0` exactly EIGHT functions reach 0x04000280-0x040002BF (`ndsRendererR2WriteLightVector`, three `ndsRendererHardware*Vertex*`, `ndsRendererSubmitNativeImpactWave`, libnds `div64`, `ndsR2CamDiv64`/`Sqrt64`, `sqrtf`) and all are mainline; the port registers ONE interrupt handler and its body is `sVBlankCount++`; no libnds or calico code touches either unit. SM64DS (`decomp/sm64ds-decomp/src/ARMMathSaveState.c`, called from `ARMSaveContext`) treats both units as THREAD CONTEXT - that is the shape this seam must take if the port ever gains preemption. **PRICES, in situ, 4,096 iterations per arm with the loop and operand generator subtracted**: 64-bit divide software 292.5 tk -> **106.0 (leading poll) / 65.0 (SM64DS form)**; 64-bit isqrt software 294.9 -> **55.5 / 35.5**; f32 divide `__aeabi_fdiv` **71.5** -> hardware **137.6 / 97.6 = 0.52x-0.73x, REFUTED**; `sqrtf` shipped Thumb **153.0** -> ARM state **90.5-96.5**. **ITEM A LANDED**: `NDS_R2_CFX_DIV64`, `NDS_R2_CFX_ISQRT64` and a third nobody had counted, `NDS_R2_COLLISION_DIV64` (`nds_r2_collision_mtx.h:361`, inside the live ring entry `ndsR2CollisionFixedInvertF32`), now reach the units: **+44 B text, 0 libgcc 64-bit divides against 6 in the control, 0 mismatches over 65,536 operands per class on four builds**. It moves the collision ring 2.68 -> **2.19-2.08** (arithmetic on `EXCHANGE.md` s3.1's own row, not a new A/B) - **still a loss on a lane whose ceiling is 0.161x of +94,481 at an exchange rate of ZERO, so do not re-price it**. `NDS_R2_CFX_HWMATH` follows `NDS_R2_COLLISION_FIXED` = 0 everywhere, so no shipped byte moves. **ITEM B**: Column N's `sqrtf` half was ALREADY SHIPPED (`NDS_R2_FIXED_SQRT ?= 1`, and it reads 1 in `build-c200-trackprof-off`, `build-c206-shipgx0` and `build-c209-simmac2`), so `SIMSIDE.md` s6's 144.62 tk/call IS the hardware form; its `fdiv` half is refused by a floor argument (the unit alone is 65.0 tk against a 71.5 tk routine that does the whole job). **ALL THAT SURVIVES, AND IT IS THE NEXT BUILD: `nds_r2_sqrtf.o` is the one R2 kernel object still `-mthumb`, so `nds_r2_sqrtf.h:73`'s 48-bit `root*root` is a `bl __aeabi_lmul` at 0x0208b10c. `-marm` costs +24 B and buys 37%-41% of 11,608 tk/fr = 4,300-4,750 tk/fr = 0.046x-0.050x, bit-identical (graded 0/65,536). Below the >=14,080 cross-build floor, so it needs a SAME-BINARY `.data` route between a Thumb body and an ARM body.** Ladder: 53,215 = 0.563x -> **57,715 = 0.611x**, leaving +36,766. **OPEN, UNPRICED**: the leading poll costs **41.0 tk per divide and 20.0 per root and protects nothing** (only the last write to DIVCNT/NUMER/DENOM matters); libnds's `div64`/`sqrt64`, five `nds_renderer.c` sites and `battleship_gmcamera.c` all carry it, SM64DS's `cstd::div` does not, both forms grade bit-identical here, and nobody has counted those sites' call rate. **CHAIN MAP (item C, assessment only)**: C1 collision cluster 50,044 tk/fr is BUILT with conv/op ~0.02 and still measured 2.68 - killed by 3,228 B at **0.97 entries/frame**, so conv/op near zero is necessary and NOT sufficient; C2 animation->joint-matrix 20,357 tk/fr has conv/op ~0 AND **22-93 entries/frame** and a free front endpoint (figatree tracks are `s16` on disk), and is **the only chain worth a build - measure its BYTES first**; C3 trig leaves 4,519 are C2's interior and already implemented in `nds_r2_collision_fixed.h`. **RETRACTED AND FIXED AT ITS SEAM**: build-c210 reported 110 wrong IEEE quotients in 65,536, deterministic over two runs. Two hypotheses died first (the 64/32 mode; the leading poll - both graded 0/0 in c211/c212). Cause: `int32_t` is `long` on devkitARM, so the bench wrapper's `(int32_t *)` cast onto an `int *` was a strict-aliasing violation and GCC deleted the `| remainder` sticky term (visible at 0x0208bec8). c213 rebuilt the identical arm with the types made consistent end to end: **0 mismatches, 223 half-cases still live**. No pointer cast is left in the chain, so drift is now a `-Werror` error. Nothing shipped carried it. New: `include/nds/nds_r2_hwmath_unit.h`, `include/nds/nds_r2_hwmath.h`, `src/port/nds_r2_hwmath_bench.c`, `scripts/check-r2-hwmath.ps1|.c|-arm-probe.c` (58,339,748 host cases, 0 mismatches, six exhaustive 2^23 significand axes).
Superseded 2026-08-16 - **THE WARM-MAC EXCHANGE RATE IS 0.83x AND 1.00x - A LEAF FIXED-POINT CONVERSION PAYS ONE SOFT-FLOAT MULTIPLY PER BOUNDARY FLOAT, SO COLUMN P DOES NOT MOVE AND `POSITION.md`'s 0.563x STAYS 0.563x** (`artifacts/performance/2026-08-16_simmac-exchange/EXCHANGE_LEAF.md`; **2 lab builds, 7 whole-match runs, 1 Boundary**, 0 gameplay values changed, no default flipped, no ROM published, root ROMs byte-unchanged `54c07fac…` / `6c939434…`). **MEASURED IN SITU, ONE BINARY** (`build-c209-simmac2`, `romSha256` identical on all six arms so there is NO placement term; `slips=0`; arm-0 rank-80 **1,238,912 raw / 1,213,965 net**, 896 from `build-c206-shipgx0`, i.e. the instrument is inert at arm 0): **`gmCollisionGetWorldPosition` gross 289.6 -> fixed 348.2 tk/entry = R 0.83x** and **`func_ovl2_800ED490` gross 987.3 -> fixed 987.8 = R 1.00x**. **A 31,850 tk/fr lane converts to -2,660 tk/fr - A LOSS.** Counting the float bodies' own self time (the gross is library-only, `CAMERA_Q20_12.md` §4's convention) lifts them only to **1.00x and 1.17x**. **THE ROUTE IS A SHADOW, NOT A REPLACEMENT**: every arm runs the decomp float body and keeps its result, then evaluates the fixed form and discards it, so the arms play the bit-identical match by construction - **all nineteen whole-match invariants equal on all six arms**, `gNdsCfxFighterDamagePhaseCalls` 2,684 and `Hits` 26 included - and the delta IS the replacement cost, not a residual divided by a count. **THE LAW, and it needs no build: an f32<->Q edge conversion costs 31-42 cycles, between one `__aeabi_fmul` (26.5) and one `__aeabi_fadd` (38), so a leaf's rate is decided by CONVERSIONS PER DELETED OPERATION - a property of the SIGNATURE.** transform 18/18 = **1.00**; compose 36/63 = **0.57**; `gmCollisionSetInvertMatrix` 24/61 = **0.39** (predicts ~1.2-1.4x); the 5.14x prior's `guMtxCatF`->`ndsRendererMtxMul20p12` = **0**, because both sides already hold their native representation. **That is why the prior never transferred - not warmth, not transcendentals, not Thumb**; `SIMSIDE.md` §5's two named reasons to expect better than 1.70x are both true here and neither mattered. **SO SIZING THE WARM-MAC SUBSET BY FUNCTION WAS THE ERROR**: its two largest members are LEAVES, are 44.6% of the 71,491 by size, and are worth less than nothing. **The projected 0.875x-1.173x is REFUTED for the leaf route; the inventory stays 53,215 = 0.563x.** **WHAT SURVIVES IS THE CHAIN ROUTE** (`n_conv` fixed at the endpoints while `n_op` grows - the design of `nds_r2_collision_fixed.h`), whose only measured instance is the ring at **2.68x**, whose named cause is still unaddressed: **`NDS_R2_CFX_DIV64` is STILL the portable bit-by-bit divide and the DS hardware-divider hook at `nds_r2_collision_fixed.h:205` has never been defined.** Column N is untouched and mildly SUPPORTED - what proved expensive is the representation boundary, and a bit-exact helper never crosses one. **BOTH BODIES ARE UNREACHABLE FROM PORT CODE and that cost the first build**: a `#define`-before-`#include` rename moves the definition AND `gmcollision.c`'s own call sites together, so measured over a whole match `func_ovl2_800ED490` has **EXACTLY 0** cross-TU entries and `gmCollisionGetWorldPosition` **168 of ~92,000 (0.18%)**; `Makefile:2302` forbids a decomp overlay patch for a new adaptation, so **converting either is a CALLER REWRITE, never an interception.** The second build drives them from the damage-collide gateway (zero in-TU callers, **2,684 of 2,684** captured) with a repeat count in the route word and reads the price from the slope. **THE STATISTIC MATTERS AND ALL THREE OBVIOUS ONES ARE WRONG HERE** - the seam fires in bursts, so the per-frame paired median is structurally ~0 (128), the mean is carried by two cartridge frames (123,628) and the trimmed mean deletes exactly the frames that carry the work (trim-40 19,755). **Use the per-ring-stop window sum**: each 96-frame window carries its own exact evaluation count from `-PerStopGlobals`, and it is stable - **632.8-714.3 tk/eval over 11 windows, median 668.0**, with a 4x density change (`r16tc` 679.7) agreeing inside 5%. **LIVE-DOMAIN EQUIVALENCE, GRADED AT ZERO FIDELITY RISK** (route word 7, fixed answer against the decomp float body's own answer on the same inputs, every captured call): 8,052 components - **6,241 exactly equal, 1,811 off by ONE Q12 quantum, none worse, max deviation 1 quantum = 0.000244 world units against the 0.0200 bound (82x inside)**, `XfrmDecline`/`CmpsDecline` **0** on every arm. The arithmetic is settled; the population is one seam's 2,684 calls, not coverage of the call set - and it is moot for the lever question, because at R <= 1.00 there is nothing to trade fidelity FOR. **BOUNDS STATED AS BOUNDS**: the once-per-driving-call term (compulsory fetch of 2,784 B of kernel) is bounded at <= ~500 tk by the `r16tc`/`r64tc` agreement and is NOT fitted - `r1tc` at 2.81 evaluations/frame is inside session noise and resolves nothing; because that term would be paid PER ENTRY at the real spread rate, 348.2/987.8 are LOWER bounds on real per-entry cost and 0.83x/1.00x are UPPER bounds on R. **DIAGNOSTIC FIXED**: `gNdsBattlePlayablePacingPresentIntervalBucket` - the one histogram `AGENTS.md` requires in every device A/B - was cleaned out of the D-cache only BY ACCIDENT, because `DC_FlushRange` rounds to whole 32-byte lines and two `NDS_BATTLE_PLAYABLE_PACING_GROUP` members happened to straddle its two lines; any `diagnostics.c` relayout would have returned it to STALE reads silently. It cannot join that group (pinned bilaterally against the `BPLAY_PACE` printf at 14/14), so it now has its own `NDS_BATTLE_PLAYABLE_PACING_HISTOGRAM_GROUP` published at the same frame-complete seam and pinned by `check-gbi-decode-fixtures.ps1`. **NOT DONE: no conversion implemented, nothing wired, no fidelity decision taken or asked for, and the other ten warm-MAC members were MODELLED not measured.** **Boundary green, 0 `Exception:`.**

## R2-07 `BUGS.md` — symmetry FIXED / OWNER-CONFIRMED; three P1 atlas-coverage cells remain open

`docs/BUGS.md` carries the owner's own wording and is the queue — do not reword it. The 2026-08-12 playtest
closed old rows 2/3, fire burn and Fox gun (`artifacts/bugs/2026-08-12_r2-07-cluster/`,
`…/2026-08-12_c130-fire-gate/GATE.md`), and the "all three rows converge on ONE texture-residency capability"
framing was wrong — never reinstate it. **A magenta bar beside Fox is the BEAM ITSELF**, relocData 316's
RGBA(219,0,134), not a debug quad. Rows 1/2 DECIDED in `…_c-residue/OWNER_DECISIONS.md` §1/§2.

- **Whispy — FIXED, OWNER-CONFIRMED 2026-08-14.** Dynamic stage matrices now validate every frame; direct A/B + owner eye closed it.
- **Fox — CLOSED 2026-08-15, OWNER-DECIDED: the bore is 0.** *"bore should be zero, no offset, not needed anymore"*. The 2026-08-14 "perfect" at **84**
  was tuned against the frame-stale pose `64c41c361a7` repaired; the owner's bore-0 acceptance covers **crouch avoiding the beam**, not only the visual.
- **Dream Land BG edges — FIXED, OWNER-CONFIRMED 2026-08-14.** DS-only centered **K=9/8** covers far-floor overscan;
  affine q16 scale metadata stays bit-exact. `artifacts/verification/2026-08-14_dreamland-bgstretch/`.
- **P1 VFX symmetry — FIXED, OWNER-CONFIRMED 2026-08-14.** MASKS/MASKT was missing from the common-atlas submit; **19**
  masked scripts reconstruct 2/4 pieces over the same rect; ledge FlashMiddle is script5/texture2/**MASKST**. ROM **8C7507F0…**; `…/2026-08-14_p1-vfx-texture-audit/`.
- **P1 coverage 4/11/14 — IMPLEMENTED 2026-08-14, NEEDS THE OWNER'S EYE ON A SHIELD BREAK AND A SIDE KO.** Source **32x32**, cell cap **64**, frame cap **1**, **same four 8 KiB sheets**. It was **packer waste, not VRAM** — 27,520 of 32,768 held, and the shelf packer's 5,248 free texels were 16-tall tails no 32x32 cell can use, so the **64 -> 32 cell-cap shortcut was never necessary**.
  Palettes went **one per sheet** (zero VRAM; `glColorTableEXT` already ran per sheet), which stops the extra cells pulling the shared k-means: **24 of 31 decode BETTER, 2 worse by <=0.002, none dropped**. 28/31/35/36 stay out **by name** (`QUAD_P1_DEFERRED`) — admitting 31+35 cost texture 33 **+39%**. ROM **2015FBD1…**; `…/2026-08-14_p1-texture-4-11-14/EVIDENCE.md`.
- **THE TREE IS SETTLED AND THE TWO REDS ARE ATTRIBUTED (2026-08-14).** `HEAD DID NOT COMPILE`: `33d7cc5d3b7` shipped `nds_renderer.c` with `mirror_mask` in `ndsRendererSubmitParticleQuad`'s definition while its declaration stayed in the worktree. All four owner-confirmed 08-14 fixes are now committed (`9b6c9e72a25`).
  **`check-gbi-decode-fixtures.ps1` IS GREEN.** Two separate faults, one hidden behind the other: the M3 consumed-field certificate still classified `workspace.slice44_validate_cursor` under `…PrepareNativeStageMatrices` after the Whispy fix removed that read (narrowed in the generator + manifest, the field still lives under `…PrepareNativeStageOwner` and the rigid guard); and `33d7cc5d3b7` respelled `ndsRendererScanList` to `NDS_R2_CENSUS_EVICTED_CODE`, so the five-hot-path count read four (`813207773c1` teaches the count the alias **and pins the alias to `NDS_RENDERER_HOT_CODE` at census 0**).
- **BOUNDARY IS GREEN 2026-08-14 — THE RED WAS A CORRUPT DLDI SD IMAGE, NOT A COMMIT, AND THE FIVE-COMMIT BISECT WINDOW IS REFUTED** (`artifacts/verification/2026-08-14_boundary-red/BOUNDARY_RED.md`; log `boundary-after-dldi-reset.log`, 0 `Exception:`, marker capture **27.8 s of the 120 s ceiling**). **No source byte changed.** `emulators/melonds/dldi.bin` (gitignored, 536,870,912 B) made the ROM load NOTHING — `sNdsRelocLoadedFileCount 0`, `gNdsRelocAssetPayloadReadCount 0` — so `gMPCollisionGroundData->wallpaper` stayed the raw token **`0x3eb`** and `lbCommonMakeSObjForGObj`'s `sprite->bmsiz` (`[0x41c]`) took a data abort. Calico's `__excpt_entry` then **disables the PU** and `blx`es junk slot `0x205`, so the ARM9 slides through zeroed RAM in ABORT mode (`cpsr=0x400000b7`, `r12=0x205`, `lr_usr=lbCommonMakeSpriteGObj+26`) and every capture times out. Three arms: 512 MB image = ABORT · DLDI **off** = SYSTEM (diagnosis only, DLDI-on is retail parity and ~29,696 P95) · **fresh 16,957,440 B image = SYSTEM and green**. Old image kept at `dldi.bin.broken-2026-08-14`. **The window is refuted by measurement, not argued**: `build-c-collfixed` (08-13 19:25) and `build-c156-vfxsymmetry` (08-14 10:44) — both BEFORE the window — abort identically. Eight of the eleven commits ship no byte anyway (`54d7d7862e4` is comment-only; `813207773c1` is a checker the Makefile never invokes). **`gdb-markers.ps1` now classifies a timeout**: gdb's attach line reading `in ?? ()` means the guest had already crashed, so do not raise the ceiling. **A second attach is impossible — melonDS's stub refuses every reconnection**; sample the PC with a FIRST attach instead. A probe that may be killed still needs `set logging enabled on`.
- **R2-08 IS ONE MAKEFILE LINE, STAGED NOT LANDED — §6 items 1 and 4 RE-MEASURED ON THIS TREE 2026-08-13**
  (`…/2026-08-13_c-r2path-recheck/SWITCH_READY.md`): Boundary green through `NDS_R2_PATH=1`; soak `NO-FREEZE`, 2 START rematches;
  gate arm 929,344/**1,204,352** vs control 924,864/1,210,944 — inside the floors, opposite in sign, invariants identical,
  **+80 B text**. Owner-only: `:391` retail test and `:385` the eye — take the eye AFTER the flip, Boundary shoots `PATH 0`.

## The two arms — same 60 s match; ONLY `BOTH_CPU 1` is R2-07's gate

| arm | role | `WORK-H` P50 | P95 |
|---|---|---:|---:|
| `battle_playable_realtime` (`BOTH_CPU 0`) | shipped, verifier GREEN | 899,136 | 1,087,616 |
| **both-CPU (`BOTH_CPU 1`)** | **R2-07 GATE, FAILS** | **939,136** | **1,184,064 raw / 1,159,117 net** |

**CADENCE IS READ FROM `NDS_TICK_HUD_DRAW=0` (owner, 2026-08-14; `plan.md` §6).** Measured on the
gate arm with no gdb stub attached: **`DRAW 0` VBI 2:1427 3:162 4:10 6:1, max 6, two-VBlank 89.1%**
vs `DRAW 1` 2:1359 3:229 4:9 5:1 6:1 10:1, max 10, **84.9%**. The HUD draw burst is worth **4.2
cadence points**, so the deficit against ≥95% is REAL and is not the instrument. Label every cadence
figure with its `NDS_TICK_HUD_DRAW`. That `DRAW 0` arm carries `NDS_TASK37_PROFILE=1`; a clean one
is one build away.

**LEDGER INDEX (2026-08-13, superseded as a bank, kept as a method)** — `…/2026-08-13_c-ledger-index/LEDGER_INDEX.md`. **The shield attach path paid for a SEARCH, not work**: each attach hit `ndsAObjEvent32FindNormalized`, a linear scan of a 1,177-entry ledger at 8.05 tk/iteration — **5,123 tk/attach, ~88% scan**. An O(1) index took P95 1,250,368 → 1,210,944, P50 flat, `SINT` −23,936, 40 frames 3→2, 12,667 oracle lookups 0 mismatches, A2 falsifier brackets control 2,752. The anim-joint fix's **+49,216 came back −39,424**, now +9,792.

**`analyze-tick-hud-excursion.ps1 -Ceilings` emits both arms' tables** (`…/2026-08-12_c130-fire-gate/LANES_BOTHCPU.md`); c125's ceilings are dead, and so is `SITR_NEXT_CUT.md`'s cut (`…/2026-08-13_sitr-aobj-layout/`). **`RESIDUE.md` §0 corrects all sizing. SLICE 50 SPENT §4 row 0 — leg A landed (195 calls/frame → 8 sweeps a match, `STG` −11,328 flat, viewport pixel-identical); legs B and C REFUTED, do not re-brief that row.** **B is blocked by RAM**: 828 B against the texture cache `_Static_assert`'s **72 B** of slack. **C does not exist** — slice 1 converted every per-corner writer; the residue is the shared `gl*` → `ndsRendererTask29Gl*` wrappers, which have stage callers, not an `#if`.
**Apparatus is 24,947 not 18,675** (third lane `sNdsEffectPacketArmed`, `#if NDS_TICK_HUD`, 6,272 on EVERY GX command), so the product-side gap is ≈62,300. **Owner package: `…/2026-08-13_c-residue/OWNER_DECISIONS.md`.** **RUNG 2
(quarter-rate particles) REFUTED, no build (`…/2026-08-13_c-particle-rate/`): `MISC` is a DRAW
residual** (`taskman_seam.c:5104`), so its 17,152 never priced the update half — which is `SRC`,
7,364 tk/fr, and pays **−7,493** quarter-rate (ALL particles, both halves, −33,818). **And it shares
ONE LCG with the level-3 AI**, so any cadence change diverges the match: **CHECK EVERY SUB-RATING
FOR `syUtilsRandFloat`.**

**A PROFILE CYCLE IS HALF A TICK.** `ticks/frame = cycles / (2 × regions)`; regions P50 2,240,838
cyc = 2.0001× two VBlanks, so `cycles/2` reproduces the gate arm's `ALL` histogram and every
`SITR`/`AObj`/`AnimValueQ`/`__aeabi_lmul` crumb halves and is gone (`RESIDUE.md` §0).
`analyze-symbol-line-profile.py` reads `regions` and prints its `basis`. Only `FTR`/`STG` are flat
where P95 lives (band min 296,320/171,520), so flat cuts there pay 1:1 and ADD.
**`…/2026-08-13_c-flagsweep/c123-pc-cycles.csv` caches that profile as 98,346 rows — a 79-second
one-pass replacement for the 2.6 GB scan. RANK THE INLINE ATTRIBUTION, NOT ONLY THE SYMBOL
CENSUS: the whole of `RESIDUE.md` §4 row 0 is invisible to a symbol ranking.**

**THE BIGGEST LEVER IS PLACEMENT.** Memory stall is **1,236,685,107 cycles, 33.8% of the match** ≈ 386,000 tk/frame — past `SINT`/`SHDT` by an order, and why slice 48's identical pair differed 94,976.

**SLICE 49 (reclaim dead ITCM) is REFUTED without a build — do not re-open it until the Task 37 port
group is understood.** `.itcm` is NOT full (30 of 82 residents never execute, 2,594 B idle), but the
census's 87,033,153 "in reach" stall cycles assume admitting ~3,118 B of mostly PORT functions, and
`NDS_TASK37_ITCM_LEAVES`' PORT bit is held on CORRECTNESS (owner: the enabled lab build misbehaves). **Eviction alone pays nothing.**

**Lane-sizing traps, now encoded in `-Ceilings`:** medians do not add (it invented a 110,336 lane in c122); `OTHR` CONTAINS `WAIT`; only `WORK-H` is spendable. A `-Ceilings` ceiling flattens a lane to its own MEDIAN, so it prices the EXCURSION only — not what deleting the lane pays (`FTR` 8,512 vs 311,744); `RESIDUE.md` §2 has both. **Profile with `NDS_TICK_HUD_DRAW=0` or you profile the instrument**; the GATE keeps `DRAW=1`, cadence comes from `DRAW=0`. Soak length is `NDS_R2_SOAK_MATCH_MINUTES`; `Makefile:382` forbids reporting a
both-CPU P95 as Boundary's. **Route to ATTRIBUTE, re-bank to BANK. Collision paid and is BANKED**
(slices 35/36/37, −10,752). Float in `mp*`/`ftMain*`/`ftComputer` is FROZEN; `gmcollision` was UNFROZEN
2026-08-13, but the LINKED ELF says only **37–52%** is reachable (`…_c-collision-seam/`, slice 52).

## What is dead, so nobody re-derives it

- **`SPRM` 13,056, `AUD` 13,824, `BG` 3,968 — closed by arithmetic 2026-08-13** on a WHOLE-MATCH MEAN; **`SPRM`'s excess on the 80 frames that SET P95 is +49,377 (25.8x presence)**, so re-price that one on the marginal frames before re-closing it. `SCPU` needs −32.1% and reads 896 on the rank-80 frame. **All 169 `?=` flags audited — ZERO unshipped wins (`…/2026-08-13_c-flagsweep/FLAG_SWEEP.md`).**
- **THE P95 EXCESS IS ~90% SIMULATION, AND IT REPRODUCES ON TWO DIFFERENT MATCHES** (`…/2026-08-14_runtime2-p95-closure/{MARGINAL_OWNERS,GATE_ARM_OWNERS}.md`; `scripts/census-tick-hud-p95-set.py` re-derives it in one command). On the 80 frames that literally SET P95 the excess over a 2-VBlank frame is **+508,993** (c147: +520,718), `SRC` **+456,480 = 89.7%**, **all inside `GCRA`** — `SRC−GCRA` = **−22** here, −68/−64 there: three populations, three zeroes. Nesting `SRC ⊃ GCRA ⊃ {SINT ⊃ SCPU, SHDT, SPHD/SPHC, SCAT, SPRM}`; never add a parent to its child. **But the ranking BELOW `SRC` is match-specific — only `SITR` survives**: `SITR` +171,234 → **+188,907**, while `SHDT` 119,920 → 80,837, `SPHD` 112,833 → 75,236, `SCPU` +7,222 → **−8,669**, and **`SPHC` +62 → +52,780 (59.3x)**. Size nothing off one match's events. **`FTR` 1.03x, `STG` 1.00x, `MISC` 1.23x — the draw side is 7.1%,** confirmed independently by the v3 profile's draw closure at **8.4%**. **Cut 64,452 at the 80th-largest frame.**
- **THE FIRST v3 STALL CAPTURE ON THE GATE ARM EXISTS** (`…/v3-gate-arm/`, `build-c159-profile-bothcpu`, `BOTH_CPU 1`/`DRAW 0`, 1,601 regions, 3.62 GiB, residual −15; reduced CSVs `gate-p95-pc.csv` + `gate-band-pc.csv` are committed so **nobody re-scans it**). Whole match tk/fr: icache 354,678 · dcache 269,944 · halt 214,226 · issue 208,689 · wbuf 48,914 · intlk 43,122 · bus 36,738 · dma 8,450. **On the 80 P95 frames the excess is icache_fill +155,795 (40.0%) · dcache +96,800 (24.8%) · issue +94,029 (24.1%)** — still no single owner, and **no function exceeds 3.6%**. **`plan.md` §7's hot-footprint re-admission test FAILS on its own condition: the draw closure holds only 11.5% of the icache excess** (+17,944 of +155,795) while the fighter-proc closure holds 41.4%. **Soft float does NOT promote**: 38.0% of the family's caller-attributed 74,283 tk/fr is inside the fighter procs, **31.2% is draw side**, largest single caller 5,402. - **NATIVE BATTLE KERNEL SLICE 1, PHASES 1–4 DONE 2026-08-14, ZERO BUILDS** (`docs/architecture/RUNTIME2_NATIVE_BATTLE_KERNEL.md`; `…/2026-08-14_native-battle-kernel/BATTLEPACK_ANIMATION.md`). **The named mechanism is NOT the FAT read.** The animation path does **15** file loads all match on 13 frames, and deleting those 13 frames ENTIRELY moves the 80th-largest frame **9,874** against 64,452. **The lever is the acquisition path: 299 acquisitions, 95.0% cache HITS, on 62 of the 80 P95 frames vs 174 of 1,520 body frames — 6.8x presence.** A hit still copies the payload, re-registers, re-finalizes, re-normalizes AObj16, strips alias nodes, writes three status entries. Dose-response **+148,969** first acquisition, **+77,440** each after, **+645,225** a miss; modelled full deletion **−73,659** at rank-80 (upper-bound model, profile arm, **not banked**). **`get_fat`/`f_lseek` are majority BGM** — ≤38.8% of the P95-set `get_fat` is on an animation frame, so sizing off the full +15,058 over-predicts 2.6x. **Reachable set = all 301** (`ftdata.c`: 143/143 Mario + 158/158 Fox); the 87-entry warm list is 28.9% of it and observational. **Pack 651,928 B vs ~511,904 proven RAM — DOES NOT FIT, RE-MEASURED 2026-08-14 AND IT STILL DOES NOT.** **Items-off is PROVEN from the LINKED ELF** (every item-status setter is a 2-byte `bx lr` stub; no item spawner exists) and priced by RE-PACKING at **553,696 (−98,232), mismatch 0**; the 14-clip matchup lead reaches 528,624 but its handlers are merely ABSENT, which is not proof. **Route 1 is 12,608–25,216 B, not 140,024** — the per-status malloc is `gFTManagerFigatreeHeapSize`, the largest single anim FILE (max payload 6,224), 2 heaps a match plus 2 on Sudden Death; **the 262,144 arena was already counted and HANDOFF's 192,240 is its bump high-water INSIDE that reservation, not a second pool.** **THE THREE POOL TERMS ARE NOT ADDITIVE**: the arena is one `calloc` of `NDS_TASKMAN_ARENA_SIZE 0x150000`, so `.rodata` draws on 211,936 ALONE and an arena pack on 299,968 ALONE; 511,904 needs the constant cut, the 0x130000 search floor lowered AND the Task 36 replay guard retaught (`nds_renderer.c:5734-5739`). **PHASE 2 IS SPENT AND MEASURED 2026-08-15 (+146,560; see the RAM section) — but "fits the FULL pack by 6,536" is the COMBINED pool only, and the 3-file coupling still stands.** More-compact-representation is REFUTED as a lossless lever — the stream is u16 headers and **s16** targets, no f32 to narrow. **PHASE 5's PREMISE IS HALF THE REASON**: the copy also exists because `decomp/…/ftmain.c:4623-4624` DISCARDS the return and hardcodes `fp->figatree = fp->figatree_heap`, and `battleship_ftmain.c` `#include`s that body; **LANDED 2026-08-15 (`6e93def43cd`) and provably inert — exactly ONE symbol changes size in the whole binary (`battleship_ftMainSetStatus` −8 B, the post-call heap reload deleted), and the callee returns exactly `heap` on every path.** FAT fallback still forbidden. **Phase 4 equivalence mismatch = 0** (297 clips / 5,629 scripts / 77,959 commands / 71,500 states / 5,629 callbacks), two falsifiers prove it can fail. **The counter build the brief asked for was not needed**: `scripts/census-profile-pc-per-region.py` reads per-presented-frame call counts for any symbol out of a v3 capture, so "does it concentrate on the P95 frames?" no longer costs a build. Still unmeasured: BYTES on the hit path — one `gNdsR2AnimCacheHitBytes` at `reloc_backend_assets.c:7207`. `gNdsRelocAssetOpenFailCount` is conflated across 11 writers and should be split.
- **THE 160-FRAME CADENCE SET IS 64% INSTRUMENT.** 102 of the 160 are ALREADY under the 1,116,096 cadence boundary in `WORK-H` and 98 of those carry the HUD draw burst; only **58** are `WORK-H`-bound, needing a mean **43,916** (94,848 is the WORST frame, not the set). A burst frame presents in 2 VBlanks 3 times in 1,360. **The 123,773 write_buffer/interlock/bus pool reproduces to the tick and is NOT a lane** — largest holder 9.1%, and every top holder is already owned (GX FIFO, fighter draw, texture upload, or `tickGetCount`/`cpuGetTiming`, 13,406 tk/fr of apparatus). Only `memset` 10,570 + `ndsRendererSyncTextureTile` 3,945 are pool-shaped, and they sum under the floor.
- **NEWLIB FORMATTED I/O IS REACHABLE AT `NDS_TICK_HUD=0` — the "instrument-only" reason was wrong**, the verdict is not: `--gc-sections` keeps the family and `ndsRelocAssetFindEntry`'s NitroFS path builder calls `sniprintf` on every animation lookup. Bound **≤769 tk/frame**, 20x under floor. Closed on cost, not on reachability.
- **`FTR` — −93,612 landed (c116); its "0/80, NOT a P95 lever" verdict is BOUNDARY-arm and the gate
  arm's 8,512 is the EXCURSION ceiling, not what deletion pays (311,744).** DS-native AOT geometry
  ships (`NDS_TASK56_FIGHTER_PRIMITIVES ?= 2`) **SHIPPED BROKEN** — 35.6% of the fighter backfacing
  with Boundary green: **a passing verifier is not visual verification.**
- **Effect DObj submits** — Boundary-only. **Projectiles** · **texture thrash** · **`Find`** ·
  **`Material`** · force-load seam. **`MISC` is the DRAW residual, not "particles"**; particles are
  FLAT. **AOT animation bake** (32): SIZE dead. **In-match FGM I/O + `FindPlanned`**: closed
  2026-08-13, `SHDT` block above.
- **Animation playback ARITHMETIC** (slices 34, 41): idle-joint skip (33), lazy track table (31), AObj walk and dispatch all under the floor. **Slice 41 spent the last lever**: 30 Hz poses cost **+7,040** *and* diverged the match (damage 130/51 vs 33/65). **Don't blanket-convert `ndsBaseGcPlayMObjMatAnim`** — 5 tracks pack 0xRRGGBBAA in f32. STRUCTURAL LAYOUT cuts closed 2026-08-13; call count is the lever.
- **THE CALL FRAME IS CLOSED 2026-08-14. Its 64,863-tick ceiling does NOT convert: best package 10,544 cyc = 5,272 ticks, 3.0x short of gate** (`…/2026-08-14_call-frame-slice/CALL_FRAME_SLICE.md`, `scripts/census-frame-candidates.py`). **A split recovers the REGISTER DELTA, not the frame** — fitted `push(N)≈1.6+1.2N`, `pop+pc(N)≈5.0+1.6N`, so **~9.3 cyc/call survive any split** (pc-load flush). `ftGetStruct` corrected to −3,770 cyc, not −7,141. **`ndsR2AnimValueQ` (6,919, largest) is BLOCKED** — its `noinline,target("arm")` is measured: Thumb has no SMULL, that arm cost **WORK-H P50 +25,472**. **202 diagnostic-SHAPED names = 98,871 cyc hold only 7,849 cyc of real diagnostic**; `…RecordCapturedDisplay` is the stage hook, `ndsIFCommonRecordHUDState` drives the HUD (`nds_platform.c:2616-2760`), renderer `Record*` IS the RDP state machine. **Method: size against the LINKED ELF** — codegraph returned `#else` non-hwtri stub bodies and nearly bought a false 14,800-cyc lead. Still worth taking WHEN BUILDING ANYWAY: `ndsFighterDisplayContractCountFlags` 7,849 cyc = 3,925 ticks, no runtime reader; gate the traversal, keep globals `used`.
- **UNITS: 2 profile cycles = 1 tick.** The dcache census compared cycles against tick floors and overstated everything 2x; the DMA0CNT poll is **8,315 ticks, UNDER the ±8,544 floor**, not "at the bankable bar". **RETRACTED with it: "88.9% of data-load excess is cacheable" was the classifier FAILING OPEN** — an unresolved base became `cacheable`; fail-closed reads **cacheable 0.1%, unknown 91.7%**. Only 0.1% was ever PROVEN cacheable; that STOP stands, its basis does not.
- **THE v3 STALL ATTRIBUTOR IS ALREADY IN-REPO: `emulators/melonds-attributor/melonDS.exe`** (since 07-27, emits `profile-v3` with `icache_fill`). An earlier note said "never adopted" — WRONG, it checked `emulators/melonds/` only. Drive it with `run-task37-profile-census.ps1 -MelonDS emulators\melonds-attributor\melonDS.exe -Build <dir> -NoBuild -StartFrame/-Frames matching that build's config`. Source: `D:\Stuff\DevFolder\melonDS-Accurate` (`r2-stall-attributor`, `4a1abf61`).
- **CODE PLACEMENT CLOSED 2026-08-14 ON MEASURED TEMPORAL EVIDENCE** (`…/2026-08-14_icache-temporal/ICACHE_TEMPORAL.md`; nothing built). The earlier "33.6x union footprint" verdict was **WITHDRAWN** — a whole-match union does not govern cache behaviour, and "evicted between calls regardless" was asserted without measuring the interval. Geometry VERIFIED from `melonDS-Accurate/src/CP15_Constants.h`: 8 KB / 32 B / 4-way / **64 sets** / **round-robin** (melonDS names WAYS "sets"). **v3: `icache_fill` = 339,275 ticks/frame, 29.7% of match, 37.5% of non-idle, 1.87x `issue`** — 20x the target. **But CAPACITY, measured**: sweeping the cutoff so set population varies, sets that FIT the 4 ways refill at 1,252 fill/1k vs 1,233 for oversubscribed (top-256); 1,407 vs 1,297 (top-512). **Room in a set does not make its lines survive.** Phase 3 DID refute the old claim — 3 hot clusters fit and are scattered over 160–713 KB, hottest edge 413 KB apart sharing 10 sets — **but they hold 0.14% of conflict.** Model gate passed only after the FIRST falsifier scored −2.3%, beating the principled layouts (it padded by a line, spreading sets); rebuilt to align bases to the 2048 B period → +27.8%.
- **HOT FOOTPRINT IS THE LIVE LANE AND IT SIZES ABOVE THE BAR** (`…/2026-08-14_hot-footprint/HOT_FOOTPRINT.md`, census only). Of **288,352 B in the 9,011 lines the match pays to fetch**: live 213,040 (73.9%), **literal pool 5,780 (2.0%, NOT removable — Thumb-1 needs the constants)**, **cold code 42,892 (14.9%, the real lever)**, padding 26,640. **ALWAYS split the pool out before quoting "dead-in-line" — raw 26.1% reads as ~88,000 ticks and is a confound**; objdump shows pool words as instructions and no PC profile reports them. Ceiling 218,820 B needed = 6,839 vs 9,011 lines = **24.1% = ~81,800 ticks**; realistic third-to-half = **25,000–40,000 ticks**. **Two objects hold 42% of all fetch**: `scene_backend.o` (61,664 B fetched, 47.3% exec) + `nds_renderer.o` (55,008, 48.6%). Main text 914,634 B, only **43.4% in functions that execute at all**. `ndsR2AnimValueQ` spends **81.4% of its own cycles fetching itself**; `.text.hot`+`.text.hot.draw` = 9,844 B is already **1.2x the cache**.
- **NEXT EXPERIMENT: `-freorder-blocks-and-partition`** (cold blocks → `.text.unlikely`). THREE GATES FIRST: (1) changes codegen, so NOT "same objects" — Boundary must re-verify gameplay/collision/RNG; (2) `.text` will likely GROW while *fetched* text shrinks — right trade, but price it against the GObj-cap RAM threshold; (3) confirm `.text.unlikely` is non-empty in the map for `-mthumb` ARMv5TE and reaches those two objects — **if empty, STOP, no build spent**. Primary evidence = v3 `stall_icache_fill` delta, NOT WORK-H. **Closed cheaply: clone bloat is 168 B total.** **Not the fetch lever:** 66,488 B of never-executed objects survive `--gc-sections` — zero fill cost, a RAM question. **Chase:** `_vfiprintf_r`/`__ssvfiscanf_r` are in the hot fetched set — newlib formatted I/O running in a battle match.
- **The 20.12 kernels' ARITHMETIC (slice 42)** — sub-floor, non-additive. **Local-matrix memo dead twice.** **Flower rigid-mask +3,200, wrong sign.** **token→asset_id MEMO dead** (Task 74). **Six more lanes closed by MEASUREMENT**, numbers in `…/2026-08-1{1_c122,2_c123}-rebank/SLICE4{5,6,8}.md`: `ndsRelocFinalizeLoadedFile` as gate; anim-cache arena growth (Rejects 0); `OTHR` ceiling; **BGM sizing**; **every memo is healthy**.

## RAM — price a change before writing it

`check-boot-headroom.ps1 -Build <dir>` after every lab build. Highest `fake_heap_start` proven to
boot **`0x02294804`**, lowest to fail **`0x02294b24`**. **Text counts as much as bss**; a failing
arm reads as a hung emulator. `gSYTaskmanGeneralHeap` free-min **72,188**, floor 32,768.
**2026-08-14 RAM:** HW wallpaper decode cache **153,600 -> 135,000 B**; `.main.bss -18,592 B`.
**2026-08-15 RAM — PHASE 2 SPENT, +146,560 B MEASURED** (`8cfbc2eaa2b`, `…/2026-08-15_framebuffer-collapse/PHASE2_FRAMEBUFFER.md`): `gSYFramebufferSets` `[2][230][320]` -> `[1][231][320]`, 294,400 -> 147,840 B. Same build dir both arms: **bss 1,453,544 -> 1,306,984 (-146,560 exact)**, text/data flat, `fake_heap_start 0x02269ee4 -> 0x02246264`, tick-HUD headroom **174,368 -> 320,928**. Reader set from the **LINKED ELF** (one reader: the Results photo wipe; one writer: `scmanager`'s `sizeof`-bounded clear); span re-derived from the wipe's own literals `+0x23f14`/`-640`/220 rows -> `base+7,060..+147,819`. Boundary GREEN. **Results tic 160 byte-identical across arms; the WIPE ITSELF is uncaptured and NEEDS THE OWNER'S EYE.**
**STATE THE ARM WITH THE HEADROOM.** Post-Phase-2, MEASURED at HEAD: proof/published-config arm **355,264** (`0x0223dc44`), tick-HUD arm **320,928** (`0x02246264`); the published root ROM still reads `0x02260724` = 213,216 because it has not been rebuilt since Phase 2, so its "359,776" is a PREDICTION. **The tick-HUD arm binds** — every gate figure is taken on it and it is 34,336 B larger. **2026-08-15 — `.rodata` COSTS THE ARENA TOO, MEASURED** (`…/2026-08-15_battlepack-pool/BATTLEPACK_POOL.md`). **THE BOOT LADDER IS NECESSARY AND NOT SUFFICIENT: read `gNdsTaskmanArenaChosenSize` after ANY static growth.** A `.incbin` pack of **+288,992 B** pushed the arena `0x150000 -> 0x140000`, `gNdsTaskmanArenaAllocFailCount 16` — *with 66,784 B of PROVEN boot headroom still left*. The arena is calloc'd from the libnds heap `fake_heap_start` bounds, so static image and arena size are the SAME bytes; against the match's measured 1,304,068 B taskman use a 0x140000 arena projects a general-heap low-water of **~6,652 vs the 32,768 floor**. **`.incbin` IS GONE, 2026-08-15** (`…/2026-08-15_battlepack-resident/`): the Fox blob is a NitroFS payload streamed into the arena at scene setup in 18×16 KB chunks, replacing the 262,144 B anim cache — **proven headroom 66,816 → 354,208 against a 355,104 flag-0 baseline, i.e. +896 B of image**, arena 0x150000, `AllocFailCount` 0. **ORDERING WAS NOT ENOUGH**: fighter setup stores 3,728 B into the arena *before* the first scene update, so a loader that merely allocated first was refused by **848 bytes** (`ArenaOverflowLastSize 287936`, `LastUsed 3728`, `Hits 0`) — the blob now owns `[0, RESERVE)` of every arena generation, carved at reservation, with `NDS_R2_BATTLEPACK_BLOB_BYTES` generated from the blob so it cannot drift. **The un-packed fighter loses the cache: `gNdsR2AnimCacheRejects` 0 → 126.** Per-fighter split costs **136 B** of dedup. **PHASE 8 PRICED IT AND THE RAM DOES NOT CLOSE (2026-08-15).** Restoring that cache beside the blob needs `287,936 + 262,144 = 550,080` reserve vs today's 292,032 = **+258,048 B of arena**; both fighters resident needs ~559,632 vs ~301,564 = **~258,068** — the same number. Phase 2's 146,560 is SPENT, Phase 1 is 21,600 unspent, Phase 4 is **≤80,096 of unsized candidates**, Phase 3's ~150 KB is **REFUTED**, Phase 5 unstarted: **optimistic ceiling 248,256, short of even the ONE-fighter case. No named phase or combination covers it** — the only `.bss` objects big enough are `sNdsAudioFgmCache` (proven *under*-sized) and `sNdsRelocSceneFileBuffer` ("already optimized once"). **The cheapest unpriced lever is the pack itself: the Fox blob is 287,904 B against the 262,144 B it evicts, 1.098× its own displacement.** A pack smaller than what it displaces closes this without buying a byte; the items-off re-pack (553,696 B both fighters, mismatch 0) is already proven. **Also: the pack was shipping DISK bit order** — the host probe's `normalize()` is pipeline 4a-4b only and 4c (`ndsRelocAObj16EncodeForNativeBitfields`) never ran, because the direct path skips `ndsRelocFinalizeLoadedFile`; a ROM data abort found it, phase 4's "mismatch 0" did not, because both sides used the same disk-order decoder. Fixed, `decode_script(native=)` added, falsifier: the blob decoded in disk order raises on **75 of 2,713** scripts, native **0**.
## Landed slices and the lanes they leave

**SLICE 43 — WITHDRAWN 2026-08-11, RE-MEASURED 2026-08-15, STILL `?= 0`.** Leak fixed; re-measure
`−17,152` rank-80, stack flat, no low-polygon frame (lead above); **the one remaining acceptance item
is the pixel pair, NOT taken.** Only `NDS_R2_FIGHTER_GX_COMPOSE_LAB=1` re-enables it, tickhud/proof
only. Leak note `nds_platform.c:3260-3294`, not `:3197`; `|| NDS_TICK_HUD` pinned by `check-gbi-decode-fixtures.ps1:2247`.
**SLICE 46 KEPT — 1,213,440 → 1,196,224** (`…/SLICE46.md`):
warm preload covered 57 of the 87 used ids; the measured 87, 4 per scene update, take **misses 32 →
2** and the arena 257,200 → 192,240 (it SHRINKS).

**SLICE 48 KEPT — read its SIZE, not its bank (`…/SLICE48.md`).** The FAT lane is **BGM**; **`AUD`
at 0.2% does NOT clear it** (a bucket brackets only its own thread). Shipped: worker created at
`MAIN_THREAD_PRIO + 1`, switched to `- 1` once playing; **deprioritizing during the MATCH was
REFUTED** (+8,064 wrong way). **SLICE 45 KEPT — 1,225,280 → 1,213,440** (`…/SLICE45.md`): resolves
16,002 → 1,143, **−12,160**. **The fighter LOCAL matrix build is NOT a P95 lane — refuted c122**;
the local-matrix memo is **DO-NOT-RETRY, killed twice**. **`SINT` is the fighter INTERRUPT proc with
`SCPU` nested, not an animation bucket** — mis-attributed an A/B in c119. **The zero-copy force-load
seam is REOPENED 2026-08-15:** `ftmain.c:4623` now takes the returned pointer (`6e93def43cd`).

**`SHDT` IS CLOSED — bar 47,424 tk/fr, not −26.6%: the band is the transform chain, four dirty flags
so nothing to memoise, band-only cuts saturate at 78,016, fixed point only**
(`…/2026-08-13_shdt-{broadphase,band-owner}/`, RESIDUE §4 row 1). **Its file-I/O co-fire is closed
too** (`…/2026-08-13_c-band-io/`): the **SOUND-EFFECT load, not the animation one** (anim prices **+0**).
**FGM IS CLOSED BY COUNTED MISSES, not inference (`…/2026-08-13_c-collision-stack/STACK.md`): 188 plays, 38 hits, 150 misses, 143 evicting resident data, ≤5 of 8 slots ever pinned. The victim rule's strict `<` made it a ONE-slot cache per size class; fixing that bought +9 hits = −764 P95-equivalent and REVERTED on its own falsifier. Working set 59 cues / 575,760 B vs 204,800, so a repartition extrapolates to ~−2,300 and TRADES class eligibility (dropped sounds). `FindPlanned` is REFUTED too — 1,188 entries a match, 13 tk/fr not 665; that profile PC range is `FindNormalized` inlined at a second site.**
A GATE LANE IS A MASK FOR THE PROFILE; a MECHANISM NEEDS NO MASK — sum over the region axis
(`analyze-io-lane-series.py`), worst-case-pairing first.

**Do not bring a micro-fix** — R2-06 E11: a load-frame-only ~8,000 cannot be banked. Clear ~16,000
in one change, or **use the `.data` route on ONE binary** (only if the change cannot alter gameplay
state). **Every change needs an engagement counter on BOTH sides**; slices 45, 46 and 48 were all
found by READING counters the code already kept, on the gate arm, for the first time. `.text.hot`
is closed both directions.
**R2-07 STRESS GATE PASSED — `…/2026-08-13_c-stress/STRESS_GATE.md`.** 3 successive matches, 5
entries, **Sudden Death UNFORCED** and played to a KO, `NO-FREEZE`, risk counters clean; the
five-minute match ran at 98.7% coverage for `WORK-H` 929,344 / **1,205,760** — **length does not
accumulate cost**. **BOTH ITS ANOMALIES ARE ATTRIBUTED, no build spent
(`…/2026-08-13_c-anim-anomalies/ANOMALIES.md`):**
- **Runaway — FIXED 2026-08-13 (`…/2026-08-13_c-animjoint-fix/ANIMJOINT_FIX.md`). There was no
  missing CLEAR; there was a missing SET.** `lbCommonAddDObjAnimJointAll` (`lb/lbcommon.c:785`) was
  an **empty stub** (`bx lr`), so `ftCommonGuardInitJoints` set `is_anim_joint` while every joint
  still held the GuardOn figatree. Five-minute arm: Figatree misreads **144 → 0** (it was 144 of
  144), runaway **50 → 0**; the runaway counter saw only 2/3 of the class — **48 of 144 decoded to
  a legal opcode and were silent**. **PRICE +49,216 P95** (bank above). Never loosen the bound.
- **AObj cliff = CAPACITY, not a leak — FIXED.** Four zero-growth stops against reuse firing 16-19/stop kills the leak theory; the shipping 1-minute arm already stood at **889/1,024**, and a LEDGER cannot be evicted (the repack has no spare bit) so capacity is the lever: **`NDS_AOBJ_EVENT32_NORMALIZED_MAX` 1024 → 2048**, +8,192 B bss, headroom **167,936**. **Corpus is 1,598 of 2,048 post-fix — 450 spare, 1.28x, NOT the 1,029/2x the source comment claimed (corrected 2026-08-13).**
**START PAUSES THE MATCH** and the old whole-window freeze hash could not see it — the watch hashes the TOP band and `-PressStartOnResults` presses only on a detected Results screen.
**WHISPY LOW-FPS FACE FIXED / OWNER-CONFIRMED 2026-08-14:** Slice 44 passed `allow_stale=TRUE` to dynamic stage bindings, freezing Whispy's world matrix for up to 8 presented frames despite 30 Hz DObj motion. Dynamic bindings now validate every frame while unchanged source keys still reuse. Control `m11` stayed **3872** through live scale 0.948/0.605/0.104/0.104/0.606/0.948/1.0; candidate tracks **3872/2464/416/416/2480/3872/4096**. Evidence: `artifacts/verification/2026-08-13_whispy-slice44-fix/WHISPY_FIX_EVIDENCE.md`.
## Measurement rules that change your FIRST action — board owns the rest

- **The sampler is bit-deterministic — never repeat a run.** Same ROM twice gives byte-identical
  buckets, so ANY cross-build delta is placement. `-Samples 1600 -RingDump`. A duplicate frame
  LABEL at a ring seam is warned; IDENTICAL payload is a stale read and always fatal, as is one
  away from a seam. **THE PLACEMENT FLOOR IS ~17,000 P95, NOT ±5,376** — 2026-08-13, three near-identical arms (`…_c-collision-stack`): the falsifier, carrying the candidate's bss and 16 fewer bytes of text, beat the control by **16,832**. Under ~17,000 a two-build comparison measures the linker; only a flag falsifier means anything. **Compare ELF SECTIONS, not the `.nds`** — NitroFS packs directory entries nondeterministically, so identical source can give 14 differing ROM bytes with `.text`/`.data`/`.rodata` identical.
- **Judge on `WORK-H`**; buckets locate, they never decide (floor ≥8,544). **`ALL` is
  VBlank-quantized** — it hid a +52,928 once. **1.85 cycles of `FTR` mean per byte of added ARM
  text.** **A bucket only sees its OWN thread** (slice 48).
- **A census row in tk/fr sizes a P50 lever, NOT a P95 one.** Slices 36/37 had equal mean self
  cost and P95 wins **2.45x apart**. **Presence is the tell** — and a lane that is BIMODAL at the
  percentile returns less than its mean (`RESIDUE.md` §1).
- **A route A/B is valid only for a change that cannot alter gameplay state
  (slice 41) AND only if the poke lands before the value is READ.** `-SetGlobals`
  fires at the first frame-complete marker; record what was actually applied or
  the control is the candidate relabelled (slice 48 got 1,102,208 on both arms).
- **Per-line/per-PC attribution BEFORE designing — no build, and it routinely names a different
  lever than the source reads like.** Slice 44's guard looked like a compare but four cold `ldr`s
  were 39%; 85.5% of `ndsAObjEvent32NormalizeScript` is two pointer scans; the whole of RESIDUE §4
  row 0 is inlined helpers a symbol census cannot see.
- **An arm that cannot produce the event reads 0 either way** — check the control differs first.
  **And a zero one level DOWNSTREAM of a rejected request reads exactly like a dead lane**: row
  2's four zeroes were correct readings of a request killed by a NULL script pointer upstream,
  and each was read as "nothing asks for this".

## Restart surface — parked items live on the board's **Parked** list

**PUSH IS UNBLOCKED — `a4100b7`, 2026-08-13**, the owner's "scrub the 16" (`OWNER_DECISIONS.md` §9):
16 Cargo blobs under `decomp/BattleShip-main/decomp/tools/` baked the build machine's user directory.
Untracked + gitignored, byte-identical on disk; they **stay in pushed HISTORY — the owner accepted.**
Scan is `git grep -l -i -e <owner-given-name> HEAD`, now **17 → 1**: the survivor, sm64's IDO
`usr/lib/copt`, is a FALSE POSITIVE. `AGENTS.md` still miscalls `/decomp/` gitignored — 26,260 tracked.

`AGENTS.md` owns the start-of-cycle commands; `docs/P1_EXECUTION_BOARD.md` is the only dynamic
queue; `docs/BUGS.md` carries the owner's verdicts — preserve their wording. A clean checkout builds
through `build.ps1`, not bare `make`: four of six `.inc` are gitignored and **`build.ps1`'s
generator is not run by `make`**. `make p1-tick` builds the measuring ROM, `make p1` the published
pair. Never pass `-j`, never override `MAKEFLAGS`, one build at a time, never build a published
target name for lab work. Preserve mode 163, renderer mode 9, mip 0, static textures, source
countdown, Dream Land water frame 0, Task 16 `1/1/1`. Never edit `decomp/`. Run
`New-Smash64DSSnapshot.ps1` last.
