# Meta Crystal — Source Venue and Metal Mario Integration

Stage completion contract over the existing native packet and source behavior. Current symptoms, candidate identity and closure state belong to `docs/BUGS.md` and the execution board.

## Preserve and reuse

Use the already registered Metal/Meta Crystal native packet, source map/collision and campaign bridge. The old “not started” and terrain sketch are not implementation specifications. Metal Mario's fighter material is a related consumer, not proof that the cave shares the same material program.

## Completion packages

**Venue profile.** Identify the exact source map/header and its collision, map objects, spawns, camera/blast bounds and music through the source ground table. Validate all source surfaces rather than inventing a shelf or overhang from memory. Qualify source foreground/background/materials in the selected native packet.

**Metal material and visibility.** Preserve the stage's source materials independently from the fighter's reflective treatment. Share generator/native machinery only where source state matches. No new effect/reflection algorithm is required merely because the venue name suggests crystals.

**Campaign fight.** Natural Samus/preceding source transition→Metal Mario intro/fight→next route must preserve run state, proper opponent and venue, countdown/timer rules, items as source specifies, camera and lower HUD. A debug map selection is only a local control.

**Scene lifetime.** Model/material/native assets and background/audio must remain valid through defeat/transition, then release at the real owning boundary. Charge variant, stage, items and temporary overlap together.

## Dependencies and lifetime

`../fighters/variants.md` owns Metal Mario behavior; P2-6 owns driver/tally; P2-2/texture admission owns capacity. A smaller two-participant fight is not automatically bounded by the VS stress scene when it loads unique background/variant assets.

## Natural-path proof

Host map/collision/profile comparison, original stage/material reference capture, real Metal Mario damage/defeat and next-scene handoff, actual music and repeated entry. Record actual resource/cadence with the variant rather than an empty venue.

Static collision parity covers source data, not moving collision or required visible pixels. Use `../P2-4-stage-production.md` for shared material/actor/scene/stress requirements. New texture/material corpus inputs require current captures for affected output; old packet admission cannot replace them.

- [ ] Source collision/map objects/bounds and spawn points match the selected profile.
- [ ] Every required static and dynamic visual, telegraph and audio element is present natively.
- [ ] Source movers/hazards and their children pass the specified natural-cycle interactions.
- [ ] Entry/exit resource ownership, actual resource/cadence/stress gates and required owner review pass.

## Source and retained evidence

Repository/source baseline: `907c46daffbec55477459cc56e83dfc9a417dabb` (September 10, 2026). This revision defines work and acceptance; it does not claim a new build or runtime pass. Current state belongs to `docs/P2_EXECUTION_BOARD.md`; owner symptoms belong to `docs/BUGS.md`.

- `decomp/BattleShip-main/decomp/src/gr`.
- `docs/p2/P2-4-stage-production.md`.
- `docs/p2/fighters/variants.md`.
- `decomp/BattleShip-main/decomp/src/sc/sc1pmode/sc1pgame.c: Metal entry`.
- `docs/p2/P2-6-one-player.md`.

[Pre-revision document and its source pins](https://github.com/rockenrooster/Smash64DS_Port/blob/907c46daffbec55477459cc56e83dfc9a417dabb/docs/p2/stages/meta-crystal.md). The bundle installer preserves that document verbatim under `docs/archive/P2_PLAN_BASELINE_2026-09-10/p2/stages/meta-crystal.md`. Use retained investigations only when relevant; superseded diagnoses are not new implementation instructions.
