**Follow Bug fixing workflow contained in `BUG_FIXING_PROCESS.md`.**
AI Agent should mark fixed items with **FIXED** prefix or a 20 word summary (or less) if not fixed yet, always using ** ** to encapsulate agent added text.
(Investigation details live in `docs/p2/BUG_NOTES.md`; this queue stays lean.)

treat anything not going through native renderer a failure.

**Every newly built ROM, including diagnostics, must exclude non-native renderer implementations. Runtime defaults and zero observed fallbacks are insufficient.**

Owner notes: This isn't meant to be comprehensive, just my quick observations:

Main Menu:
-1P mode not selectable — **Paused by owner; do not resume campaign work.**
-VS Mode is perfect and should be used as a reference as well as the main menu for the
-Data not selectable — **Native entry is documented; children and natural-path acceptance remain unverified. Keep behind match repairs.**

VS mode CSS:
-Kirby not selectable — **Shared CSS residency/storage blocker; integrate compact previews and classify the libfat stop before enabling the roster.**
-Jigglypuff not selectable — **Same shared CSS residency/storage blocker; no independent rendering fix established.**
-Ness not selectable — **Same shared CSS residency/storage blocker; no independent rendering fix established.**

For 1P mode CSS, look at the VS Mode CSS since they are VERY similar.

**Match rendering regressions (owner, 2026-09-08):**
-KO blast pillar — **OPEN pending recheck: the cause was the particle submitter dropping LBPARTICLE_FLAG_ENVCOLOR, collapsing the source (PRIM-ENV)xTEXEL+ENV ramp to one prim tint so only the bright ray cores survived. Fixed 2026-09-08 15:51 by `761cbdae046`, about seven hours AFTER the screenshot, so the report predates the fix. No post-fix KO has been observed; force one and read the env counters before closing.**
-Shield texture — **OPEN: cause measured, it is the PALETTE not the alpha. The source grades red-to-white per texel at 16 intensity levels; the live entry packet bakes A5I3, whose three index bits give only EIGHT pre-blended entries, and the shield's used ramp spans about 1.5 of them -- so a radial gradient reads as two flat concentric bands. Alpha refuted (A5I3 carries 32 levels against the source's flat alpha) and per-vertex collapse refuted (one quad, no strips). The 32-entry precedent already exists in the off-by-default quad path.**

Stages:
-peaches castle: NOT FIXED The steep red roof on the upper central tower is missing large portions of its polygon faces. Only several narrow triangular strips/edges render, leaving large transparent holes through the roof. The roof should form a continuous solid red pyramidal/conical surface around the tower. — **Near-plane and near-fan causes both measured dead 2026-09-08; runs admitted, 0 vertices behind near plane.** The colors on the geometry that does render appear correct. — **Still open, and now SUBMITTED-BUT-NOT-DRAWN is measured, not inferred: the stage puts 136 of its 136 emitted triangles into DIAG_OWNERTRI with every decline witness at zero, including a cull counter built for this. Narrowed twice on 2026-09-09. GENERATION IS LOSSLESS: roof root 0x2240 emits 56 triangles over 16 runs and the whole stage 136 over 12 roots, source count equal to emitted count on every one, with no drop policy reachable. Every one of those 16 runs then passes EVERY STATIC decline gate -- bindings resolve, alphas all 0xff, classes only 0 and 6, shifted coordinates inside +/-2048. And the stage records zero native failures, so the packet is admitted and drawn. What remains is a RUNTIME per-run decline or the silent `inside_count == 0` cull; read the PrepareRun and ValidateFull witnesses on a Castle frame.**
-Zebes: Acid plane STILL NOT FIXED. still looks like a low poly dome instead of a flat plane. Still get submerged a good amount before damage occurs. — **Still open; compare rendered plane with source root-plus-child contact height and fighter-root predicate on identical simulation ticks.**
-Mushroom kingdom: STILL MISSING THE LARGE SIDE BRICK PLATFORMS. Still renders at 20FPS. — **2026-09-09: the side bricks are listed, emitted AND submitted -- 176 of 176 stage triangles reach hardware with every decline witness at zero. Layer 1's roots 0x5C70 and 0x5E40 in FILE 107 carry 18 triangles / 3 runs each, and the whole layer-1 segment 82 triangles / 22 runs, bindings 12-14 of the emitted packet. They are the stepped side hills whose translates match the DokanL/DokanR map objects and the collision tiers exactly. So this is the THIRD stage -- with the Castle roof and the Yoster floor -- whose geometry is listed, emitted, admitted and still not drawn, which is a strong argument for one shared draw-side cause rather than three. Cadence remains separately open.**
-Yoshi's Island: TRANPARENCY STILL NOT BEING APPLIED IN TEXTURES/SPRITES (both spinning textures, all three clouds (which are also the wrong color), the white sparkle textures around the heart). also new thing i noticed is that part of the BGM sounds garbled, but like only one instrument. STILL MISSING THE MAIN PLATFORMS AND MAIN FLOOR PATH. — **Floor/platform: 164 of 164 emitted triangles reach hardware with every decline witness at zero, so it is submitted and not drawn. Same shape as the Castle roof: all 19 drawable roots are listed in the descriptor and emitted 1:1, the missing floor root 0x49A0 carries 155 commands / 77 triangles / 22 runs, every run passes every static gate, and the stage records zero native failures. Not a coverage or omission defect; it dies at draw. Still open: the garbled instrument, and see the transparency note below.** **2026-09-08: sparkles never admitted to the particle bank (P1 seam list only); cloud colour loses its env tint in the I4 converter.**
-SectorZ: Arwings are visible now, but do not act like platforms and the lasers do not spawn at correct locations. — **2026-09-09: the SPAWN MATH IS SOURCE-VERBATIM and is not the defect -- the port includes grsector.c literally, no 2D screen-space transform exists in the spawn path, every descriptor and joint offset matches reloc_data.us.h, and the suspicious axis literal 4 at grsector.c:702 is simply SYVECTOR_AXIS_Z. Laser attribute normalization is refuted too: it is applied after the position is set. Two candidates remain, both untestable statically: the Arwing model transforms feeding map_dobjs (a refused FoxSpecial3 joint leaves a stale basis with correct math), and the native laser draw, which pushes SIX BAKED vertices through its config matrices -- if those are camera-only, every laser draws in one wrong place while collisions stay right, which looks identical by eye.**
-saffron city: transparency around the pokemon garage door hazard thing is missing. REMOVE WHITE HAZE. — **2026-09-09: a claimed `silent empty draw` here was RETRACTED the same day. `DIAG_OWNERTRI` is indexed by NDSRendererProfileOwner (STAGE, MARIO, FOX, LUIGI...), not by stage owner_spec, so its zero slot was simply a fighter not in the match. The gate geometry IS in the packet: roots 0x0420 and 0x04f0 carry runs and triangles. The door transparency remains open on your own report and nothing else about it is established.** — **Door transparency remains open. 2026-09-09: the haze panel omission IS already in the generator -- `omitted_draw_roots=((112, "layer3", 2, 1, 0x8688),)` -- and its guarding test suite, which had been erroring in setUpClass since the gate commit and therefore proving nothing, was repaired the same day and is green. The door's own transparency sits at a different seam: the head-1 XLU policy and flat per-run alpha derivation for bindings 19/20, which renders the source's graded 0xdc panel opaque. The packet side is sound.**
