#ifndef _FTNLINK_STATUS_H_
#define _FTNLINK_STATUS_H_
/* P2-6 variant NLink: shares Link's special status table verbatim
 * (BattleShip ftmain.c:78-107 dFTMainSpecialStatusDescs). No motion/status
 * enums of its own; the kind slot reuses dFTLinkSpecialStatusDescs. */
#include <ft/ftchar/ftlink/ftlinkstatus.h>
#endif
