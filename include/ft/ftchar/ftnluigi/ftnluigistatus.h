#ifndef _FTNLUIGI_STATUS_H_
#define _FTNLUIGI_STATUS_H_
/* P2-6 variant NLuigi: shares Luigi's special status table verbatim
 * (BattleShip ftmain.c:78-107 dFTMainSpecialStatusDescs). No motion/status
 * enums of its own; the kind slot reuses dFTLuigiSpecialStatusDescs. */
#include <ft/ftchar/ftluigi/ftluigistatus.h>
#endif
